export const metadata = {
  title: 'Bandpass signals & I/Q canonical form — η γέφυρα προς τη modulation',
  description:
    'Baseband vs bandpass σήματα, Hilbert transform ως phase-shifter στη συχνότητα, pre-envelope και complex envelope, και η canonical I/Q μορφή που ξεδιπλώνει AM/DSB-SC/SSB/FM/PM ως πέντε γραμμές ενός πίνακα. 5-stage loop (νιώσε / δες / συμπύκνωσε / ανακάλεσε / αναγνώρισε), bespoke vizzes για κάθε hard concept, και προεπισκόπηση των downstream εφαρμογών σε όλη την modulation και τη bandpass PSD.',
}

export const frontmatter = {
  title: 'Bandpass signals & I/Q canonical form',
  slug: 'modulation/bridge',
  order: 1,
  prerequisites: [
    'foundations/signals',
    'foundations/systems',
    'foundations/fourier-series',
    'foundations/fourier-transform',
    'foundations/filters',
  ],
  examWeight: 5,
  estimatedReadTime: 35,
  lastUpdated: '2026-05-25',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'
import Link from 'next/link'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Modulation · Bridge</span>
  <span>·</span>
  <span>~35 min read</span>
</div>

# Bandpass signals & I/Q canonical form

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="modulation/bridge" />
  <Bookmark id="modulation/bridge" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc
  sources={[
    { pdf: 'SE_session7&8_theory_2025.pdf', slides: '17–35', label: 'Hilbert, bandpass signals, complex envelope, I/Q canonical form, X(f) ↔ G(f) σχέση' },
    { pdf: 'SE_session11&12&13.pdf', slides: '1–9', label: 'γιατί διαμορφώνουμε, οικογένεια AM' },
  ]}
/>

Αυτή είναι η **γέφυρα από τα Foundations στη modulation**. Ένα κεφάλαιο, μία δουλειά: ονομάζουμε επιτέλους τη μεγάλη οικογένεια σημάτων που δουλεύει όλο το υπόλοιπο μάθημα — τα **bandpass signals** — και βρίσκουμε τη μία **canonical μορφή** που τα γράφει όλα.

Όταν τελειώσει αυτή η σελίδα, η AM/FM/PM/SSB/DSB-SC δεν θα είναι πέντε ξεχωριστά μυστήρια — θα είναι **πέντε γραμμές ενός πίνακα**, διαφορετικές επιλογές για δύο baseband σήματα $x_I$ και $x_Q$. Κάθε επόμενο κεφάλαιο της modulation θα ξεκινάει quotάροντας μία γραμμή αυτού του πίνακα.

## 1. Νιώσε — η σελίδα που ξεκλειδώνει όλη τη modulation

Πριν μπούμε στη μηχανική: γιατί σήμερα δαπανάμε ένα ολόκληρο κεφάλαιο πάνω σε τέσσερις τύπους (Hilbert, pre-envelope, complex envelope, I/Q) πριν καν δεις πρώτη AM εξίσωση;

Η σύντομη απάντηση: επειδή **κάθε επόμενο κεφάλαιο της modulation γράφεται με αυτή τη γλώσσα**. Αν την έχεις, τα επόμενα κεφάλαια είναι παραλλαγές μιας ιδέας. Αν δεν την έχεις, η AM, η DSB-SC, η SSB, η FM και η PM φαίνονται σαν πέντε ασυσχέτιστα μυστήρια — με τη χειρότερη συνέπεια ότι οι ασκήσεις των εξετάσεων που συνδυάζουν δύο από αυτές μοιάζουν με δύο φορές δυσκολότερες από όσο είναι.

Τέσσερα συγκεκριμένα σημεία του υπόλοιπου μαθήματος όπου χρειάζεσαι ΑΚΡΙΒΩΣ αυτή τη γλώσσα:

- **Όλες οι AM-παραλλαγές γίνονται γραμμές ενός πίνακα.** Το **Συμβατικό AM** είναι «πάρε $x_I(t) = A_c + m(t)$ και $x_Q(t) = 0$». Η **DSB-SC** είναι «$x_I(t) = m(t)$, $x_Q(t) = 0$». Η **SSB** χρησιμοποιεί $\hat{m}(t)$ (τον Hilbert του $m$) ως $x_Q$. Η **FM** είναι «κράτα σταθερή την περιβάλλουσα $V(t) = A_c$ και διαμόρφωσε τη φάση $\theta(t)$». Πέντε διαμορφώσεις, ένας πίνακας §6b.
- **Σφάλμα φάσης σε σύμφωνη αποδιαμόρφωση = I/Q rotation.** Στις εξετάσεις βλέπεις συχνά «αν ο τοπικός ταλαντωτής έχει σφάλμα φάσης $\varphi$, πώς εξασθενεί η έξοδος του DSB-SC;» (π.χ. Πρόοδος Απρ 2026 Θέμα 7). Η απάντηση γίνεται μία γραμμή στην I/Q γλώσσα: το $\varphi$ «στρίβει» το $x_I$ ώστε ένα μέρος του να διαρρεύσει στο $x_Q$ — εκεί που το LPF δεν θα το βρει. Χωρίς I/Q, η ίδια απάντηση χρειάζεται μισή σελίδα τριγωνομετρίας.
- **SSB = παίρνεις την pre-envelope του μηνύματος.** Όταν αργότερα δεις «εκπέμπουμε μόνο την upper sideband και απαλείφουμε τη lower», αυτό που πραγματικά κάνεις είναι «κατασκεύασε το $m_p(t) = m(t) + j\hat{m}(t)$ — έχει φάσμα μόνο στις θετικές συχνότητες — και μετά μετατόπισέ το στο $+f_c$». Η §3 αυτής της σελίδας είναι ο ορισμός· η SSB είναι η εφαρμογή.
- **Bandpass PSD και θόρυβος μέσα σε ραδιοδέκτη.** Στο κεφάλαιο της randomness/PSD θα δεις τη σχέση $S_x(f) = \tfrac{1}{4}[S_g(f-f_c) + S_g(-f-f_c)]$, που βγαίνει αυτόματα από τη σχέση $X(f) = \tfrac{1}{2}[G(f-f_c) + G^*(-f-f_c)]$ της §7 παρακάτω. Το «πώς διαβάζω τη PSD ενός θορύβου μέσα σε bandpass receiver» είναι αυτή η σχέση εφαρμοσμένη πάνω σε random g(t).

<Callout type="key" title="Τι θέλει να σου περάσει αυτή η σελίδα">
  Τέσσερα εργαλεία που από εδώ και πέρα είναι το «λεξιλόγιο της modulation»:

  1. **Hilbert** $\hat{x}(t)$ — phase-shifter $-\pi/2$ για όλες τις θετικές συχνότητες. Το μέτρο μένει αμετάβλητο.
  2. **Pre-envelope** $x_p(t) = x(t) + j\hat{x}(t)$ — έχει φάσμα **μόνο στις θετικές συχνότητες**.
  3. **Complex envelope** $g(t) = x_p(t) e^{-j 2\pi f_c t}$ — η pre-envelope μεταφερμένη πίσω στο baseband.
  4. **Canonical I/Q μορφή** $x(t) = x_I(t)\cos(2\pi f_c t) - x_Q(t)\sin(2\pi f_c t)$ — με $g(t) = x_I(t) + j x_Q(t)$.

  Κάθε επόμενο κεφάλαιο διαλέγει διαφορετική $g(t)$. Τίποτα άλλο. **Αυτό είναι το ξεκλείδωμα.**
</Callout>

## 2. Baseband vs Bandpass — δύο οικογένειες σημάτων

Από το [κεφάλαιο του Fourier transform](/foundations/fourier-transform#7-modulation-theorem-) ξέρουμε ότι ο πολλαπλασιασμός με $\cos(2\pi f_c t)$ μετατοπίζει το φάσμα στις $\pm f_c$. Αυτή η κίνηση παράγει μια ολόκληρη οικογένεια σημάτων που μας ενδιαφέρει εδώ.

**Σήμα Βασικής Ζώνης (baseband):** το φάσμα είναι **συγκεντρωμένο γύρω από το 0**. Μαθηματικός ορισμός (slide 21): $X(f) = 0$ για $|f| \geq W$. Ζει στις χαμηλές συχνότητες. Παραδείγματα: η ηχητική πίεση από ένα μικρόφωνο, ένα τηλεοπτικό composite video signal, μια σύνθεση μουσικής. Συνηθισμένο εύρος ζώνης (bandwidth) $W$: λίγα kHz για φωνή, λίγα MHz για βίντεο.

**Ζωνοπερατό σήμα (bandpass / passband):** το φάσμα είναι **συγκεντρωμένο γύρω από κάποιο $\pm f_c$**, μακριά από το 0. Μαθηματικός ορισμός (slide 23): $X(f) = 0$ για $|f - f_c| \geq W$. Παραδείγματα: AM εκπομπή στα 1 MHz, FM εκπομπή στα 100 MHz, σήμα Wi-Fi στα 2.4 GHz. Το $f_c$ λέγεται **carrier frequency** (συχνότητα φέροντος).

**Real-world σύγκριση:**

| Σήμα | Τύπος | Bandwidth | Carrier (αν υπάρχει) |
| --- | --- | --- | --- |
| Ανθρώπινη φωνή | Baseband | ~3–4 kHz | — |
| AM ραδιόφωνο | Bandpass | ~10 kHz | 540–1700 kHz |
| FM ραδιόφωνο | Bandpass | ~200 kHz | 88–108 MHz |
| Wi-Fi (2.4 GHz) | Bandpass | ~20–40 MHz | 2.4 GHz |
| 4G LTE | Bandpass | ~1.4–20 MHz | 700 MHz – 2.6 GHz |

Σχεδόν **καθετί που συναντάς στον αέρα είναι bandpass**. Το baseband εμφανίζεται μόνο στα δύο άκρα — στην πηγή (το audio που βγάζει το μικρόφωνο) και στον προορισμό (μετά τη demodulation στον δέκτη). Στη μεταφορά, το σήμα είναι πάντα bandpass.

<Callout type="key" title="Narrowband: η συνηθισμένη υπόθεση">
Σχεδόν πάντα το bandwidth του σήματος $W$ είναι **πολύ μικρότερο** από το carrier $f_c$ — δηλαδή $W \ll f_c$. Αυτό λέγεται **narrowband (σήμα στενής ζώνης)** στις διαφάνειες (slide 23). Π.χ. για AM ραδιόφωνο: $W \approx 10$ kHz, $f_c \approx 1$ MHz, λόγος $\sim 1{:}100$. Για Wi-Fi: $W \approx 20$ MHz, $f_c \approx 2.4$ GHz, λόγος $\sim 1{:}120$. Αυτή η υπόθεση μάς αφήνει να μεταχειριστούμε το $f_c$ σαν σταθερά σε πολλούς υπολογισμούς, και είναι υπόρρητη σε όλα τα παρακάτω.
</Callout>

<Callout type="intuition" title="Γιατί δεν εκπέμπουμε baseband — slide 5 με νούμερα">
Οι διαφάνειες (SE_session11&12&13, slide 5) δίνουν το πιο πειστικό επιχείρημα: το μέγεθος μιας κεραίας είναι περίπου $\lambda/2$ του μήκους κύματος που εκπέμπει. Για baseband ανθρώπινη ομιλία με $f \approx 20$ kHz:

$$
\frac{\lambda}{2} = \frac{\,3 \times 10^8 \text{ m/sec}\,}{2 \cdot 2 \times 10^4 \text{ Hz}} = 7.5 \text{ km}
$$

7.5 km κεραία στην ταράτσα κάθε σπιτιού — προφανώς αδύνατο. Αντίθετα για AM στα $f_c = 1$ MHz, $\lambda/2 = 150$ m· για FM στα 100 MHz, $\lambda/2 = 1.5$ m (αυτή την έχεις στο αυτοκίνητο). Για Wi-Fi στα 2.4 GHz, $\lambda/2 \approx 6$ cm. **Η διαμόρφωση δεν είναι μαθηματική φαντασίωση — είναι η μόνη πρακτική λύση** για να ζωντανέψουν χιλιάδες ταυτόχρονοι χρήστες στον αέρα. Άλλες δύο σημαντικές αιτίες (από τα slides): **πολυπλεξία** (κινητή τηλεφωνία — πολλοί χρήστες σε ίδιο κανάλι) και **απονομή συχνοτήτων** (ραδιοφωνία/τηλεόραση — κάθε σταθμός σε δικό του $f_c$).
</Callout>

<Callout type="connection" title="Σχέση με το modulation theorem">
Από τον FT: η AM-style διαμόρφωση $y(t) = x(t)\cos(2\pi f_c t)$ μετατοπίζει το $X(f)$ από baseband στις $\pm f_c$. Δηλαδή τα bandpass signals **δεν** είναι νέο είδος σημάτων — είναι αυτό που παίρνεις όταν διαμορφώνεις. Σε αυτό το κεφάλαιο δίνουμε στην οικογένεια αυτή το σωστό όνομα και μια canonical μορφή για να τη δουλέψεις.
</Callout>

## 3. Μετασχηματισμός Hilbert — phase-shifter όλων των συχνοτήτων

Πριν χτίσουμε την canonical μορφή χρειαζόμαστε ένα νέο εργαλείο. Ο **Μετασχηματισμός Hilbert** $\hat{x}(t) = \mathcal{H}\{x(t)\}$ ορίζεται στον χρόνο σαν συνέλιξη με $h(t) = \tfrac{1}{\pi t}$ (slide 17):

$$
\hat{x}(t) = x(t) * \frac{1}{\pi t} = \frac{1}{\pi}\int_{-\infty}^{\infty} \frac{x(\tau)}{t - \tau}\, d\tau \quad \text{✓ Στο τυπολόγιο}
$$

Αλλά αυτή η ολοκληρωτική μορφή δεν είναι η διαισθητική. **Στη συχνότητα ο Hilbert είναι πολύ πιο καθαρός:**

$$
\boxed{\,\mathcal{H}\{x(t)\} \overset{\mathcal{F}}{\longleftrightarrow} -j\,\mathrm{sgn}(f)\, X(f)\,} \quad \text{✓ Στο τυπολόγιο}
$$

Με λόγια: **πολλαπλασιάζει τις θετικές συχνότητες με $-j$** (= phase shift $-\pi/2$) **και τις αρνητικές με $+j$** (= phase shift $+\pi/2$). Το **μέτρο** του φάσματος **δεν αλλάζει** ($|{-j\,\mathrm{sgn}(f)}| = 1$). Αλλάζει μόνο η φάση, ομοιόμορφα ανά πλευρά. Είναι **ένας τέλειος all-pass phase shifter**.

<Callout type="warning" title="«Θετικές συχνότητες» — όχι «θετικά σήματα»">
Είναι εύκολο μπέρδεμα: το «$-\pi/2$ για $f > 0$» αναφέρεται στον **άξονα συχνοτήτων**, όχι στο πρόσημο του ίδιου του σήματος. Όλες οι frequencies στο φάσμα έχουν θέση πάνω στον $f$-άξονα — οι «θετικές» είναι αυτές με $f > 0$. Δεν έχει σημασία αν το $x(t)$ είναι θετικό ή αρνητικό σε κάποια στιγμή.
</Callout>

### 3a. Παράδειγμα: cos → sin

Ένα cosine έχει φάσμα δύο κρούσεων: $\tfrac{1}{2}\delta(f - f_0) + \tfrac{1}{2}\delta(f + f_0)$ (από τη Section 4e του [FT chapter](/foundations/fourier-transform#4e-cos2π-f-t-δύο-κρούσεις-στις-f)). Εφαρμόζοντας Hilbert με τον τύπο της συχνότητας:

- Στο $+f_0$: $\tfrac{1}{2}$ πολλαπλασιάζεται με $-j$ → γίνεται $-j/2$
- Στο $-f_0$: $\tfrac{1}{2}$ πολλαπλασιάζεται με $+j$ → γίνεται $+j/2$

Το φάσμα $\tfrac{j}{2}\delta(f+f_0) - \tfrac{j}{2}\delta(f-f_0)$ είναι **ακριβώς το φάσμα του $\sin(2\pi f_0 t)$** (όπως δείξαμε στη Section 4.5 του FT chapter). Άρα:

$$
\mathcal{H}\{\cos(2\pi f_0 t)\} = \sin(2\pi f_0 t)
$$

Ο Hilbert γυρίζει το cosine σε sine. Συμμετρικά: $\mathcal{H}\{\sin(2\pi f_0 t)\} = -\cos(2\pi f_0 t)$. Δύο εφαρμογές δίνουν $\mathcal{H}\{\mathcal{H}\{x\}\} = -x$ (αναμενόμενο — δύο πολλαπλασιασμοί με $-j$ κάνουν $-1$).

<HilbertTransformViz />

### 3b. Βασικά ζεύγη Hilbert — ο πίνακας από slide 18

Το slide 18 δίνει έναν πίνακα από κανονικά ζεύγη Hilbert. Έχει αξία να τον δεις γιατί η μορφή κάποιων από αυτά εμφανίζεται σε ασκήσεις (ειδικά οι παλμοί $\Pi$ και $\mathrm{sinc}$, που είναι τα βασικά μηνύματα στη SSB):

| $x(t)$ | $\hat{x}(t) = \mathcal{H}\{x(t)\}$ |
| --- | --- |
| $a_1 x_1(t) + a_2 x_2(t)$ | $a_1 \hat{x}_1(t) + a_2 \hat{x}_2(t)$ (γραμμικότητα) |
| $x(t - t_0)$ | $\hat{x}(t - t_0)$ (χρονική μετατόπιση μετακυλίεται) |
| $x(at)$, $a \in \mathbb{R}$ | $\mathrm{sgn}(a)\,\hat{x}(at)$ |
| $\dfrac{dx(t)}{dt}$ | $\dfrac{d\hat{x}(t)}{dt}$ (διανέμεται πάνω σε derivative) |
| $\cos t$ | $\sin t$ |
| $\sin t$ | $-\cos t$ |
| $e^{jt}$ | $-j\, e^{jt}$ |
| $e^{-jt}$ | $j\, e^{-jt}$ |
| $\delta(t)$ | $\dfrac{1}{\pi t}$ |
| $\dfrac{1}{\pi t}$ | $-\delta(t)$ (δύο εφαρμογές δίνουν $-\delta$) |
| $\Pi(t)$ | $\dfrac{1}{\pi}\log\left\lvert\dfrac{2t+1}{2t-1}\right\rvert$ |
| $\mathrm{sinc}(t)$ | $\dfrac{1 - \cos(\pi t)}{\pi t}$ |

**Διάβασέ τον έτσι.** Οι γραμμές 1–4 είναι **ιδιότητες** (γραμμικότητα, χρονική μετατόπιση, κλιμάκωση, παράγωγος) — όχι ζεύγη. Οι υπόλοιπες γραμμές είναι συγκεκριμένα **παραδείγματα** που μπορείς να χρησιμοποιήσεις απευθείας. Το $\cos t \to \sin t$ είναι αυτό που μόλις απέδειξες. Το $\Pi(t) \to (1/\pi)\log|(2t+1)/(2t-1)|$ είναι η αιτία που η SSB ενός τετραγωνικού παλμού δίνει «λογαριθμικές κορυφές» στα άκρα. Το $\mathrm{sinc}(t) \to (1-\cos \pi t)/(\pi t)$ εμφανίζεται όταν το μήνυμα είναι sinc (bandlimited tile message).

<Callout type="note" title="Γιατί ο πίνακας του Hilbert «μοιάζει» με του Fourier">
Σαν τον πίνακα Fourier pairs, ο πίνακας Hilbert έχει μερικά «βιαστικά» ζεύγη μαζί με «βαρετές» ιδιότητες. Όπως στο FT, αυτό **δεν** σημαίνει ότι πρέπει να τον αποστηθίσεις. Σημαίνει ότι αν δεις στα γραπτά να σου ζητούν τον Hilbert ενός παράξενου σήματος (π.χ. $\Pi(t)$), η σύσταση είναι: ή αναγνώρισέ το από αυτή τη γραμμή ή πέσε πίσω στη φασματική σχέση $-j\,\mathrm{sgn}(f) X(f)$.
</Callout>

### 3c. Άλλες χρήσιμες ιδιότητες

- **Διπλή εφαρμογή:** $\mathcal{H}\{\mathcal{H}\{x\}\} = -x$
- **Ορθογωνιότητα:** $x(t)$ και $\hat{x}(t)$ είναι ορθογώνια ως σήματα (μηδενική συσχέτιση)
- **Γραμμικότητα:** ο Hilbert είναι γραμμικός
- **Διανέμεται πάνω σε convolution:** $\mathcal{H}\{x * y\} = \hat{x} * y = x * \hat{y}$
- **Διατήρηση ενέργειας:** $\int |\hat{x}(t)|^2\, dt = \int |x(t)|^2\, dt$ (το μέτρο φάσματος δεν αλλάζει)

### 3d. Γιατί τον χρειαζόμαστε εδώ

Επειδή στη §4 χτίζουμε το **pre-envelope** $x_p(t) = x(t) + j\hat{x}(t)$, και αποδεικνύουμε ότι το φάσμα του είναι **μηδέν στις αρνητικές συχνότητες**. Αυτό μόνο ο Hilbert το κάνει — οποιαδήποτε άλλη συνάρτηση δεν θα ακύρωνε ακριβώς τη μία πλευρά. Η pre-envelope με τη σειρά της είναι το ένα από τα δύο βασικά υλικά της complex envelope, που είναι η καρδιά της canonical I/Q μορφής. Όλα σχετίζονται με αυτόν τον έναν τύπο.

## 4. Pre-envelope $x_p(t)$ — μονόπλευρο φάσμα

Πάρε ένα real signal $x(t)$ και τον Hilbert του $\hat{x}(t)$. Συνδύασέ τα σε ένα **μιγαδικό** σήμα:

$$
\boxed{\,x_p(t) = x(t) + j\,\hat{x}(t)\,}
$$

Τι είναι το φάσμα του; Από γραμμικότητα του FT και την ιδιότητα του Hilbert ($\hat{x} \leftrightarrow -j\,\mathrm{sgn}(f)\,X(f)$):

$$
X_p(f) = X(f) + j\cdot[-j\,\mathrm{sgn}(f)]\,X(f) = X(f) + \mathrm{sgn}(f)\,X(f) = (1 + \mathrm{sgn}(f))\,X(f)
$$

Δηλαδή:
- Για $f > 0$: $X_p(f) = 2X(f)$ — η θετική πλευρά **διπλασιάζεται**.
- Για $f < 0$: $X_p(f) = 0$ — η αρνητική πλευρά **εξαφανίζεται**.
- Για $f = 0$: $X_p(0) = X(0)$.

<PreEnvelopeSpectrumViz />

<Callout type="intuition" title="Γιατί η αρνητική πλευρά πεθαίνει">
Διαισθητικά: το $j\hat{x}(t)$ έχει μετατοπίσει τη φάση κάθε συχνότητας κατά $\pm\pi/2$. Στις θετικές συχνότητες αυτό εξευρετικά **προστίθεται** πάνω στο original $X(f)$ (συντονισμός = ×2). Στις αρνητικές συχνότητες αυτό **αναιρεί** το original (καταστροφική ακύρωση = 0). Είναι το ίδιο μηχανικό φαινόμενο που στρώνει την παλίρροια όταν δύο κύματα συναντηθούν σε αντίθετη φάση.
</Callout>

<Callout type="note" title="Γιατί ένα real signal δίνει complex pre-envelope">
Παρατήρησε ότι δίνουμε ένα real signal $x(t)$ και παίρνουμε ένα complex signal $x_p(t)$. Αυτό **δεν** είναι λάθος — είναι σκόπιμο. Έχουμε αλλάξει αναπαράσταση: real-στον-χρόνο/two-sided-στη-συχνότητα ↔ complex-στον-χρόνο/one-sided-στη-συχνότητα. Καμία πληροφορία δεν χάθηκε (η αρνητική πλευρά του real spectrum ήταν ούτως ή άλλως ο μιγαδικός συζυγής της θετικής, οπότε redundant). Η ίδια συζήτηση από διαφορετική γωνία ζει στη [/reference/spectrum-conventions](/reference/spectrum-conventions).
</Callout>

## 5. Complex envelope $g(t)$ — το demodulated baseband ισοδύναμο

Το $X_p(f)$ είναι one-sided αλλά το «λούτσο» του ζει γύρω από το $+f_c$. Το **complex envelope** $g(t)$ (μιγαδική περιβάλλουσα) είναι αυτό το pre-envelope, **κατεβασμένο πίσω στο baseband** με πολλαπλασιασμό με $e^{-j 2\pi f_c t}$:

$$
\boxed{\,g(t) = x_p(t)\, e^{-j 2\pi f_c t}\,}
$$

Τι κάνει αυτό στη συχνότητα; Πολλαπλασιασμός με $e^{-j 2\pi f_c t}$ μετατοπίζει το φάσμα κατά $-f_c$ (frequency-shift property από το FT chapter). Άρα $G(f) = X_p(f + f_c)$ — πιάνουμε το one-sided $X_p$ που ζούσε γύρω από το $+f_c$ και το **ολισθαίνουμε στο 0**. Πίσω στο baseband, αλλά τώρα με ένα **complex-valued** σήμα.

**Αντίστροφη κατεύθυνση.** Από τον ορισμό $g(t) = x_p(t)\, e^{-j 2\pi f_c t}$, λύνοντας:

$$
x_p(t) = g(t)\, e^{j 2\pi f_c t}
$$

Και επειδή $x(t) = \mathrm{Re}\{x_p(t)\}$ (το real σήμα είναι το real part του pre-envelope):

$$
\boxed{\,x(t) = \mathrm{Re}\{g(t)\, e^{j 2\pi f_c t}\}\,}
$$

**Αυτή είναι η canonical μορφή κάθε ζωνοπερατού σήματος.** Κάθε διαμόρφωση που θα δούμε (AM, DSB-SC, SSB, FM, PM) είναι **μια συγκεκριμένη επιλογή του $g(t)$**. Το complex envelope είναι το πιο συμπαγές πακέτο πληροφορίας: μέτρο = envelope, φάση = phase, και τα δύο μαζί συντίθενται με το carrier για να σου δώσουν το real bandpass signal.

<Callout type="key" title="Όταν το g(t) είναι πραγματικό — slide 32">
Μια χρήσιμη ειδική περίπτωση που εμφανίζεται **σε όλο το AM**: αν το $g(t)$ είναι πραγματικό (δηλαδή το $x_Q(t) = 0$), τότε η canonical μορφή απλοποιείται σε:

$$
x(t) = g(t)\,\cos(2\pi f_c t)
$$

Δηλαδή: ένα **πραγματικό** baseband «μήνυμα» πολλαπλασιασμένο με το carrier. Αυτή είναι η μορφή που αναγνωρίζεις από το modulation theorem. **Όλη η AM γράφεται έτσι** — Συμβατικό AM έχει $g(t) = A_c + m(t)$, DSB-SC έχει $g(t) = m(t)$. Η §6b θα δείξει αυτές τις γραμμές του πίνακα ρητά.
</Callout>

## 6. I/Q components — η canonical μορφή που ξεδιπλώνει τα πάντα

Φτάσαμε στο πιο σημαντικό μέρος του κεφαλαίου. Αναλύουμε το $g(t)$ σε real και imaginary part:

$$
g(t) = x_I(t) + j\, x_Q(t)
$$

όπου $x_I(t) = \mathrm{Re}\{g(t)\}$ και $x_Q(t) = \mathrm{Im}\{g(t)\}$ είναι **και τα δύο real-valued, baseband** σήματα. Οι διαφάνειες (slide 30) δίνουν την Ελληνική ορολογία verbatim:

- $x_I(t)$ — **Συμφασική (In-phase)** συνιστώσα: φάση 0 με το carrier $\cos(2\pi f_c t)$.
- $x_Q(t)$ — **Ορθογώνια (Quadrature)** συνιστώσα: φάση $-\pi/2$ με το carrier — γι' αυτό πολλαπλασιάζεται με $\sin = \cos(\cdot - \pi/2)$.

Αντικαθιστώντας στο $x(t) = \mathrm{Re}\{g(t)\, e^{j 2\pi f_c t}\}$ και αναπτύσσοντας το γινόμενο:

$$
x(t) = \mathrm{Re}\{(x_I + jx_Q)(\cos(2\pi f_c t) + j\sin(2\pi f_c t))\}
$$

Κρατώντας το real part: $\mathrm{Re}\{x_I\cos + jx_I\sin + jx_Q\cos - x_Q\sin\} = x_I\cos - x_Q\sin$. Άρα:

$$
\boxed{\,x(t) = x_I(t)\,\cos(2\pi f_c t) - x_Q(t)\,\sin(2\pi f_c t)\,} \quad \text{⚠️ Πρέπει να θυμάσαι — δεν δίνεται στο τυπολόγιο}
$$

<Callout type="warning" title="Το μείον δεν είναι λάθος πληκτρολόγησης">
Ο κανόνας είναι **$x_I\cos - x_Q\sin$** με **μείον**. Πολλοί φοιτητές γράφουν λάθος «+» στις εξετάσεις. Το μείον προκύπτει επειδή ο πολλαπλασιασμός $(x_I + jx_Q)\cdot(\cos + j\sin)$ δίνει στο real part: $x_I\cos + j^2 x_Q\sin = x_I\cos - x_Q\sin$ (το $j^2 = -1$ έδωσε το μείον). Γνήσιο και consequential — αλλάζει το πρόσημο της phase modulation αν το ξεχάσεις.
</Callout>

<Collectible id="iq-skin" />

### 6a. Πολική μορφή: envelope και phase

Σε πολική μορφή, $g(t) = V(t)\, e^{j\theta(t)}$ όπου:

- **Περιβάλλουσα (envelope):** $V(t) = |g(t)| = \sqrt{x_I^2(t) + x_Q^2(t)}$
- **Στιγμιαία φάση:** $\theta(t) = \arg(g(t)) = \arctan\!\left(\dfrac{x_Q(t)}{x_I(t)}\right)$

Τότε το bandpass γράφεται σε πολική μορφή:

$$
x(t) = V(t)\,\cos(2\pi f_c t + \theta(t))
$$

— **Ένα κύμα carrier του οποίου το πλάτος είναι $V(t)$ και η στιγμιαία φάση είναι $\theta(t)$.** Αυτή η εικόνα είναι κρίσιμη: κάθε διαμόρφωση «κουνάει» είτε το $V$ είτε το $\theta$ είτε και τα δύο.

**Από την πολική μορφή πίσω στο I/Q.** Οι δύο αναπαραστάσεις είναι ισοδύναμες:

$$
x_I(t) = V(t)\cos\theta(t), \quad x_Q(t) = V(t)\sin\theta(t)
$$

### 6b. Πέντε διαμορφώσεις, μία canonical μορφή

Ο πίνακας που θα ξαναδείς σε **κάθε επόμενο κεφάλαιο** της modulation:

| Διαμόρφωση | $x_I(t)$ | $x_Q(t)$ | $V(t)$ | $\theta(t)$ |
| --- | --- | --- | --- | --- |
| **Συμβατικό AM** (slide 32) | $A_c + m(t)$ | $0$ | $\lvert A_c + m(t)\rvert$ | $0$ ή $\pi$ |
| **DSB-SC** | $m(t)$ | $0$ | $\lvert m(t)\rvert$ | $0$ ή $\pi$ |
| **SSB (USSB)** | $\tfrac{1}{2} m(t)$ | $-\tfrac{1}{2} \hat{m}(t)$ | μεταβάλλεται | μεταβάλλεται |
| **FM** | $A_c\cos\phi(t)$ | $-A_c\sin\phi(t)$ | $A_c$ (σταθερό) | $\phi(t)$ |
| **PM** | $A_c\cos(k_p m(t))$ | $-A_c\sin(k_p m(t))$ | $A_c$ (σταθερό) | $k_p m(t)$ |

**Διαβάζοντας τον πίνακα.**

- **AM/DSB-SC** έχουν $x_Q = 0$. Όλη η πληροφορία ζει στο $x_I$ — δηλαδή στο envelope. Η phase είτε είναι μηδέν (όταν το envelope είναι θετικό) είτε flips κατά $\pi$ όπου το envelope αλλάζει πρόσημο. Στο Συμβατικό AM προσθέτουμε $A_c$ για να βεβαιωθούμε ότι το $A_c + m(t) > 0$ πάντα — αυτή είναι η `ολικό φέρον` συνθήκη.
- **FM/PM** έχουν **σταθερό envelope** $V = A_c$. Όλη η πληροφορία ζει στη **φάση** $\theta(t)$. Στο complex plane το $g(t)$ κινείται πάνω σε **κύκλο** ακτίνας $A_c$ — δεν αλλάζει το μέτρο, μόνο η γωνία.
- **SSB** χρησιμοποιεί τον Hilbert ($\hat{m}$) για να μηδενίσει τη μία από τις δύο sidebands στο φάσμα. Και τα δύο $x_I, x_Q$ φέρουν πληροφορία. Με το πρόσημο $-\hat{m}/2$ παίρνεις USSB (upper sideband)· με $+\hat{m}/2$ παίρνεις LSSB.

<IQDecompositionViz />

**Δες την FM/PM «σταθερή περιβάλλουσα» σαν να κινείται κύκλος στο complex plane.** Αυτή είναι η σημαντικότερη γεωμετρική διαφορά της οικογένειας PM/FM από την οικογένεια AM/DSB. Δίπλα-δίπλα: αριστερά FM (g(t) σε κύκλο, |g| σταθερό), δεξιά AM (g(t) σε ευθεία στον real άξονα, |g| μεταβλητό):

<ConstantEnvelopeCircleViz />

<Callout type="key" title="Γιατί η σταθερή περιβάλλουσα του FM είναι μεγάλο δώρο">
Επειδή ένας FM δέκτης μπορεί να χρησιμοποιεί **limiter** (κόβει όλες τις μεταβολές πλάτους) χωρίς να χάσει πληροφορία — όλη η πληροφορία είναι στη φάση. Έτσι ο amplitude θόρυβος, που είναι το κυρίαρχο πρόβλημα της AM, **εξαλείφεται** πριν τη φασματική επεξεργασία. Αυτή είναι η αιτία που η FM έχει πολύ καλύτερη ανθεκτικότητα σε θόρυβο από την AM. Όλα συμβαίνουν επειδή το $g(t)$ κινείται σε κύκλο.
</Callout>

<RecallCard
  variant="concept"
  title="Συμπύκνωσε — από bandpass σε canonical I/Q μορφή"
  keywords={[
    'pre-envelope',
    'complex envelope g(t)',
    'συμφασική x_I',
    'ορθογώνια x_Q',
    'envelope V, phase θ',
    'πέντε διαμορφώσεις, μία μορφή',
  ]}
  skeleton={[
    'Αναγνώρισε ότι το σήμα είναι bandpass: φάσμα γύρω από ±f_c.',
    'Φτιάξε pre-envelope x_p(t) = x(t) + j·Hilbert{x}(t) — έχει φάσμα μόνο για f > 0.',
    'Πολλαπλασίασε με e^{-j 2π f_c t} για να γυρίσεις στο baseband: g(t) = x_p(t)·e^{-j 2π f_c t}.',
    'Σπάσε σε real + imaginary: g(t) = x_I(t) + j x_Q(t).',
    'Διάβασε ως x(t) = x_I cos(2π f_c t) − x_Q sin(2π f_c t) ή ως V(t) cos(2π f_c t + θ(t)).',
  ]}
  trap={
    <>
      Το <strong>μείον</strong> στο <em>x_I cos − x_Q sin</em>. Έρχεται από το{' '}
      <em>j² = −1</em>· αν γράψεις <em>+</em>, αλλάζεις πρόσημο της phase
      modulation και χαλάς τη γραμμή FM/PM του πίνακα.
    </>
  }
/>

<Callout type="closes-commitment" title="🎯 Κλείνει υπόσχεση — bandpass signals και η canonical μορφή">
Στο [κεφάλαιο του Fourier transform](/foundations/fourier-transform#7-modulation-theorem-) δείξαμε ότι ο πολλαπλασιασμός με cosine μετατοπίζει το φάσμα στις $\pm f_c$, και υποσχεθήκαμε ότι «όλη η AM είναι αυτό σε δράση» — και πιο γενικά κάθε διαμόρφωση. **Να η canonical μορφή που το αποδεικνύει.** Το $x(t) = x_I\cos(2\pi f_c t) - x_Q\sin(2\pi f_c t)$ καλύπτει όλες τις διαμορφώσεις σε ένα ενιαίο πλαίσιο. Ο πίνακας πάνω σου δίνει τις γραμμές για AM/DSB/SSB/FM/PM ως διαφορετικές επιλογές των $(x_I, x_Q)$. Από εδώ και πέρα, **κάθε modulation chapter ξεκινά με μία γραμμή αυτού του πίνακα**.
</Callout>

<Callout type="closes-commitment" title="🎯 Κλείνει υπόσχεση — Hilbert transform">
Στο FT chapter αναφέρθηκε αόριστα ότι «τα bandpass signals και ο Hilbert ζουν σε επόμενο κεφάλαιο». Εδώ τα έχουμε όλα: ο Hilbert (§3) ορίστηκε ως phase-shifter στη συχνότητα, η pre-envelope (§4) τον χρησιμοποίησε για να φτιάξει one-sided spectrum, και η complex envelope (§5) τον περιέλαβε σιωπηρά μέσω της pre-envelope.
</Callout>

## 7. Φάσμα ζωνοπερατού σήματος — X(f) ως συνάρτηση του G(f)

Είναι ένα ερώτημα που εμφανίζεται σε τόσες ασκήσεις: **έχοντας το φάσμα $G(f)$ της complex envelope, πώς διαβάζω το φάσμα $X(f)$ του πραγματικού bandpass σήματος;** Η απάντηση είναι μία σχέση που οι διαφάνειες (slides 34–35) δίνουν ως **Άσκηση 8** του δείκτη.

Ξεκίνα από την canonical μορφή. Επειδή $x(t) = \mathrm{Re}\{g(t) e^{j2\pi f_c t}\}$ και $\mathrm{Re}\{z\} = \tfrac{1}{2}(z + z^*)$:

$$
x(t) = \tfrac{1}{2}\!\left(g(t)\, e^{j 2\pi f_c t} + g^*(t)\, e^{-j 2\pi f_c t}\right)
$$

Τώρα παίρνουμε Fourier και στα δύο μέλη. Από το frequency-shift theorem ($g(t) e^{j2\pi f_c t} \leftrightarrow G(f - f_c)$), ο πρώτος όρος γίνεται $G(f - f_c)$. Για τον δεύτερο όρο χρειάζεται η ιδιότητα του συζυγούς: $g^*(t) \leftrightarrow G^*(-f)$· συνδυασμένο με frequency-shift κατά $-f_c$ δίνει $G^*(-(f + f_c)) = G^*(-f - f_c)$. Άρα:

$$
\boxed{\,X(f) = \tfrac{1}{2}\!\left[G(f - f_c) + G^*(-f - f_c)\right]\,}
$$

**Πώς να το διαβάσεις.** Το πραγματικό bandpass spectrum αποτελείται από:

- Ένα αντίγραφο του $G$ μετατοπισμένο στο $+f_c$ (αυτό είναι το «λούτσο» γύρω από τη θετική συχνότητα).
- Ένα αντίγραφο του **συζυγούς** $G^*$ ανακλασμένο και μετατοπισμένο στο $-f_c$ (αυτό είναι η αρνητική πλευρά, που πρέπει να υπάρχει για να βγει real το $x(t)$).

Όταν το $g(t)$ είναι πραγματικό (δηλαδή $x_Q = 0$), το $G(f)$ είναι ήδη συζυγής-συμμετρικό ($G^*(-f) = G(f)$), και η σχέση απλοποιείται σε $X(f) = \tfrac{1}{2}[G(f-f_c) + G(f+f_c)]$ — η οικεία μορφή του modulation theorem. Όταν το $g(t)$ είναι μιγαδικό (π.χ. SSB), τα δύο άκρα **δεν** είναι ίδιοι αντίγραφα — γι' αυτό η SSB έχει «μη συμμετρικό» φάσμα.

<Callout type="connection" title="Από εδώ βγαίνει η bandpass PSD της randomness">
Όταν αργότερα δεις στο κεφάλαιο της randomness/PSD τη σχέση
$$S_x(f) = \tfrac{1}{4}\!\left[S_g(f - f_c) + S_g(-f - f_c)\right]$$
αυτή είναι **ακριβώς** η παραπάνω σχέση παίρνοντας μέσες τιμές πάνω σε ensemble. Το $X(f) \to S_x(f)$ μέσω $|X(f)|^2$, το $1/2 \to 1/4$ από το τετράγωνο. Όταν το $g(t)$ είναι ένα random complex envelope (π.χ. θόρυβος μέσα σε bandpass receiver), αυτή η σχέση σου λέει πώς το φάσμα ισχύος του θορύβου εμφανίζεται γύρω από το $f_c$.
</Callout>

## 8. Εξάσκηση

<ExerciseProgress slug="modulation/bridge" total={7} />

Επτά ερωτήσεις για να εμπεδώσεις την canonical μορφή — από αυτή θα ξεκινούν όλα τα επόμενα κεφάλαια. Οι λύσεις γράφονται για να σου δείξουν **πώς σκέφτεσαι** όταν δεις τέτοιο πρόβλημα στις εξετάσεις, όχι μόνο για να επιβεβαιώσεις το αποτέλεσμα.

<ExamProblem id="tf-baseband-vs-bandpass" year="True/False" weight="ορισμός">
**Δήλωση:** Κάθε σήμα που μεταδίδεται από κεραία είναι **bandpass**.

<Example title="Λύση" defaultOpen={true}>
**Πριν τη λύση — πώς το αναγνωρίζεις.** Η δήλωση μιλάει για «κεραία». Αυτό σου δίνει το «νήμα»: η κεραία χρειάζεται $\lambda/2$ του μήκους κύματος που εκπέμπει, και για baseband (~kHz) τα μήκη είναι **χιλιόμετρα**. Άρα η ερώτηση κρύβει το ερώτημα: «πρακτικά μπορεί κάποιος να έχει 7.5 km κεραία στη ταράτσα;»

**Σωστό** (πρακτικά). Για να εκπέμψει η κεραία πρέπει το σήμα να ζει σε υψηλή συχνότητα ($f_c \gg W$) — αλλιώς θα χρειαζόταν εξωπραγματικά μεγάλη κεραία ($\lambda/4$ στα baseband Hz σημαίνει χιλιόμετρα — βλ. §2 για το νούμερο $7.5$ km). Ο μετασχηματισμός baseband → bandpass είναι ακριβώς αυτό που κάνει η **διαμόρφωση**: ανυψώνει το $m(t)$ γύρω από $f_c$ ώστε να γίνει εκπέμψιμο σε μια λογικού μεγέθους κεραία.

**Take-away.** Όταν δεις «κεραία», «εκπομπή Η/Μ», «αερας», «κανάλι» — σκέψου bandpass. Όταν δεις «μικρόφωνο», «output του LPF μετά τη demodulation», «αρχικό σήμα πληροφορίας» — σκέψου baseband.
</Example>
</ExamProblem>

<ExamProblem id="hilbert-properties" year="Hilbert" weight="phase-shifter">
**Ερώτηση:** Τι κάνει ο Hilbert transform σε ένα cosine $\cos(2\pi f_0 t)$ ($f_0 > 0$); Στο πλάτος $|X(f)|$;

<Example title="Λύση">
**Πριν τη λύση — πώς το αναγνωρίζεις.** Η ερώτηση έχει δύο μέρη: «τι κάνει στο σήμα» και «τι κάνει στο πλάτος». Αυτό είναι το «δίδυμο» που εμφανίζεται όταν εξετάζεται phase-shifter / all-pass μεταχείριση — μέτρο και φάση αντιμετωπίζονται ξεχωριστά. Άρα ξεκινάς γράφοντας τη φασματική σχέση του Hilbert και διαβάζεις τις δύο πτυχές απευθείας από εκεί.

**Στο cosine:** $\widehat{\cos(2\pi f_0 t)} = \cos(2\pi f_0 t - \pi/2) = \sin(2\pi f_0 t)$ (γραμμή «cos t → sin t» του πίνακα §3b). Δηλαδή ο Hilbert μετατρέπει cosine σε sine — είναι ένας $-\pi/2$ phase shift για **όλες** τις θετικές συχνότητες.

**Στο πλάτος:** Από τη σχέση $\hat{X}(f) = -j\,\mathrm{sgn}(f)\,X(f)$ και $|{-j\,\mathrm{sgn}(f)}| = 1$ για κάθε $f \neq 0$. Άρα **το πλάτος δεν αλλάζει** — αλλάζει μόνο η φάση. Είναι τέλειος all-pass phase shifter.

**Take-away.** Ο Hilbert είναι ένα από τα ωραιότερα «καθαρά» phase-shifters: αλλάζει μόνο φάση, χωρίς να διαστρέφει το μέτρο. Δύο εφαρμογές → $-\pi$ phase shift → αναποδογυρισμένο σήμα → επιβεβαιώνει το $\mathcal{H}\{\mathcal{H}\{x\}\} = -x$.
</Example>
</ExamProblem>

<ExamProblem id="iq-fm-constant-envelope" year="I/Q" weight="FM hint">
**Ερώτηση:** Στην FM, $x_I = A_c\cos\phi(t)$ και $x_Q = -A_c\sin\phi(t)$. Δείξε ότι το envelope $V(t)$ είναι **σταθερό**, και ερμήνευσε την πρακτική σημασία.

<Example title="Λύση">
**Πριν τη λύση — πώς το αναγνωρίζεις.** Όταν δεις στην εκφώνηση «σταθερή περιβάλλουσα» ή ζητείται «δείξτε V σταθερό», σχεδόν πάντα έχεις FM/PM. Η μηχανική ταυτότητα είναι $\sin^2 + \cos^2 = 1$ — προσπάθησε να την «βρεις» στις δύο γραμμές του πίνακα.

$$V(t) = \sqrt{x_I^2 + x_Q^2} = \sqrt{A_c^2\cos^2\phi + A_c^2\sin^2\phi} = A_c\sqrt{\cos^2\phi + \sin^2\phi} = A_c$$

Δηλαδή $V(t) = A_c$ ανεξάρτητα από το $\phi(t)$ — το complex envelope $g(t) = A_c\,e^{j\phi(t)}$ κινείται πάνω σε **κύκλο** ακτίνας $A_c$ στο complex plane (δες το `ConstantEnvelopeCircleViz` στη §6b).

**Πρακτική συνέπεια:** ένας FM δέκτης μπορεί να χρησιμοποιεί **limiter** (κόβει όλες τις μεταβολές πλάτους) χωρίς να χάνει πληροφορία — όλη η πληροφορία είναι στη φάση. Γι' αυτό η FM είναι ανθεκτική σε amplitude noise.

**Take-away.** Σταθερή περιβάλλουσα ↔ όλη η πληροφορία στη φάση ↔ ανθεκτικότητα σε amplitude θόρυβο. Σε εξετάσεις, αν σου δείξουν $x_I, x_Q$ και σου ζητήσουν να ταυτοποιήσεις ποιο μοντέλο modulation ταιριάζει: υπολόγισε το $V$. Αν βγει σταθερό = FM/PM. Αν εξαρτάται από $t$ = AM/DSB/SSB.
</Example>
</ExamProblem>

<ExamProblem id="dsb-iq-form" year="DSB-SC" weight="canonical match">
**Ερώτηση:** Γράψε το DSB-SC σήμα $x(t) = m(t)\cos(2\pi f_c t)$ στην canonical μορφή $x = x_I\cos(\omega_c t) - x_Q\sin(\omega_c t)$. Ποια είναι τα $x_I, x_Q$;

<Example title="Λύση" defaultOpen={true}>
**Πριν τη λύση — πώς το αναγνωρίζεις.** Η εκφώνηση δίνει ήδη ένα σήμα σε «μορφή πραγματικού γινομένου» $m(t) \cdot \cos$. Στην canonical, ο όρος που πολλαπλασιάζεται με $\cos$ είναι το $x_I$ και αυτός που πολλαπλασιάζεται με $\sin$ (με μείον) είναι το $x_Q$. Άρα η σύγκριση είναι σχεδόν αυτόματη.

Συγκρίνοντας με την canonical: $x_I = m(t)$, $x_Q = 0$.

Αυτό αντιστοιχεί στη γραμμή **DSB-SC** του πίνακα στο §6b. Όλη η πληροφορία ζει στο in-phase channel· το quadrature είναι άδειο. Επειδή $V = |x_I| = |m(t)|$ μεταβάλλεται και αλλάζει πρόσημο, το «envelope» του DSB-SC δεν είναι ίδιο με το $m(t)$ — εμφανίζονται phase reversals όπου το $m$ αλλάζει πρόσημο. Γι' αυτό envelope detection **δεν** δουλεύει στο DSB-SC.

**Take-away.** Όταν βλέπεις ένα bandpass σήμα γραμμένο ως «πραγματικός όρος × cos», αμέσως έχεις $x_I = $ ο πραγματικός όρος, $x_Q = 0$. Αν εμφανιστούν δύο όροι (ένας × cos και ένας × sin), εκείνος του $\sin$ είναι το $x_Q$ (με αντίθετο πρόσημο).
</Example>
</ExamProblem>

<ExamProblem id="pre-envelope-spectrum" year="Pre-envelope" weight="one-sided">
**Δήλωση:** Η pre-envelope $x_p(t) = x(t) + j\hat{x}(t)$ έχει φάσμα μόνο στις θετικές συχνότητες.

<Example title="Λύση">
**Πριν τη λύση — πώς το αναγνωρίζεις.** Όταν δεις «pre-envelope», «αναλυτικό σήμα», «$x + j\hat{x}$» — αυτό είναι το one-sided spectrum theorem. Η απάντηση είναι μία γραμμή αλγέβρας: εφάρμοσε τη σχέση $\hat{X}(f) = -j\,\mathrm{sgn}(f)\,X(f)$ και απλοποίησε.

**Σωστό.** Από τον τύπο $\hat{X}(f) = -j\,\mathrm{sgn}(f)\,X(f)$:
$$X_p(f) = X(f) + j\cdot(-j\,\mathrm{sgn}(f))X(f) = X(f)[1 + \mathrm{sgn}(f)]$$

- Για $f > 0$: $1 + \mathrm{sgn}(f) = 2$ → $X_p(f) = 2X(f)$.
- Για $f < 0$: $1 + \mathrm{sgn}(f) = 0$ → $X_p(f) = 0$.
- Στο $f = 0$: $X_p(0) = X(0)$.

Άρα η pre-envelope έχει **μονόπλευρο φάσμα** (analytic signal). Αυτή η ιδιότητα είναι θεμέλιο για το SSB: όταν θέλουμε να μεταδώσουμε μόνο την upper sideband, χρησιμοποιούμε την pre-envelope του $m(t)$ — επειδή έχει ήδη απαλειμμένη τη μία πλευρά.

**Take-away.** Pre-envelope ⇒ one-sided spectrum ⇒ SSB. Όταν δεις «εκπέμπουμε μόνο USSB» ή «το φάσμα μετά τον Hilbert filter έχει μόνο θετική πλευρά», ξέρεις ότι το load-bearing concept είναι αυτή η γραμμή.
</Example>
</ExamProblem>

<ExamProblem id="phase-error-iq-rotation" year="DSB-SC" weight="I/Q rotation">
**Ερώτηση (νέο — phase error ως I/Q rotation):** Σε σύμφωνο αποδιαμορφωτή DSB-SC, ο τοπικός ταλαντωτής έχει σφάλμα φάσης $\varphi$: δηλαδή είναι $\cos(2\pi f_c t + \varphi)$ αντί για $\cos(2\pi f_c t)$. Δείξε χρησιμοποιώντας την canonical μορφή ότι το σήμα εξόδου εξασθενεί κατά παράγοντα $\cos\varphi$. Τι συμβαίνει στο $\varphi = \pi/2$;

<Example title="Λύση">
**Πριν τη λύση — πώς το αναγνωρίζεις.** Η εκφώνηση μιλάει για «σφάλμα φάσης σε σύμφωνη αποδιαμόρφωση». Στη γλώσσα I/Q αυτό είναι μια **στροφή των $(x_I, x_Q)$ αξόνων κατά $\varphi$**: ένα κομμάτι του I διαρρέει στο Q (και αντίστροφα). Άρα η σωστή στρατηγική είναι: γράψε το σήμα στην canonical μορφή με carrier $\cos(2\pi f_c t + \varphi)$ και κατάλαβε αμέσως ποιο μέρος του $x_I$ μένει στον σωστό άξονα.

**Βήμα 1 — DSB-SC στην canonical μορφή.** Από την προηγούμενη άσκηση: $x_I(t) = m(t)$, $x_Q(t) = 0$. Άρα

$$x(t) = m(t)\cos(2\pi f_c t)$$

**Βήμα 2 — πολλαπλασιασμός με τον lo που έχει σφάλμα.**

$$x(t)\cos(2\pi f_c t + \varphi) = m(t)\cos(2\pi f_c t)\cos(2\pi f_c t + \varphi)$$

Χρησιμοποιώντας $\cos A \cos B = \tfrac{1}{2}[\cos(A-B) + \cos(A+B)]$:

$$= \tfrac{m(t)}{2}\big[\cos\varphi + \cos(4\pi f_c t + \varphi)\big]$$

**Βήμα 3 — LPF αφαιρεί τον όρο $2f_c$.** Μετά το low-pass filter:

$$y(t) = \tfrac{1}{2} m(t)\cos\varphi$$

Το πλάτος εξόδου **πολλαπλασιάζεται με $\cos\varphi$** — ακριβώς η ζητούμενη απόκριση.

**Στο $\varphi = \pi/2$:** $\cos(\pi/2) = 0$, άρα **πλήρης απώλεια του σήματος**. Αυτό λέγεται **quadrature null**: το μήνυμα που ζούσε στον I-άξονα έχει στραφεί 90° και τώρα ζει στον Q-άξονα — αλλά ο σύμφωνος αποδιαμορφωτής του DSB-SC κοιτάει μόνο το I κανάλι μέσω του LPF. Στον I-άξονα δεν μένει τίποτα.

**Take-away.** Σφάλμα φάσης $\varphi$ ⇒ rotation των I/Q αξόνων ⇒ έξοδος $\propto \cos\varphi$. Αυτό **είναι** ο λόγος που η σύμφωνη αποδιαμόρφωση χρειάζεται phase-locked loop ή carrier recovery — αλλιώς ένα slow phase drift κάνει το σήμα να ξεθωριάζει. Αυτό το pattern επανεμφανίζεται στις εξετάσεις (π.χ. Πρόοδος Απρ 2026 Θέμα 7).
</Example>
</ExamProblem>

<ExamProblem id="bandpass-spectrum-from-g" year="X(f) ↔ G(f)" weight="exam-template">
**Άσκηση 8 (slides 34–35):** Έχοντας $G(f) = \mathcal{F}\{g(t)\}$ της complex envelope, βρες το $X(f)$ του bandpass $x(t) = \mathrm{Re}\{g(t)e^{j2\pi f_c t}\}$. Εφάρμοσε για δύο περιπτώσεις: (α) $g(t)$ πραγματικό, (β) $g(t)$ μιγαδικό.

<Example title="Λύση">
**Πριν τη λύση — πώς το αναγνωρίζεις.** Η εκφώνηση σου ζητάει «φάσμα του bandpass δοθέντος του φάσματος της complex envelope». Αυτή είναι ΆΣΚΗΣΗ-template της σελίδας — η σχέση που πρέπει να ξέρεις απέξω είναι αυτή της §7. Άρα ξεκινάς γράφοντας $\mathrm{Re}\{z\} = \tfrac{1}{2}(z + z^*)$ και παίρνεις FT.

**Βήμα 1 — γράψε real part με συζυγή.** $x(t) = \tfrac{1}{2}\!\left(g(t) e^{j2\pi f_c t} + g^*(t) e^{-j2\pi f_c t}\right)$.

**Βήμα 2 — FT κάθε όρου.** Πρώτος όρος: από frequency-shift, $g(t) e^{j2\pi f_c t} \leftrightarrow G(f - f_c)$. Δεύτερος όρος: $g^*(t) \leftrightarrow G^*(-f)$ (συζυγής στον χρόνο = συζυγής με ανάκλαση στη συχνότητα), συν frequency-shift κατά $-f_c$ δίνει $G^*(-f - f_c)$. Άρα:

$$X(f) = \tfrac{1}{2}\!\left[G(f - f_c) + G^*(-f - f_c)\right]$$

**Περίπτωση (α) — $g(t)$ πραγματικό.** Τότε $G^*(-f) = G(f)$ (conjugate symmetry), και η σχέση γίνεται:

$$X(f) = \tfrac{1}{2}\!\left[G(f - f_c) + G(f + f_c)\right]$$

Δηλαδή ένα αντίγραφο του $G$ στις $+f_c$ και ένα ίδιο αντίγραφο στις $-f_c$. **Συμμετρικό φάσμα** — αυτό είναι το χαρακτηριστικό του AM και του DSB-SC.

**Περίπτωση (β) — $g(t)$ μιγαδικό.** Τότε $G^*(-f) \neq G(f)$ γενικά, και τα δύο άκρα του $X$ δεν είναι ίδια αντίγραφα — το ένα είναι κανονικό $G$, το άλλο είναι **συζυγές με ανάκλαση**. **Μη συμμετρικό φάσμα** — αυτό είναι το χαρακτηριστικό της SSB και της FM.

**Take-away.** Συμμετρικό bandpass spectrum ⇒ $g(t)$ πραγματικό ⇒ AM/DSB. Μη-συμμετρικό ⇒ $g(t)$ μιγαδικό ⇒ SSB/FM/PM. Αυτή η σχέση είναι το γέφυρα προς το κεφάλαιο της randomness/PSD ($S_x = \tfrac{1}{4}[S_g(f-f_c) + S_g(-f-f_c)]$) και προς όλη την SSB ανάλυση.
</Example>
</ExamProblem>

## 9. Ανακάλεσε — δοκίμασε από μνήμης

Πριν προχωρήσεις: τρία γρήγορα drills για να ελέγξεις αν τα έχεις «πραγματικά μέσα σου» ή απλώς τα διάβασες.

<ReorderDrill
  id="canonical-derivation-order"
  prompt={
    <>
      Βάλε τα παρακάτω <strong>πέντε βήματα</strong> στη σωστή σειρά για να πας από{' '}
      «έχω ένα real bandpass σήμα <em>x(t)</em>» σε «το έχω γράψει στην canonical I/Q μορφή».
    </>
  }
  steps={[
    'Αναγνώρισε ότι το x(t) είναι bandpass — φάσμα γύρω από ±f_c, μηδέν αλλού.',
    'Φτιάξε pre-envelope: x_p(t) = x(t) + j·H{x(t)} (Hilbert της x).',
    'Μετατόπισε στο baseband: g(t) = x_p(t) · e^{-j 2π f_c t}.',
    'Σπάσε σε real + imaginary: g(t) = x_I(t) + j x_Q(t).',
    'Διάβασε: x(t) = x_I(t) cos(2π f_c t) − x_Q(t) sin(2π f_c t).',
  ]}
/>

<ClozeDrill
  id="canonical-form-sign-trap"
  prompt={
    <>
      Συμπλήρωσε τα κενά στη canonical μορφή και στους τύπους envelope + phase.
    </>
  }
  parts={[
    'x(t) = x_I(t) · cos(2π f_c t) ',
    { blank: 'sign', accept: ['-', '−', 'μείον'], hint: '+ ή -', size: 4 },
    ' x_Q(t) · sin(2π f_c t). Η περιβάλλουσα είναι V(t) = ',
    { blank: 'envelope', accept: ['sqrt(x_I^2+x_Q^2)', 'sqrt(xi^2+xq^2)', '√(x_I²+x_Q²)', 'sqrt(x_i^2+x_q^2)'], hint: 'σε όρους x_I, x_Q', size: 18 },
    ', η στιγμιαία φάση θ(t) = arctan(',
    { blank: 'phase', accept: ['x_Q/x_I', 'xq/xi', 'x_q/x_i'], hint: 'λόγος', size: 9 },
    ').',
  ]}
  solution={
    <>
      <p>
        <strong>x(t) = x_I cos(2π f_c t) <em>−</em> x_Q sin(2π f_c t)</strong> με{' '}
        <em>μείον</em> — έρχεται από <em>j² = −1</em>. Η περιβάλλουσα είναι το{' '}
        μέτρο του complex envelope: <strong>V(t) = √(x_I² + x_Q²)</strong>. Η{' '}
        στιγμιαία φάση είναι η γωνία του complex envelope:{' '}
        <strong>θ(t) = arctan(x_Q / x_I)</strong>.
      </p>
    </>
  }
/>

<RecallDrill
  id="hilbert-three-truths"
  prompt={
    <>
      Πες με δικά σου λόγια τις <strong>τρεις αλήθειες</strong> για τον Hilbert
      transform σε ένα bandpass-relevant signal:
      <br />(α) τι κάνει στις θετικές συχνότητες,
      <br />(β) τι κάνει στις αρνητικές,
      <br />(γ) τι κάνει στο μέτρο του φάσματος.
    </>
  }
  answer={
    <ul className="ml-5 list-disc space-y-1">
      <li>
        <strong>Θετικές συχνότητες:</strong> πολλαπλασιάζονται με <em>−j</em> — phase shift <em>−π/2</em>.
      </li>
      <li>
        <strong>Αρνητικές συχνότητες:</strong> πολλαπλασιάζονται με <em>+j</em> — phase shift <em>+π/2</em>.
      </li>
      <li>
        <strong>Μέτρο του φάσματος:</strong> αμετάβλητο. |−j sgn(f)| = 1. Ο Hilbert είναι all-pass.
      </li>
    </ul>
  }
/>

## 10. Αναγνώρισε — πού θα δεις τη γέφυρα σε εξέταση

<ThinkingPattern
  signals={[
    'σταθερή περιβάλλουσα',
    'σφάλμα φάσης φ',
    'γράψτε x(t) στην canonical μορφή',
    'βρείτε x_I, x_Q (ή συμφασική / ορθογώνια)',
    'envelope V(t) και phase θ(t)',
    'μονόπλευρο φάσμα / αναλυτικό σήμα',
    'φάσμα bandpass δοθέντος του G(f)',
  ]}
>
  <p>
    Αυτές οι φράσεις σχεδόν πάντα σημαίνουν «μετέφρασε το πρόβλημα στην γλώσσα I/Q» —
    και η λύση είναι ένα από τα τρία:
  </p>
  <ul className="ml-5 list-disc space-y-1">
    <li>
      <strong>«Σταθερή περιβάλλουσα»</strong> → FM ή PM. Δείξε{' '}
      <em>√(x_I² + x_Q²) = A_c</em> και ανέπτυξε γιατί η πληροφορία είναι στη φάση.
    </li>
    <li>
      <strong>«Σφάλμα φάσης φ»</strong> → I/Q rotation. Έξοδος{' '}
      σύμφωνου αποδιαμορφωτή πολλαπλασιάζεται με <em>cos φ</em>· στο{' '}
      <em>φ = π/2</em> εμφανίζεται quadrature null.
    </li>
    <li>
      <strong>«Γράψτε στην canonical» / «βρες x_I, x_Q»</strong> → σπας το{' '}
      σήμα σε γινόμενα <em>(κάτι)·cos − (κάτι)·sin</em>. Ο{' '}
      πρώτος όρος είναι το x_I, ο δεύτερος το x_Q.
    </li>
    <li>
      <strong>«Envelope V(t)»</strong> ή <strong>«πολική μορφή»</strong> →{' '}
      υπολόγισε <em>V = √(x_I² + x_Q²)</em>, <em>θ = arctan(x_Q/x_I)</em>, και{' '}
      γράψε <em>x = V cos(2π f_c t + θ)</em>.
    </li>
    <li>
      <strong>«Μονόπλευρο φάσμα» / «αναλυτικό σήμα»</strong> → pre-envelope. Το{' '}
      σήμα είναι <em>x_p = x + j·H{'{x}'}</em>· φάσμα <em>X_p(f) = 2X(f)</em> για{' '}
      f &gt; 0, μηδέν για f &lt; 0.
    </li>
    <li>
      <strong>«Φάσμα bandpass δοθέντος του G(f)»</strong> →{' '}
      <em>X(f) = (1/2)[G(f − f_c) + G*(−f − f_c)]</em>. Αν το{' '}
      <em>g</em> είναι πραγματικό, απλοποιείται στο familiar modulation-theorem-pair.
    </li>
  </ul>
</ThinkingPattern>

<ExamRadar
  title="Πού εμφανίζεται η canonical I/Q μορφή στα παλιά θέματα"
  ids={[
    {
      id: 'proodos26-7',
      likelihood: 'high',
      note: 'DSB-SC με σφάλμα φάσης φ — direct exercise σε I/Q rotation. Το cos φ της εξόδου είναι ακριβώς αυτό που έδειξες στο ExamProblem «phase-error-iq-rotation» παραπάνω.',
    },
    {
      id: 'jan26-th2-7',
      likelihood: 'high',
      note: 'AM σχεδίαση χρόνου + φάσματος — γραμμή «Συμβατικό AM» του πίνακα §6b με x_I = A_c + m, x_Q = 0. Η πολική μορφή V(t)·cos(2π f_c t) είναι το ίδιο σήμα.',
    },
    {
      id: 'proodos26-11',
      likelihood: 'high',
      note: 'USSB δύο σημάτων — εφαρμόζει την pre-envelope της §4: SSB = ολίσθηση της pre-envelope του m(t) στο +f_c. Η γραμμή SSB του πίνακα §6b γράφεται έτσι.',
    },
    {
      id: 'sept25-th1-5',
      likelihood: 'high',
      note: 'AM φάσμα δύο-τόνου message — direct εφαρμογή της σχέσης X(f) = (1/2)[G(f-fc) + G*(-f-fc)] της §7, με g(t) = A_c + m(t) πραγματικό άρα συμμετρικό spectrum.',
    },
    {
      id: 'jun25-th1-4',
      likelihood: 'medium',
      note: 'Φάσμα πλάτους + φάσης 2cos(1000πt + π/4) — η canonical μορφή με σταθερό x_I = 2 cos(π/4), x_Q = 2 sin(π/4) (από 2cos(A+B) = 2cos A·cos B − 2sin A·sin B) δίνει αμέσως κρούσεις με ίδιο μέτρο και αντίθετη φάση στις ±f_0 (πραγματικό σήμα ⇒ conjugate-symmetric φάσμα).',
    },
    {
      id: 'pa25-th2-2',
      likelihood: 'medium',
      note: 'Σχεδίαση AM cos(8πt) με 2sin(2πt) — το envelope V(t) = |1 + 2sin(2πt)| διαβάζεται απευθείας από τη γραμμή «Συμβατικό AM» του πίνακα §6b.',
    },
  ]}
/>

## 11. Πού θα χρειαστείς όλα αυτά αργότερα

Έξι forward-references στις σελίδες που quotάρουν μία γραμμή του πίνακα §6b:

- **[/am/conventional](/am/conventional)** — η γραμμή «Συμβατικό AM»: $x_I = A_c + m(t)$, $x_Q = 0$. Όλη η συζήτηση για modulation index $\mu$, ισχύ, efficiency $\eta$ ξεκινά από εκεί.
- **[/am/dsb-sc](/am/dsb-sc)** — η γραμμή DSB-SC: $x_I = m(t)$, $x_Q = 0$. Το worked-example «phase-error-iq-rotation» στο §8 αυτής της σελίδας είναι ακριβώς το pattern που η DSB-SC σελίδα θα χρειαστεί.
- **[/am/ssb](/am/ssb)** — οι γραμμές USSB/LSSB. Η pre-envelope της §4 είναι ο κεντρικός μηχανισμός γέννησης SSB: $\hat{m}(t)$ στο $x_Q$ ακυρώνει τη μία πλευρά του spectrum.
- **[/fm/idea](/fm/idea)** και **[/fm/pm](/fm/pm)** — η γραμμή FM/PM με σταθερή περιβάλλουσα $V = A_c$. Όλη η συζήτηση για Bessel sidebands του [/fm/bessel](/fm/bessel) ξεκινά από $g(t) = A_c e^{j\beta\sin(2\pi f_m t)}$ — μιγαδικό envelope, σταθερό μέτρο.
- **[/randomness/psd](/randomness/psd)** — η σχέση $S_x(f) = \tfrac{1}{4}[S_g(f-f_c) + S_g(-f-f_c)]$ είναι το averaging της σχέσης X(f)↔G(f) της §7.
- **[/noise/through-filters](/noise/through-filters)** — όταν θόρυβος περάσει από bandpass filter, η ανάλυση γράφεται σε I/Q. Το «bandpass noise = $n_I \cos − n_Q \sin$» είναι ακριβώς ο πίνακας §6b εφαρμοσμένος σε random $g(t) = n_I + j n_Q$.

## 12. Συμπύκνωσε όλο το κεφάλαιο

<RecallCard
  variant="summary"
  title="Συμπύκνωσε όλο το bridge"
  keywords={[
    'baseband vs bandpass',
    'narrowband W ≪ f_c',
    'Hilbert = −j sgn(f)',
    'pre-envelope = one-sided',
    'complex envelope g(t)',
    'canonical x = x_I cos − x_Q sin',
    'πέντε διαμορφώσεις, ένας πίνακας',
    'X(f) = (1/2)[G(f-fc) + G*(-f-fc)]',
  ]}
  skeleton={[
    'Bandpass = φάσμα γύρω από ±f_c. Σχεδόν καθετί στον αέρα είναι bandpass.',
    'Hilbert = phase-shifter −π/2 στις θετικές συχνότητες, +π/2 στις αρνητικές, μέτρο αμετάβλητο.',
    'Pre-envelope x_p = x + j·H{x} → one-sided spectrum (×2 στις θετικές, 0 στις αρνητικές).',
    'Complex envelope g = x_p · e^{-j 2π f_c t} → x_p μεταφερμένο στο baseband.',
    'Canonical I/Q μορφή: x = x_I cos − x_Q sin με g = x_I + j x_Q. Πολική: x = V cos(2π f_c t + θ).',
    'AM/DSB-SC: x_Q = 0. FM/PM: V = A_c σταθερό. SSB: x_Q = ∓ (1/2) Hilbert{m}.',
    'X(f) bandpass δοθέντος G(f): X = (1/2)[G(f − f_c) + G*(−f − f_c)].',
  ]}
  trap={
    <>
      Δύο κοινά λάθη: (α) γράφεις <em>+</em> αντί για <em>−</em> στην canonical
      ξεχνώντας το <em>j² = −1</em>· (β) ξεχνάς ότι το V και η θ είναι{' '}
      <strong>χρονο-εξαρτημένα</strong>, όχι σταθερές — εκτός από την περίπτωση
      FM/PM όπου το <em>V</em> τυχαίνει να είναι σταθερό.
    </>
  }
/>

## 13. Recap + Next up

<Recap>
- Τα σήματα κατατάσσονται σε **baseband** (φάσμα γύρω από 0) και **bandpass** (φάσμα γύρω από $\pm f_c$). Σχεδόν καθετί στον αέρα είναι bandpass, με narrowband υπόθεση $W \ll f_c$.
- Ο **Μετασχηματισμός Hilbert** $\hat{x}$ είναι phase-shifter: $-\pi/2$ για $f > 0$, $+\pi/2$ για $f < 0$. Στη συχνότητα: πολλαπλασιασμός με $-j\,\mathrm{sgn}(f)$. Μέτρο αμετάβλητο. Πίνακας ζευγών στη §3b.
- Η **pre-envelope** $x_p = x + j\hat{x}$ έχει **μονόπλευρο φάσμα**: η θετική πλευρά διπλασιάζεται, η αρνητική εξαφανίζεται.
- Η **complex envelope** $g = x_p\, e^{-j 2\pi f_c t}$ μεταφέρει το pre-envelope στο baseband. Όλη η πληροφορία του bandpass σήματος συμπυκνώνεται σε αυτό.
- **Canonical μορφή κάθε ζωνοπερατού:** $x(t) = x_I(t)\cos(2\pi f_c t) - x_Q(t)\sin(2\pi f_c t)$, με $g = x_I + jx_Q$. Συμφασική (I) / Ορθογώνια (Q). Σε πολική μορφή: $x = V\cos(2\pi f_c t + \theta)$ με $V = \sqrt{x_I^2 + x_Q^2}$, $\theta = \arctan(x_Q/x_I)$.
- **AM, DSB, SSB, FM, PM** είναι όλες ειδικές περιπτώσεις αυτής της μορφής — **διαφορετικές επιλογές για $x_I, x_Q$** (ή ισοδύναμα για $V, \theta$). AM/DSB έχουν $x_Q = 0$. FM/PM έχουν σταθερό envelope $V = A_c$. SSB χρησιμοποιεί $\hat{m}$.
- **Φάσμα bandpass από το $G(f)$:** $X(f) = \tfrac{1}{2}[G(f-f_c) + G^*(-f-f_c)]$. Αν $g$ πραγματικό → συμμετρικό φάσμα (AM/DSB). Αν μιγαδικό → μη συμμετρικό (SSB/FM).
</Recap>

**Από εδώ ξεκινάει η modulation.** Η AM είναι το πρώτο κεφάλαιο — ξεκινάμε από τη γραμμή «Συμβατικό AM» του πίνακα §6b ($x_I = A_c + m(t)$, $x_Q = 0$), παίρνουμε τη συνέπεια στο φάσμα μέσω της σχέσης §7, και χτίζουμε όλη την οικογένεια AM (Conventional, DSB-SC, SSB, VSB) ως παραλλαγές του ίδιου σχήματος.

<NextUp slug="modulation/am" />
