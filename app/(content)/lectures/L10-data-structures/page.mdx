export const metadata = {
  title: 'L10 · Δομές Δεδομένων — Σωροί & Ξένα Σύνολα',
  description:
    'Δυαδικοί σωροί (heaps), heapify-up / heapify-down, ουρά προτεραιότητας, heapsort, και η δομή ξένων συνόλων (union-find) με βάρη στα δέντρα και συμπίεση μονοπατιών.',
}

export const frontmatter = {
  title: 'L10 · Δομές Δεδομένων — Σωροί & Ξένα Σύνολα',
  slug: 'lectures/L10-data-structures',
  order: 10,
  prerequisites: ['lectures/L09-graphs-iv'],
  examWeight: 6,
  estimatedReadTime: 32,
  lastUpdated: '2026-05-21',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Data structures</span>
  <span>·</span>
  <span>Διάλεξη 10</span>
  <span>·</span>
  <span>~32 min read</span>
</div>

# L10 · Δομές Δεδομένων — Σωροί &amp; Ξένα Σύνολα

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="lectures/L10-data-structures" />
  <Bookmark id="lectures/L10-data-structures" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc sources={{ pdf: 'L10 - Δομές Δεδομένων.pdf' }} />

## Τι θα δούμε

Στο [L09](/lectures/L09-graphs-iv) είπαμε ότι ο **Dijkstra** και ο **Prim** τρέχουν σε $O(m \log n)$ «με δυαδικό σωρό», και ο **Kruskal** χρειάζεται μια δομή που απαντάει «είναι αυτές οι δύο κορυφές στην ίδια συνεκτική συνιστώσα;». Είπαμε «θα το δούμε αργότερα». **Αυτή είναι η διάλεξη του «αργότερα».**

Δύο δομές δεδομένων:

1. **Σωρός (heap)** — υλοποιεί μια **ουρά προτεραιότητας**: «δώσε μου το μικρότερο στοιχείο» σε $O(\log n)$. Αυτό κάνει τον Dijkstra και τον Prim γρήγορους.
2. **Ξένα σύνολα (union-find)** — υλοποιεί «σε ποιο σύνολο ανήκει το $x$;» και «ένωσε δύο σύνολα» σχεδόν σε σταθερό χρόνο. Αυτό κάνει τον Kruskal γρήγορο.

<Callout type="key">
**Δεν είναι «θεωρία για τη θεωρία».** Κάθε δομή εδώ υπάρχει επειδή ένας αλγόριθμος που ήδη ξέρεις την χρειάζεται. Διάβασε κάθε ενότητα ρωτώντας: «ποια ακριβώς πράξη επιταχύνει αυτό, και σε ποιον αλγόριθμο;»
</Callout>

## Μέρος Α' — Σωροί (Heaps)

### Το πρόβλημα που λύνουν

Φαντάσου ότι έχεις μια συλλογή στοιχείων με **κλειδιά** (αριθμούς) και θέλεις, ξανά και ξανά, να ρωτάς: **«ποιο είναι το μικρότερο;»** και μετά να το **βγάζεις** από τη συλλογή. Ακριβώς αυτό κάνει ο Dijkstra: σε κάθε βήμα βγάζει την ανεξερεύνητη κορυφή με το μικρότερο $\pi(v)$.

Τι επιλογές έχουμε;

| Δομή | FindMin | Insert / Delete |
|---|---|---|
| **Μη ταξινομημένος πίνακας** | $O(n)$ — σάρωσε όλα | $O(1)$ |
| **Ταξινομημένος πίνακας** | $O(1)$ — είναι στην άκρη | $O(n)$ — μετατόπιση |
| **Σωρός** | $O(1)$ | $O(\log n)$ |

<Callout type="intuition">
**Διαισθητικά:** ο σωρός είναι ο **συμβιβασμός**. Ο ταξινομημένος πίνακας κρατάει τα πάντα σε τέλεια σειρά — υπερβολικά πολλή δουλειά. Ο μη ταξινομημένος δεν κρατάει τίποτα — πληρώνεις στο ψάξιμο. Ο σωρός κρατάει **μόνο όση τάξη χρειάζεται** για να ξέρει πάντα ποιο είναι το μικρότερο: κάθε γονιός μικρότερος από τα παιδιά του. Τίποτα παραπάνω.
</Callout>

### Ορισμός

<Callout type="key">
**Σωρός (heap):** ένα **ισοσταθμισμένο δυαδικό δέντρο** που ικανοποιεί την **ιδιότητα διάταξης σωρού**: για κάθε κορυφή $v$ με γονέα $w$ ισχύει
$$\text{key}(w) \le \text{key}(v).$$
</Callout>

Δύο κομμάτια, ξεχωριστά:

- **«Ισοσταθμισμένο δυαδικό δέντρο»** — αφορά το **σχήμα**. Κάθε επίπεδο γεμίζει πλήρως πριν ξεκινήσει το επόμενο, και το τελευταίο γεμίζει από αριστερά. Αυτό εγγυάται ύψος $\lfloor \log_2 n \rfloor$ — εκεί κρύβεται το $O(\log n)$ κάθε πράξης.
- **«Ιδιότητα διάταξης σωρού»** — αφορά τα **κλειδιά**. Κάθε γονιός $\le$ κάθε παιδί του.

Πρόσεξε τι **δεν** λέει η ιδιότητα: δεν λέει τίποτα για τη σχέση ανάμεσα σε **αδέλφια**, ούτε ανάμεσα σε κορυφές διαφορετικών κλάδων. Ένας σωρός είναι «χαλαρά» ταξινομημένος — και ακριβώς γι' αυτό συντηρείται φθηνά.

<Callout type="key">
**Συνέπεια:** η ρίζα έχει πάντα το **ελάχιστο** κλειδί. (Είναι γονιός — άμεσα ή έμμεσα — όλων.) Άρα `FindMin` $= O(1)$: κοίτα τη ρίζα.
</Callout>

### Αναπαράσταση με πίνακα

Το ωραιότερο κόλπο: ένας σωρός **δεν χρειάζεται δείκτες**. Επειδή το σχήμα είναι αυστηρά ισοσταθμισμένο, αποθηκεύουμε τις κορυφές σε έναν πίνακα $H[1..n]$, επίπεδο-επίπεδο από αριστερά προς δεξιά. Τότε για κάθε θέση $i$:

$$
\text{parent}(i) = \left\lfloor i/2 \right\rfloor, \qquad
\text{leftChild}(i) = 2i, \qquad
\text{rightChild}(i) = 2i + 1
$$

Δηλαδή «να κατέβεις στο παιδί» = πολλαπλασίασε με 2· «να ανέβεις στον γονέα» = διαίρεσε με 2. Καθαρή αριθμητική, μηδέν δείκτες.

<div className="graph-canvas">
<svg viewBox="0 0 720 360" className="w-full max-w-3xl" xmlns="http://www.w3.org/2000/svg">
  <style>{`
    .hn { fill: rgb(var(--bg-elevated)); stroke: rgb(var(--accent)); stroke-width: 2; }
    .hroot { fill: rgb(244 63 94 / 0.22); stroke: rgb(244 63 94); stroke-width: 2.5; }
    .hk { font: 700 15px ui-sans-serif, system-ui; fill: rgb(var(--fg)); text-anchor: middle; dominant-baseline: middle; }
    .hi { font: 600 11px ui-sans-serif, system-ui; fill: rgb(var(--fg-subtle)); text-anchor: middle; }
    .he { stroke: rgb(var(--fg-muted)); stroke-width: 1.8; }
    .acell { fill: rgb(var(--bg-elevated)); stroke: rgb(var(--border-strong)); stroke-width: 1.5; }
    .acell-root { fill: rgb(244 63 94 / 0.18); stroke: rgb(244 63 94); stroke-width: 1.8; }
    .ann { font: 600 12px ui-sans-serif, system-ui; fill: rgb(var(--fg-muted)); }
  `}</style>
  {/* tree edges */}
  <line x1="360" y1="52" x2="200" y2="132" className="he" />
  <line x1="360" y1="52" x2="520" y2="132" className="he" />
  <line x1="200" y1="132" x2="110" y2="212" className="he" />
  <line x1="200" y1="132" x2="290" y2="212" className="he" />
  <line x1="520" y1="132" x2="430" y2="212" className="he" />
  <line x1="520" y1="132" x2="610" y2="212" className="he" />
  {/* tree nodes: [key, x, y, index] */}
  {[[2,360,52,1],[5,200,132,2],[8,520,132,3],[9,110,212,4],[11,290,212,5],[14,430,212,6],[18,610,212,7]].map(([k,x,y,idx]) => (
    <g key={idx}>
      <circle cx={x} cy={y} r="24" className={idx === 1 ? 'hroot' : 'hn'} />
      <text x={x} y={y} className="hk">{k}</text>
      <text x={x} y={y + 38} className="hi">i = {idx}</text>
    </g>
  ))}
  {/* array */}
  <text x="40" y="288" className="ann">Πίνακας H:</text>
  {[[2,1],[5,2],[8,3],[9,4],[11,5],[14,6],[18,7]].map(([k,idx],n) => (
    <g key={idx}>
      <rect x={150 + n*72} y="272" width="64" height="40" className={idx === 1 ? 'acell-root' : 'acell'} />
      <text x={150 + n*72 + 32} y="294" className="hk">{k}</text>
      <text x={150 + n*72 + 32} y="328" className="hi">H[{idx}]</text>
    </g>
  ))}
  <text x="360" y="350" textAnchor="middle" className="ann">leftChild(2) = 4 → H[4]=9 · parent(5) = ⌊5/2⌋ = 2 → H[2]=5</text>
</svg>
</div>

Παρατήρησε στο σχήμα ότι **κάθε** γονιός είναι μικρότερος από τα παιδιά του ($2 \le 5,8$· $5 \le 9,11$· $8 \le 14,18$), αλλά τα αδέλφια δεν είναι ταξινομημένα μεταξύ τους — και δεν χρειάζεται.

### Heapify-up — επιδιόρθωση προς τη ρίζα

Όταν προσθέτουμε ένα στοιχείο, το βάζουμε στο **τέλος** του πίνακα (πρώτη ελεύθερη θέση — διατηρεί το σχήμα). Όμως το κλειδί του μπορεί να είναι **πολύ μικρό** και να παραβιάζει την ιδιότητα: μπορεί να είναι μικρότερο από τον γονέα του.

Η `Heapify-up` το διορθώνει **σπρώχνοντάς το προς τα πάνω** όσο είναι μικρότερο από τον γονέα του:

```
Heapify-up(H, i):
  Εάν i > 1 τότε:
    j ← parent(i) = ⌊i/2⌋
    Εάν key(H[i]) < key(H[j]) τότε:
      Εναλλαγή των H[i] και H[j]
      Heapify-up(H, j)
```

<Callout type="intuition">
**Διαισθητικά:** το νέο, πολύ ελαφρύ στοιχείο «αναδύεται» σαν φυσαλίδα προς την επιφάνεια. Σε κάθε βήμα ανταλλάσσεται με τον γονέα του, μέχρι ή να βρει γονέα μικρότερο από αυτό, ή να γίνει το ίδιο ρίζα.
</Callout>

**Γιατί διορθώνει σωστά:** όταν ανταλλάσσουμε το $H[i]$ με τον γονέα του, το μικρό κλειδί ανεβαίνει — και ο γονέας, που ήταν μεγαλύτερος, κατεβαίνει σε μια θέση όπου τα παιδιά του ήταν ήδη $\ge$ από το παλιό περιεχόμενό της, άρα $\ge$ από αυτόν. Δεν δημιουργείται νέα παραβίαση πουθενά.

**Χρόνος:** το μονοπάτι προς τη ρίζα έχει μήκος $\le \log_2 n$, και κάθε βήμα είναι $O(1)$. Άρα `Heapify-up` $= O(\log n)$.

**Χρήση — Insert:** τοποθέτησε το νέο στοιχείο στο τέλος, κάλεσε `Heapify-up`. Συνολικά $O(\log n)$.

### Heapify-down — επιδιόρθωση προς τα φύλλα

Συμμετρικό πρόβλημα: μια κορυφή έχει κλειδί **πολύ μεγάλο**, μεγαλύτερο από κάποιο παιδί της. Τη σπρώχνουμε **προς τα κάτω**:

```
Heapify-down(H, i):
  Έστω l = 2i, r = 2i + 1   (παιδιά της i)
  Έστω j η θέση του παιδιού με το ΜΙΚΡΟΤΕΡΟ κλειδί (αν υπάρχουν παιδιά)
  Εάν υπάρχει j και key(H[j]) < key(H[i]) τότε:
    Εναλλαγή των H[i] και H[j]
    Heapify-down(H, j)
```

<Callout type="warning">
**Κρίσιμη λεπτομέρεια:** ανταλλάσσουμε με το **μικρότερο** από τα δύο παιδιά — όχι με όποιο τύχει. Αν ανταλλάσσαμε με το μεγαλύτερο, αυτό θα κατέληγε γονιός του μικρότερου αδελφού του και θα παραβιαζόταν ξανά η ιδιότητα. Με το μικρότερο, ο νέος γονιός είναι σίγουρα $\le$ και από τα δύο παιδιά.
</Callout>

**Χρόνος:** ίδιο επιχείρημα — μονοπάτι προς τα φύλλα μήκους $\le \log_2 n$. `Heapify-down` $= O(\log n)$.

### Ουρά προτεραιότητας

Συνδυάζοντας τα παραπάνω παίρνουμε την **ουρά προτεραιότητας** — μια δομή που πάντα ξέρει «τι έχει σειρά»:

| Πράξη | Τι κάνει | Χρόνος |
|---|---|---|
| `CreateHeap(H)` | κενός σωρός για $\le n$ στοιχεία | $O(n)$ |
| `Insert(H, v)` | βάλε στο τέλος, `Heapify-up` | $O(\log n)$ |
| `FindMin(H)` | επίστρεψε τη ρίζα $H[1]$ | $O(1)$ |
| `Delete(H, i)` | βγάλε τη θέση $i$, `Heapify-down` (ή up) | $O(\log n)$ |
| `ExtractMin(H)` | `FindMin` + `Delete` της ρίζας | $O(\log n)$ |

<Callout type="intuition">
**Πώς δουλεύει το `ExtractMin`:** η απάντηση είναι η ρίζα — εύκολο. Αλλά τώρα η ρίζα είναι «τρύπα». Φέρνουμε το **τελευταίο** στοιχείο του πίνακα στη ρίζα (διατηρεί το σχήμα), και κάνουμε `Heapify-down` — αυτό το (μάλλον μεγάλο) στοιχείο βυθίζεται στη σωστή του θέση.
</Callout>

Η ουρά προτεραιότητας υποστηρίζει επίσης `decreaseKey(H, v, k)`: μειώνει το κλειδί ενός στοιχείου και καλεί `Heapify-up`. Αυτή ακριβώς είναι η πράξη που κάνουν ο Dijkstra και ο Prim όταν βρίσκουν φθηνότερο μονοπάτι/ακμή προς μια κορυφή.

### Εφαρμογή — Heapsort

Με την ουρά προτεραιότητας παίρνουμε δωρεάν έναν αλγόριθμο ταξινόμησης:

1. `CreateHeap` και βάλε μέσα και τα $n$ στοιχεία.
2. Κάλεσε `ExtractMin` $n$ φορές — βγαίνουν σε **αύξουσα** σειρά.

**Χρόνος:** $n$ φορές το $O(\log n)$ → **$O(n \log n)$**. Είναι το ίδιο φράγμα με τη συγχωνευτική ταξινόμηση του [L03](/lectures/L03-divide-and-conquer-i), αλλά με εντελώς διαφορετική ιδέα.

### Πώς οι σωροί επιταχύνουν τον Dijkstra και τον Prim

Τώρα κλείνει η υπόσχεση του [L09](/lectures/L09-graphs-iv). Και οι δύο αλγόριθμοι κρατάνε μια ουρά προτεραιότητας από ανεξερεύνητες κορυφές:

- **`ExtractMin`** — διάλεξε την επόμενη κορυφή προς εξερεύνηση. Καλείται $n$ φορές → $O(n \log n)$.
- **`decreaseKey`** — όταν μια κορυφή $v$ μπει στο $S$, για κάθε ακμή $(v, w)$ ενημέρωσε την προτεραιότητα του $w$. Συνολικά μέχρι $m$ φορές → $O(m \log n)$.

Σύνολο: **$O(m \log n)$** — ακριβώς το φράγμα που ανακοινώσαμε στο L09. Χωρίς σωρό, το `ExtractMin` θα ήταν γραμμική σάρωση $O(n)$ και θα κατέληγες σε $O(n^2)$.

## Μέρος Β' — Ξένα Σύνολα (Union-Find)

### Το πρόβλημα που λύνουν

Θυμήσου τον **Kruskal** από το [L09](/lectures/L09-graphs-iv): σαρώνει ακμές σε αύξουσα σειρά κόστους και προσθέτει την $\{u, v\}$ **εκτός αν δημιουργεί κύκλο**. Πώς ελέγχεις «δημιουργεί κύκλο;» Μια ακμή $\{u, v\}$ κλείνει κύκλο **ακριβώς όταν** οι $u$ και $v$ είναι **ήδη** στην ίδια συνεκτική συνιστώσα του μέχρι τώρα δέντρου.

Άρα χρειαζόμαστε μια δομή που κρατάει μια **διαμέριση** — μια συλλογή από **ανά δύο ξένα σύνολα** — και απαντά γρήγορα: «το $u$ και το $v$ είναι στο ίδιο σύνολο;»

### Οι τρεις πράξεις

<Callout type="key">
Η δομή **ξένων συνόλων (union-find)** διατηρεί μια συλλογή $\mathcal{S} = \{S_1, \dots, S_k\}$ από ανά δύο ξένα σύνολα. Κάθε σύνολο έχει έναν **αντιπρόσωπο** (ένα μέλος του). Πράξεις:

- **`MakeSet(x)`** — φτιάξε νέο σύνολο με μοναδικό μέλος (και αντιπρόσωπο) το $x$.
- **`Union(x, y)`** — συγχώνευσε τα σύνολα που περιέχουν τα $x$ και $y$ σε ένα.
- **`Find(x)`** — επίστρεψε τον αντιπρόσωπο του συνόλου που περιέχει το $x$.
</Callout>

Ο αντιπρόσωπος είναι το «όνομα» του συνόλου: δύο στοιχεία είναι στο ίδιο σύνολο **αν και μόνο αν** έχουν τον ίδιο `Find`.

### Εφαρμογή — συνεκτικές συνιστώσες

Με αυτές τις τρεις πράξεις, η εύρεση των συνεκτικών συνιστωσών ενός γραφήματος γίνεται μηχανικά:

```
ΣυνεκτικέςΣυνιστώσες(G = (V, E)):
  Για κάθε κορυφή v ∈ V:
    MakeSet(v)
  Για κάθε ακμή {u, v} ∈ E:
    Εάν Find(u) ≠ Find(v):
      Union(u, v)
```

Στην αρχή κάθε κορυφή είναι μόνη της. Κάθε ακμή «κολλάει» τα δύο της άκρα στην ίδια συνιστώσα. Στο τέλος, για δύο κορυφές $x, y$:

```
ΊδιαΣυνιστώσα(x, y):
  επίστρεψε ( Find(x) = Find(y) )
```

### Μια αφελής προσέγγιση

Η πιο απλή ιδέα: ένας πίνακας $\text{label}[\,]$ όπου $\text{label}[x]$ = το «όνομα» του συνόλου του $x$.

- `Find(x)` — επίστρεψε $\text{label}[x]$. **$O(1)$** — εξαιρετικό.
- `Union(x, y)` — διάλεξε ένα όνομα και **σάρωσε όλον τον πίνακα** αλλάζοντας κάθε εμφάνιση του άλλου ονόματος. **$O(n)$** — απαίσιο.

Με $n$ ενώσεις φτάνουμε $O(n^2)$. Πληρώνουμε ακριβά στο `Union`. Χρειαζόμαστε καλύτερη ισορροπία.

### Κατευθυνόμενα δέντρα με ρίζα

Η σωστή ιδέα: αναπαριστούμε **κάθε σύνολο σαν ένα κατευθυνόμενο δέντρο με ρίζα**.

- Κάθε κορυφή έχει **έναν** δείκτη: προς τον **γονέα** της.
- Η **ρίζα** δείχνει στον εαυτό της (θηλιά) — και είναι ο **αντιπρόσωπος** του συνόλου.
- Πολλά ξένα σύνολα → ένα κατευθυνόμενο **δάσος** με ρίζες.

<div className="graph-canvas">
<svg viewBox="0 0 720 320" className="w-full max-w-3xl" xmlns="http://www.w3.org/2000/svg">
  <style>{`
    .un { fill: rgb(var(--bg-elevated)); stroke: rgb(var(--accent)); stroke-width: 2; }
    .uroot { fill: rgb(244 63 94 / 0.22); stroke: rgb(244 63 94); stroke-width: 2.6; }
    .uk { font: 700 14px ui-sans-serif, system-ui; fill: rgb(var(--fg)); text-anchor: middle; dominant-baseline: middle; }
    .ue { stroke: rgb(var(--fg-muted)); stroke-width: 1.8; fill: none; marker-end: url(#uf-arr); }
    .ann { font: 600 12px ui-sans-serif, system-ui; fill: rgb(var(--fg-muted)); }
  `}</style>
  <defs>
    <marker id="uf-arr" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="rgb(var(--fg-muted))" />
    </marker>
  </defs>
  {/* tree 1 rooted at a */}
  <path d="M 150 48 a 16 16 0 1 1 28 0" className="ue" />
  <line x1="120" y1="90" x2="135" y2="62" className="ue" />
  <line x1="200" y1="90" x2="170" y2="62" className="ue" />
  <line x1="80" y1="150" x2="110" y2="112" className="ue" />
  <line x1="160" y1="150" x2="130" y2="112" className="ue" />
  {[['a',150,48,true],['b',110,100,false],['d',210,100,false],['c',70,160,false],['e',150,160,false]].map(([id,x,y,root]) => (
    <g key={id}>
      <circle cx={x} cy={y} r="22" className={root ? 'uroot' : 'un'} />
      <text x={x} y={y} className="uk">{id}</text>
    </g>
  ))}
  <text x="140" y="210" textAnchor="middle" className="ann">Σύνολο {'{a,b,c,d,e}'}, αντιπρόσωπος a</text>
  {/* tree 2 rooted at f */}
  <path d="M 470 48 a 16 16 0 1 1 28 0" className="ue" />
  <line x1="440" y1="90" x2="455" y2="62" className="ue" />
  <line x1="540" y1="90" x2="500" y2="62" className="ue" />
  <line x1="560" y1="150" x2="555" y2="112" className="ue" />
  {[['f',470,48,true],['g',430,100,false],['h',550,100,false],['i',570,160,false]].map(([id,x,y,root]) => (
    <g key={id}>
      <circle cx={x} cy={y} r="22" className={root ? 'uroot' : 'un'} />
      <text x={x} y={y} className="uk">{id}</text>
    </g>
  ))}
  <text x="500" y="210" textAnchor="middle" className="ann">Σύνολο {'{f,g,h,i}'}, αντιπρόσωπος f</text>
  <text x="360" y="270" textAnchor="middle" className="ann">Η ρίζα κάθε δέντρου έχει θηλιά στον εαυτό της — είναι ο αντιπρόσωπος.</text>
</svg>
</div>

### Υλοποίηση των πράξεων

- **`MakeSet(x)`** — φτιάξε δέντρο με μία κορυφή $x$ (θηλιά στον εαυτό της). $O(1)$.
- **`Find(x)`** — ακολούθησε τους δείκτες-γονείς από το $x$ μέχρι να φτάσεις στη ρίζα (την κορυφή με θηλιά). Επίστρεψέ την.
- **`Union(x, y)`** — βρες τις δύο ρίζες· κάνε τη ρίζα του ενός δέντρου να δείχνει στη ρίζα του άλλου (αφαιρώντας τη θηλιά της). $O(1)$ **αφού** έχεις τις ρίζες.

<Callout type="warning">
**Το πρόβλημα:** το κόστος είναι ολόκληρο στο `Find`. Αν ένα δέντρο εκφυλιστεί σε **μονοπάτι** (αλυσίδα), το `Find` από το φύλλο διασχίζει $O(n)$ κορυφές. Πίσω στο τετράγωνο. Χρειαζόμαστε τα δέντρα να μένουν **κοντά**.
</Callout>

### Βάρη στα δέντρα (union by size)

Πρώτη βελτίωση: για κάθε δέντρο αποθηκεύουμε το **μέγεθός** του, και στο `Union(x, y)` κάνουμε **τη ρίζα του μικρότερου δέντρου να δείχνει στη ρίζα του μεγαλύτερου** (όχι ανάποδα).

<Callout type="key">
**Θεώρημα.** Με union-by-size, σε ένα σύνολο με $k$ στοιχεία κάθε στοιχείο απέχει το πολύ $\log_2 k$ βήματα από τη ρίζα του. Άρα `Find` $= O(\log n)$, και αρκούν $2\log n$ βήματα για να αποφασίσουμε αν δύο στοιχεία είναι στο ίδιο σύνολο.
</Callout>

**Απόδειξη (επαγωγή στο πλήθος ενώσεων).** Ισχυρισμός: δέντρο με $k$ κορυφές έχει βάθος $\le \log_2 k$.

Βάση: δέντρο με $1$ κορυφή έχει βάθος $0 = \log_2 1$. ✓

Επαγωγικό βήμα: ένα δέντρο με $k$ κορυφές προέκυψε από `Union` δύο δέντρων με $k_1$ και $k_2$ κορυφές, $k_1 \le k_2$, $k_1 + k_2 = k$. Βάλαμε το μικρό ($k_1$) κάτω από το μεγάλο ($k_2$). Το βάθος του νέου δέντρου είναι το πολύ:

$$
\max\bigl\{\, \underbrace{\log_2 k_2}_{\text{το μεγάλο μένει ίδιο}},\ \ \underbrace{1 + \log_2 k_1}_{\text{το μικρό κατεβαίνει 1}} \,\bigr\}
$$

Αφού $k_1 \le k/2$, έχουμε $1 + \log_2 k_1 \le 1 + \log_2(k/2) = \log_2 k$. Και $\log_2 k_2 \le \log_2 k$. Άρα το βάθος $\le \log_2 k$. ∎

<Callout type="intuition">
**Γιατί δουλεύει:** μια κορυφή «κατεβαίνει» ένα επίπεδο μόνο όταν το δέντρο της ενώνεται κάτω από ένα **τουλάχιστον ισομέγεθες**. Άρα κάθε φορά που κατεβαίνει, το σύνολό της **τουλάχιστον διπλασιάζεται**. Δεν μπορεί να διπλασιαστεί πάνω από $\log_2 n$ φορές → βάθος $\le \log_2 n$.
</Callout>

### Συμπίεση μονοπατιών (path compression)

Δεύτερη βελτίωση, σχεδόν δωρεάν. Κάθε φορά που τρέχουμε `Find(v)` έχουμε ήδη διασχίσει όλο το μονοπάτι από το $v$ μέχρι τη ρίζα $x$. Πριν επιστρέψουμε, **ξανασυνδέουμε κάθε κορυφή αυτού του μονοπατιού απευθείας στη ρίζα $x$**.

<Callout type="intuition">
**Διαισθητικά:** «αφού πλήρωσα ήδη να ανέβω αυτό το μονοπάτι, ας το ισιώσω για την επόμενη φορά.» Η επόμενη `Find` σε οποιαδήποτε από αυτές τις κορυφές θα είναι ένα βήμα. Μικραίνουμε το **ύψος** των δέντρων χωρίς να πειράζουμε τα μεγέθη/βάρη.
</Callout>

Με **union-by-size + path compression** μαζί, μια ακολουθία $m$ πράξεων σε $n$ στοιχεία τρέχει σε $O(m \cdot \alpha(n))$, όπου $\alpha$ είναι η **αντίστροφη συνάρτηση Ackermann** — μεγαλώνει τόσο αφάνταστα αργά που για κάθε $n$ που θα συναντήσεις ποτέ στο σύμπαν, $\alpha(n) \le 4$. Πρακτικά **σταθερά**.

### Πώς το union-find επιταχύνει τον Kruskal

Κλείνει και η δεύτερη υπόσχεση του L09. Ο Kruskal:

```
Kruskal(G, c):
  Ταξινόμησε τις ακμές κατά κόστος          → O(m log n)
  Για κάθε u ∈ V: MakeSet(u)
  Για i = 1 έως m, με {u, v} = e_i:
    Εάν Find(u) ≠ Find(v):                  ← «κλείνει κύκλο;»
      πρόσθεσε e_i στο T
      Union(u, v)
```

- **Ταξινόμηση:** $O(m \log n)$.
- **$m$ ζεύγη `Find` + έως $n-1$ `Union`:** $O(m \cdot \alpha(n))$ — ουσιαστικά γραμμικό.
- **Σύνολο:** $O(m \log n)$ — η ταξινόμηση κυριαρχεί. Ακριβώς το φράγμα του L09.

<Recap>
**Τι κρατάμε από αυτή τη διάλεξη**

1. **Σωρός** — ισοσταθμισμένο δυαδικό δέντρο με ιδιότητα διάταξης (γονιός $\le$ παιδιά). Αποθηκεύεται σε πίνακα: $\text{parent}(i)=\lfloor i/2 \rfloor$, $\text{children}=2i, 2i{+}1$.
2. **Heapify-up / Heapify-down** — επιδιορθώνουν μία παραβίαση σε $O(\log n)$, σπρώχνοντας ένα στοιχείο προς τη ρίζα ή προς τα φύλλα (πάντα προς το **μικρότερο** παιδί).
3. **Ουρά προτεραιότητας** — `FindMin` $O(1)$, `Insert`/`Delete`/`ExtractMin` $O(\log n)$. Δίνει **Heapsort** $O(n \log n)$ και κάνει τον **Dijkstra** & **Prim** $O(m \log n)$.
4. **Union-Find** — `MakeSet`, `Union`, `Find` πάνω σε διαμέριση σε ξένα σύνολα. Κάθε σύνολο = κατευθυνόμενο δέντρο με ρίζα = αντιπρόσωπο.
5. **Union by size** κρατάει το βάθος $\le \log n$· **path compression** το μικραίνει κι άλλο. Μαζί → $O(\alpha(n))$ ανά πράξη, πρακτικά σταθερό.
6. Το union-find κάνει τον **Kruskal** $O(m \log n)$ — η ταξινόμηση των ακμών είναι πια το ακριβό κομμάτι.
</Recap>

<NextUp current="lectures/L10-data-structures" />

<LectureExercises lectureSlug="lectures/L10-data-structures" />
