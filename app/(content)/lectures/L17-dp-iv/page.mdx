export const metadata = {
  title: 'L17 · Δυναμικός Προγραμματισμός IV — Ανεξάρτητο Σύνολο σε Δέντρα & Bellman-Ford',
  description:
    'Δυναμικός προγραμματισμός σε γραφήματα: ανεξάρτητο σύνολο μέγιστου βάρους σε δέντρα, και ο αλγόριθμος Bellman-Ford για συντομότερες διαδρομές με αρνητικά βάρη.',
}

export const frontmatter = {
  title: 'L17 · Δυναμικός Προγραμματισμός IV — Ανεξάρτητο Σύνολο σε Δέντρα & Bellman-Ford',
  slug: 'lectures/L17-dp-iv',
  order: 17,
  prerequisites: ['lectures/L16-dp-iii', 'lectures/L09-graphs-iv'],
  examWeight: 8,
  estimatedReadTime: 42,
  lastUpdated: '2026-05-23',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Dynamic programming</span>
  <span>·</span>
  <span>Διάλεξη 17</span>
  <span>·</span>
  <span>~42 min</span>
</div>

# L17 · Δυναμικός Προγραμματισμός IV — Ανεξάρτητο Σύνολο σε Δέντρα &amp; Bellman-Ford

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="lectures/L17-dp-iv" />
  <Bookmark id="lectures/L17-dp-iv" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc
  sources={{ pdf: 'L17 - Δυναμικός Προγραμματισμός ΙV.pdf' }}
  note="54 διαφάνειες — η τελευταία διάλεξη του μαθήματος: δυναμικός προγραμματισμός σε δέντρα, και ο Bellman-Ford για αρνητικά βάρη."
/>

## Τι θα δούμε

Φτάσαμε στην **τελευταία** διάλεξη. Σε όλον τον δυναμικό προγραμματισμό μέχρι τώρα ([L14](/lectures/L14-dp-i)–[L16](/lectures/L16-dp-iii)) το υποπρόβλημα είχε σχεδόν πάντα το ίδιο σχήμα: «τα πρώτα $j$ στοιχεία μιας ακολουθίας». Σήμερα το υποπρόβλημα γίνεται κάτι πιο πλούσιο — ένα κομμάτι ενός **γραφήματος**:

1. **Ανεξάρτητο σύνολο σε δέντρα** — DP όπου τα υποπροβλήματα είναι **υποδέντρα**.
2. **Bellman-Ford** — συντομότερες διαδρομές όταν κάποιες ακμές έχουν **αρνητικό** βάρος, εκεί ακριβώς που ο Dijkstra του [L09](/lectures/L09-graphs-iv) τα παρατάει. Έτσι κλείνει και μια υπόσχεση που έμεινε ανοιχτή από τότε.

## Ανεξάρτητο σύνολο σε δέντρα — «το πάρτι»

### Το πρόβλημα

Οργανώνουμε ένα πάρτι για μια εταιρεία με **αυστηρή ιεραρχική δομή**. Κάθε υπάλληλος $v$ έχει μια θετική «προσφορά» $\chi(v)$ — πόσο κέφι φέρνει στο πάρτι. Ένας περιορισμός: **δεν** καλούμε ταυτόχρονα έναν υπάλληλο και τον **άμεσο προϊστάμενό** του.

**Είσοδος:** ένα **δέντρο** που περιγράφει την ιεραρχία, με μια προσφορά σε κάθε κορυφή.

**Στόχος:** μια λίστα καλεσμένων με το **μέγιστο άθροισμα προσφορών**, που σέβεται τον περιορισμό.

<Callout type="intuition">
Σε ορολογία γραφημάτων: ζητάμε ένα **ανεξάρτητο σύνολο** — ένα σύνολο κορυφών χωρίς καμία ακμή ανάμεσά τους — με **μέγιστο βάρος**. Στα γενικά γραφήματα αυτό το πρόβλημα είναι NP-δύσκολο: κανείς δεν ξέρει γρήγορο αλγόριθμο. Αλλά σε **δέντρο** ο δυναμικός προγραμματισμός το λύνει εύκολα και γρήγορα. Η δομή του δέντρου είναι όλο το μυστικό.
</Callout>

### Δύο τιμές ανά κορυφή

Όπως σε κάθε DP ([L14](/lectures/L14-dp-i)), το πρώτο βήμα είναι να βρούμε το σωστό **υποπρόβλημα**. Εδώ η επιλογή είναι φυσική: για κάθε κορυφή $v$, κοίτα μόνο το **υποδέντρο** της — την $v$ και όλους τους απογόνους της. Αν λύσουμε κάθε υποδέντρο, η απάντηση για ολόκληρο το δέντρο είναι απλώς το υποδέντρο της ρίζας.

Μένει να συνδέσουμε ένα υποδέντρο με τα υποδέντρα των παιδιών του. Κι εδώ κρύβεται μια παγίδα.

**Μία τιμή ανά κορυφή δεν αρκεί.** Ας υποθέσουμε ότι για κάθε κορυφή κρατάμε έναν μόνο αριθμό: «το βάρος του καλύτερου ανεξάρτητου συνόλου μέσα στο υποδέντρό της». Ακούγεται λογικό — αλλά σπάει. Όταν αποφασίζουμε για μια κορυφή $v$, αν την **βάλουμε** μέσα τότε **κανένα παιδί της δεν επιτρέπεται**. Χρειαζόμαστε λοιπόν να ξέρουμε «το καλύτερο του υποδέντρου ενός παιδιού *χωρίς το ίδιο το παιδί*». Ένας μόνο αποθηκευμένος αριθμός δεν το ξεχωρίζει αυτό — μπορεί κρυφά να περιλαμβάνει το παιδί, κι έτσι η σύνθεση βγάζει παράνομο σύνολο. Δοκίμασέ το:

<WhyTwoTreeValues />

Η λύση είναι ακριβώς αυτό που έδειξε το παράδειγμα: κρατάμε **δύο** τιμές ανά κορυφή, μία για κάθε σενάριο.

<Callout type="key">
- $A[v]$ = μέγιστη προσφορά ενός έγκυρου υποσυνόλου του **υποδέντρου** της $v$ (η $v$ επιτρέπεται να μπει ή όχι — ό,τι δίνει μεγαλύτερο άθροισμα).
- $B[v]$ = μέγιστη προσφορά ενός έγκυρου υποσυνόλου του υποδέντρου της $v$ που **δεν περιέχει** την $v$.
</Callout>

Έστω $v$ με παιδιά (άμεσους υφισταμένους) $v_1, v_2, \dots, v_k$.

**Για το $B[v]$:** η $v$ **δεν** μπαίνει — άρα δεν μπλοκάρει κανέναν. Κάθε υφιστάμενος επιλέγει ελεύθερα· για κάθε υποδέντρο παίρνουμε το βέλτιστο $A[v_i]$:

$$B[v] = \sum_{i=1}^{k} A[v_i]$$

**Για το $A[v]$:** δύο σενάρια, παίρνουμε το καλύτερο.

- Η $v$ **δεν** μπαίνει: αυτό είναι ακριβώς το $B[v]$.
- Η $v$ **μπαίνει**: τότε **κανένα** από τα παιδιά δεν επιτρέπεται, οπότε κάθε υποδέντρο $v_i$ συνεισφέρει το $B[v_i]$. Συνολικά: $\chi(v) + \sum_i B[v_i]$.

$$A[v] = \max\Bigl\{\, B[v],\ \ \chi(v) + \sum_{i=1}^{k} B[v_i] \,\Bigr\}$$

### Ο αλγόριθμος

<Algorithm
  name="Ανεξάρτητο σύνολο μέγιστου βάρους σε δέντρο"
  idea="Για κάθε κορυφή κράτα δύο τιμές, A[v] και B[v]· υπολόγισέ τες από τα φύλλα προς τη ρίζα."
  complexity="O(n)"
  io={{
    input: 'δέντρο με προσφορά χ(v) σε κάθε κορυφή',
    output: 'η μέγιστη συνολική προσφορά ενός ανεξάρτητου συνόλου',
  }}
>

**Με λόγια.** Υπολογίζουμε τα $A[v], B[v]$ **από τα φύλλα προς τη ρίζα** — κάθε κορυφή χρειάζεται πρώτα τα παιδιά της. Για **φύλλο** $\ell$: $B[\ell] = 0$ και $A[\ell] = \chi(\ell)$. Για εσωτερική κορυφή, εφαρμόζουμε τους δύο τύπους. Η τελική απάντηση είναι το $A[\text{ρίζα}]$.

<Pseudocode
  code={`ανεξάρτητο-σύνολο-δέντρου(δέντρο T με ρίζα):
  για κάθε κορυφή v του T κατά σειρά φύλλα → ρίζα:
    αν η v είναι φύλλο:
      B[v] ← 0 ;  A[v] ← χ(v)
    αλλιώς, με παιδιά v₁, …, v_k:
      B[v] ← Σᵢ A[vᵢ]
      A[v] ← max{ B[v],  χ(v) + Σᵢ B[vᵢ] }
  επίστρεψε A[ρίζα]`}
  caption="Η σειρά «φύλλα → ρίζα» είναι μια αντίστροφη τοπολογική διάταξη του δέντρου."
/>

**Πολυπλοκότητα.** Κάθε κορυφή και κάθε ακμή επεξεργάζεται $O(1)$ φορές → **$O(n)$** συνολικά.

</Algorithm>

Δες τον υπολογισμό να ανεβαίνει από τα φύλλα στη ρίζα. Σε κάθε κορυφή εμφανίζονται τα $A$ και $B$ της — και στο τέλος φωτίζεται η βέλτιστη λίστα καλεσμένων:

<TreeIndependentSet />

## Συντομότερες διαδρομές με αρνητικά βάρη

### Γιατί ξαναπιάνουμε το πρόβλημα

Στο [L09](/lectures/L09-graphs-iv) λύσαμε το πρόβλημα της συντομότερης διαδρομής με τον αλγόριθμο **Dijkstra** — αλλά με μια ρητή προϋπόθεση: **όλα τα βάρη των ακμών έπρεπε να είναι θετικά**. Τώρα ρίχνουμε αυτόν τον περιορισμό και επιτρέπουμε και **αρνητικά** βάρη $\ell_e$. Φαντάσου ένα δίκτυο όπου κάποιες ακμές κοστίζουν, ενώ άλλες αποδίδουν «κέρδος» — μια πληρωμή, μια έκπτωση, ενέργεια που ανακτάς.

Ακούγεται μικρή αλλαγή. Δεν είναι: όπως θα δούμε αμέσως, ο Dijkstra μπορεί τώρα να δώσει **λάθος** απάντηση — και το ίδιο και κάθε εύκολο μπάλωμα που θα σου ερχόταν στο μυαλό.

### Γιατί ο Dijkstra αποτυγχάνει

Θυμήσου την καρδιά του Dijkstra ([L09](/lectures/L09-graphs-iv)): σε κάθε βήμα **οριστικοποιεί** την κορυφή με τη μικρότερη προσωρινή απόσταση και δεν την ξανακοιτάζει ποτέ. Αυτό στηρίζεται σε μια σιωπηρή υπόθεση: «αν μια κορυφή έχει αυτή τη στιγμή τη μικρότερη απόσταση, τίποτα στο μέλλον δεν μπορεί να τη βελτιώσει». Με θετικά βάρη η υπόθεση ισχύει — κάθε επιπλέον ακμή μόνο **προσθέτει** κόστος.

Με μια αρνητική ακμή, η υπόθεση καταρρέει: μια ακμή μπορεί να **αφαιρέσει** κόστος, οπότε ένα μονοπάτι με περισσότερες ακμές μπορεί τελικά να βγει φθηνότερο. Όμως ο Dijkstra έχει ήδη κλειδώσει την κορυφή. Δες το να συμβαίνει, στο μικρότερο δυνατό παράδειγμα:

<DijkstraNegFail />

<Callout type="warning">
**Ο Dijkstra προϋποθέτει θετικά βάρη.** Η οριστικοποίηση «κλειδώνει» μια κορυφή για πάντα — και μια αρνητική ακμή που θα τη βελτίωνε φτάνει πάντα πολύ αργά.
</Callout>

### Γιατί δεν αρκεί να προσθέσεις μια σταθερά

Εντάξει — ο Dijkstra θέλει θετικά βάρη. Να μια ιδέα που μοιάζει να σώζει την κατάσταση: **βρες το πιο αρνητικό βάρος και πρόσθεσε μια αρκετά μεγάλη σταθερά $k$ σε όλες τις ακμές**, ώστε να γίνουν όλες θετικές. Μετά τρέξε ήσυχα τον Dijkstra.

Δεν δουλεύει — και αξίζει να καταλάβεις **γιατί**, γιατί το λάθος είναι λεπτό. Όταν προσθέτεις $k$ σε **κάθε** ακμή, μια διαδρομή με πολλές ακμές μαζεύει το $k$ **πολλές φορές**, ενώ μια διαδρομή με λίγες ακμές το μαζεύει λίγες. Οι δύο διαδρομές δεν επιβαρύνονται το ίδιο — άρα η σύγκρισή τους **αλλοιώνεται**, και η συντομότερη μπορεί να αλλάξει. Σύρε το $k$ και δες τη σωστή απάντηση να ανατρέπεται:

<ConstantShiftFail />

<Callout type="note">
Η ρίζα του προβλήματος: η προσθήκη σταθεράς αλλάζει τα μήκη **ανάλογα με το πλήθος των ακμών** κάθε διαδρομής, όχι ομοιόμορφα. Χρειαζόμαστε έναν αλγόριθμο που χειρίζεται τα αρνητικά βάρη **κατευθείαν**.
</Callout>

### Αρνητικοί κύκλοι

Πριν φτιάξουμε τον αλγόριθμο, πρέπει να ξεκαθαρίσουμε ένα όριο. Με αρνητικά βάρη, μερικές φορές **δεν υπάρχει καν** συντομότερη διαδρομή.

Φαντάσου έναν **κύκλο** του οποίου τα βάρη αθροίζουν σε αρνητικό αριθμό. Κάθε φορά που τον διασχίζεις ολόκληρο, το συνολικό σου κόστος **πέφτει** κατά ένα σταθερό ποσό. Αν στον δρόμο σου προς τον προορισμό μπορείς να φτάσεις σε έναν τέτοιο κύκλο, μπορείς να γυρνάς μέσα του ξανά και ξανά, ρίχνοντας το κόστος όσο χαμηλά θέλεις. Δεν υπάρχει «καλύτερη» διαδρομή — υπάρχει πάντα μια ακόμη φθηνότερη:

<NegativeCycleWalk />

<Callout type="key">
**Αρνητικός κύκλος:** ένας κύκλος του οποίου το άθροισμα βαρών είναι **αρνητικό**.

Αν υπάρχει διαδρομή από την $s$ στην $t$ που περνά μέσα από αρνητικό κύκλο, **δεν υπάρχει** συντομότερη $s$–$t$ διαδρομή. Διαφορετικά, η συντομότερη διαδρομή **υπάρχει** και είναι **απλή** — δεν επαναλαμβάνει καμία κορυφή.
</Callout>

Κράτα αυτό το «**απλή**»: θα μας φανεί χρήσιμο σε λίγο. Μια απλή διαδρομή σε γράφημα με $n$ κορυφές έχει **το πολύ $n-1$ ακμές** — δεν γίνεται να έχει περισσότερες χωρίς να ξαναπεράσει από κάποια κορυφή.

## Ο αλγόριθμος Bellman-Ford

### Η αναδρομή — παραμετροποίηση με το πλήθος ακμών

Ξέρουμε ήδη τη συνταγή του DP ([L14](/lectures/L14-dp-i)): σπάσε το πρόβλημα σε υποπροβλήματα, βάλ' τα σε μια **σειρά** ώστε καθένα να εξαρτάται μόνο από προηγούμενα, και λύσε τα με αυτή τη σειρά.

Εδώ σκοντάφτουμε. Σε ένα γράφημα με **κύκλους**, η «συντομότερη διαδρομή ως την $v$» μπορεί να εξαρτάται από τη «συντομότερη διαδρομή ως την $w$», που με τη σειρά της εξαρτάται πάλι από την $v$. Τα υποπροβλήματα είναι μπλεγμένα κυκλικά — **δεν υπάρχει φυσική σειρά** να τα λύσεις.

Το κόλπο του Bellman-Ford: πρόσθεσε μια **δεύτερη παράμετρο** που αυξάνει πάντα μονότονα — το **πλήθος των ακμών** που επιτρέπεται να χρησιμοποιεί η διαδρομή. Αυτό δεν μπλέκεται ποτέ σε κύκλο: μια διαδρομή με $i$ ακμές χτίζεται πάντα πάνω σε μία με $i-1$ ακμές. Να η σειρά που έλειπε.

Σταθεροποιούμε έναν **προορισμό** $t$ και ορίζουμε:

<Callout type="key">
$\text{OPT}(i, v)$ = το μήκος του συντομότερου $v \to t$ μονοπατιού που χρησιμοποιεί **το πολύ $i$ ακμές**.
</Callout>

Έστω $P$ το βέλτιστο τέτοιο μονοπάτι. Δύο περιπτώσεις:

- Το $P$ χρησιμοποιεί **το πολύ $i-1$** ακμές — τότε $\text{OPT}(i, v) = \text{OPT}(i-1, v)$.
- Το $P$ χρησιμοποιεί **ακριβώς $i$** ακμές. Έστω $(v, w)$ η πρώτη του ακμή· το υπόλοιπο είναι το συντομότερο $w \to t$ μονοπάτι με $i-1$ ακμές. Κόστος: $\ell_{vw} + \text{OPT}(i-1, w)$.

<Callout type="key">
$$
\text{OPT}(i, v) =
\begin{cases}
0 & i = 0,\ v = t \\
\infty & i = 0,\ v \ne t \\[4pt]
\min\Bigl\{\, \text{OPT}(i-1, v),\ \ \min_{(v,w) \in A}\bigl(\ell_{vw} + \text{OPT}(i-1, w)\bigr) \,\Bigr\} & i > 0
\end{cases}
$$
</Callout>

<Callout type="intuition">
**Πόσο μεγάλο πρέπει να γίνει το $i$;** Αν δεν υπάρχουν αρνητικοί κύκλοι, η συντομότερη διαδρομή είναι **απλή** — δεν επαναλαμβάνει κορυφή — άρα έχει **το πολύ $n-1$ ακμές**. Συνεπώς το $M[n-1, v]$ δίνει τη συντομότερη $v \to t$ απόσταση για **κάθε** κορυφή $v$.
</Callout>

### Ο αλγόριθμος

<Algorithm
  name="Bellman-Ford"
  idea="n−1 γύροι· σε κάθε γύρο, χαλάρωσε κάθε ακμή — επεκτείνοντας τις διαδρομές κατά μία ακμή τη φορά."
  complexity="Θ(mn)"
  io={{
    input: 'κατευθυνόμενο γράφημα με βάρη (ίσως αρνητικά), και προορισμός t',
    output: 'η συντομότερη απόσταση από κάθε κορυφή v προς το t',
  }}
>

**Με λόγια.** Ξεκινάμε με $M[0, t] = 0$ και $\infty$ για κάθε άλλη κορυφή. Σε κάθε από τους $n-1$ γύρους, για κάθε κορυφή $v$ δοκιμάζουμε να βελτιώσουμε την απόστασή της μέσω κάθε εξερχόμενης ακμής $(v, w)$. Μετά τον γύρο $i$, ο πίνακας περιέχει τις συντομότερες διαδρομές με το πολύ $i$ ακμές — οπότε μετά από $n-1$ γύρους έχουμε τις τελικές αποστάσεις.

<Pseudocode
  code={`Bellman-Ford(G, t):
  για κάθε v ∈ V:  M[0, v] ← ∞
  M[0, t] ← 0
  για i = 1 έως n−1:
    για κάθε v ∈ V:
      M[i, v] ← M[i−1, v]
      για κάθε ακμή (v, w) ∈ A:
        M[i, v] ← min( M[i, v],  ℓ(v,w) + M[i−1, w] )
  επίστρεψε M[n−1, ·]`}
/>

**Πολυπλοκότητα.** $n-1$ γύροι, και κάθε γύρος εξετάζει κάθε ακμή μία φορά → $\Theta(mn)$ χρόνος, $\Theta(n^2)$ χώρος.

</Algorithm>

Δες τους γύρους να γεμίζουν τον πίνακα. Πρόσεξε ιδιαίτερα το $M[i, s]$: μετά τον γύρο $i$ είναι ακριβώς το καλύτερο μονοπάτι $s \to t$ με **το πολύ $i$ ακμές** — γι' αυτό πέφτει σταδιακά, $\infty \to 20 \to 10 \to 6$, ανακαλύπτοντας κάθε γύρο ένα μονοπάτι με μία ακμή παραπάνω:

<BellmanFordAnimator />

### Βελτιώσεις

<Callout type="key">
- **Γραμμικός χώρος.** Κράτα έναν **μονοδιάστατο** πίνακα $M[v]$ = το μήκος του καλύτερου $v \to t$ μονοπατιού που βρέθηκε ως τώρα. Μετά από $i$ γύρους, το $M[v]$ δεν είναι μεγαλύτερο από το συντομότερο μονοπάτι με $\le i$ ακμές. Χώρος: $O(m + n)$.
- **Παράλειψη.** Μην εξετάζεις ακμή $(v, w)$ εκτός αν το $M[w]$ **άλλαξε** στον προηγούμενο γύρο — αλλιώς δεν θα δώσει τίποτα νέο.
- **Πρόωρος τερματισμός.** Αν σε έναν ολόκληρο γύρο **καμία** τιμή δεν άλλαξε, σταμάτα — έχεις συγκλίνει. (Αν ύστερα από $n-1$ γύρους κάτι ακόμα αλλάζει, υπάρχει **αρνητικός κύκλος**.)
</Callout>

Με αυτές: $O(m + n)$ χώρος, $O(mn)$ χρόνος στη χειρότερη περίπτωση — αλλά συχνά πολύ ταχύτερα.

<Callout type="note">
**Παραλλαγές.** Ο αλγόριθμος, δοσμένου ενός $t$, υπολογίζει το συντομότερο $v \to t$ μονοπάτι για **κάθε** $v$. Προσαρμόζεται εύκολα: για $s \to v$ αντιστρέφεις τις ακμές· για **DAG** αρκεί ένα πέρασμα κατά τοπολογική διάταξη· και — αλλάζοντας `min` σε `max` — βρίσκει τα **μακρύτερα** μονοπάτια.
</Callout>

<RecallCard
  algorithm="Ανεξάρτητο σύνολο σε δέντρο"
  keywords={['υποπροβλήματα = υποδέντρα', 'δύο τιμές: A[v], B[v]', 'υπολογισμός φύλλα → ρίζα', 'A[v] = max{B[v], χ(v)+ΣB}', 'O(n)']}
  skeleton={[
    'Για κάθε κορυφή v κράτα δύο τιμές: A[v] (η v ελεύθερη) και B[v] (η v έξω).',
    'Φύλλο: B = 0, A = χ.',
    'B[v] = Σ A[παιδιών]· A[v] = max{ B[v], χ(v) + Σ B[παιδιών] }.',
    'Υπολόγισέ τα από τα φύλλα προς τη ρίζα· η απάντηση είναι το A[ρίζα].',
  ]}
  complexity="O(n)"
  trap="Μία τιμή ανά κορυφή δεν αρκεί — η απάντηση εξαρτάται από το ΑΝ η v είναι μέσα. Γι' αυτό δύο τιμές. Στα γενικά γραφήματα το πρόβλημα είναι NP-δύσκολο· μόνο η δομή του δέντρου το κάνει εύκολο."
/>

<RecallCard
  algorithm="Bellman-Ford"
  keywords={['αρνητικά βάρη επιτρεπτά', 'OPT(i, v) = ≤ i ακμές', 'n−1 γύροι', 'αρνητικός κύκλος → καμία λύση', 'Θ(mn)']}
  skeleton={[
    'OPT(i, v) = μήκος συντομότερου v→t μονοπατιού με το πολύ i ακμές.',
    'Αναδρομή: min{ OPT(i−1,v), min στις ακμές (v,w) του ℓ(v,w) + OPT(i−1,w) }.',
    'Χωρίς αρνητικό κύκλο η συντομότερη διαδρομή είναι απλή → n−1 ακμές αρκούν.',
    'Η απάντηση για κάθε v είναι το M[n−1, v].',
  ]}
  complexity="Θ(mn)"
  trap="Ο Dijkstra ΔΕΝ δουλεύει με αρνητικά βάρη — και η προσθήκη σταθεράς δεν βοηθάει (μια πολυάκμη διαδρομή κερδίζει τη σταθερά πολλές φορές). Αν υπάρχει αρνητικός κύκλος στη διαδρομή, ΔΕΝ υπάρχει συντομότερο μονοπάτι."
/>

## Κλείδωσε τη γνώση

Βάλε στη σειρά τα βήματα του Bellman-Ford:

<ReorderDrill
  prompt="Ο αλγόριθμος Bellman-Ford, από την πρώτη ενέργεια στην τελευταία."
  steps={[
    { id: 'bf1', text: 'Θέσε M[0, t] = 0 και M[0, v] = ∞ για κάθε άλλη κορυφή v.' },
    { id: 'bf2', text: 'Επανάλαβε για i = 1 έως n−1 γύρους.' },
    { id: 'bf3', text: 'Σε κάθε γύρο, για κάθε κορυφή v, ξεκίνα από την προηγούμενη τιμή M[i−1, v].' },
    { id: 'bf4', text: 'Για κάθε εξερχόμενη ακμή (v, w), δοκίμασε τη βελτίωση ℓ(v,w) + M[i−1, w].' },
    { id: 'bf5', text: 'Κράτα το ελάχιστο ως νέα τιμή M[i, v].' },
    { id: 'bf6', text: 'Μετά από n−1 γύρους, το M[n−1, v] είναι η συντομότερη v→t απόσταση.' },
  ]}
/>

Και συμπλήρωσε τα κενά:

<ClozeDrill
  prompt="Οι δύο αλγόριθμοι της τελευταίας διάλεξης σε τέσσερις λέξεις-κλειδιά."
  segments={[
    { text: 'Στο ανεξάρτητο σύνολο σε δέντρο, τα υποπροβλήματα είναι ' },
    { blank: 'υποδέντρα', hint: 'τι κομμάτι του δέντρου;' },
    { text: ' και κρατάμε ' },
    { blank: 'δύο', hint: 'πόσες τιμές ανά κορυφή;' },
    { text: ' τιμές ανά κορυφή. Ο Bellman-Ford λύνει συντομότερες διαδρομές ακόμα και με ' },
    { blank: 'αρνητικά', hint: 'τι είδους βάρη;' },
    { text: ' βάρη, παραμετροποιώντας το υποπρόβλημα με το πλήθος των ' },
    { blank: 'ακμών', hint: 'τι μετράμε στη διαδρομή;' },
    { text: ' της διαδρομής.' },
  ]}
/>

## Μοτίβο σκέψης

<ThinkingPattern
  title="DP πέρα από τον πίνακα — δέντρα και γραφήματα"
  signals={['πρόβλημα βελτιστοποίησης σε δέντρο', 'συντομότερες διαδρομές με αρνητικά βάρη', 'το «προφανές» υποπρόβλημα δεν έχει σειρά']}
>
Ο δυναμικός προγραμματισμός δεν περιορίζεται σε πίνακες 1D/2D:

1. **Σε δέντρο**, το φυσικό υποπρόβλημα είναι ένα υποδέντρο. Υπολόγισε από τα φύλλα προς τη ρίζα.
2. **Αν η απάντηση εξαρτάται από μια διακριτή «κατάσταση»** της ρίζας του υποπροβλήματος (μέσα / έξω), κράτα μία τιμή ανά κατάσταση — εδώ $A[v]$, $B[v]$.
3. **Σε γράφημα με κύκλους** δεν υπάρχει φυσική σειρά υποπροβλημάτων. Λύση: παραμετροποίησε με κάτι που **αυξάνει μονότονα** — π.χ. το πλήθος ακμών (Bellman-Ford).
4. **Πάντα οι ίδιες 4 ερωτήσεις** ([L14](/lectures/L14-dp-i)): τι είναι υποπρόβλημα, ποια η αναδρομή, με ποια σειρά υπολογίζω, πώς ανακτώ τη λύση.
</ThinkingPattern>

<ExamRadar
  items={[
    {
      topic: 'DP σε δέντρο — όρισε τα υποπροβλήματα στα υποδέντρα, με τιμές ανά κατάσταση',
      likelihood: 'high',
    },
    {
      topic: 'Bellman-Ford — τρέξε τον και υπολόγισε τις συντομότερες αποστάσεις',
      likelihood: 'high',
      relatedExerciseIds: ['pt4-th1-q3'],
    },
    {
      topic: 'Γιατί ο Dijkstra αποτυγχάνει με αρνητικά βάρη — αντιπαράδειγμα',
      likelihood: 'medium',
    },
    {
      topic: 'Αρνητικοί κύκλοι: γιατί καταργούν τη συντομότερη διαδρομή, και πώς εντοπίζονται',
      likelihood: 'medium',
      relatedExerciseIds: ['pt1-th2-a'],
    },
    {
      topic: 'Ανεξάρτητο σύνολο μέγιστου βάρους σε δέντρο σε O(n)',
      likelihood: 'medium',
    },
    {
      topic: 'Ταξινόμηση αλγορίθμων: άπληστος vs DP — διαγνωστικό «κλείδωμα ή πίνακας;»',
      likelihood: 'high',
      relatedExerciseIds: ['pt4-th1-q3'],
      note: 'Σ/Λ ερωτήσεις τύπου «είναι ο X άπληστος;» θέλουν το διαγνωστικό «μία αμετάκλητη απόφαση το βήμα, ή νέα γραμμή που διαβάζει την προηγούμενη;»',
    },
    {
      topic: 'Διπλή αναδρομή σε DAG — count + sum για μέσο όρο πάνω σε όλα τα μονοπάτια',
      likelihood: 'medium',
      relatedExerciseIds: ['front-set-8-ask1'],
      note: 'Όταν θες μέσο όρο πάνω σε ολόκληρη οικογένεια (μονοπάτια, βέλτιστες λύσεις, αναμενόμενη τιμή), τρέξε ΔΥΟ παράλληλες αναδρομές σε λοκ-στεπ.',
    },
  ]}
/>

<Recap>
**Τι κρατάμε από αυτή τη διάλεξη**

1. **Ανεξάρτητο σύνολο σε δέντρα** — DP με υποπροβλήματα τα υποδέντρα. Δύο τιμές ανά κορυφή: $A[v]$ (η $v$ ελεύθερη) και $B[v]$ (η $v$ έξω). $B[v] = \sum A[v_i]$, $A[v] = \max\{B[v],\ \chi(v) + \sum B[v_i]\}$. $O(n)$.
2. Ο **Dijkstra αποτυγχάνει** με αρνητικά βάρη· η προσθήκη σταθεράς **δεν** βοηθάει.
3. **Αρνητικός κύκλος** → δεν υπάρχει συντομότερη διαδρομή· αλλιώς υπάρχει και είναι **απλή** ($\le n-1$ ακμές).
4. **Bellman-Ford** — $\text{OPT}(i, v)$ = συντομότερο $v \to t$ μονοπάτι με $\le i$ ακμές. Αναδρομή: $\min\{\text{OPT}(i-1,v),\ \min_{(v,w)}(\ell_{vw} + \text{OPT}(i-1,w))\}$. Απάντηση στο $M[n-1, v]$.
5. $\Theta(mn)$ χρόνος· με μονοδιάστατο πίνακα, παράλειψη αμετάβλητων κορυφών και πρόωρο τερματισμό → $O(m+n)$ χώρος.
</Recap>

<NextUp current="lectures/L17-dp-iv" />

<LectureExercises lectureSlug="lectures/L17-dp-iv" />
