export const metadata = {
  title: 'L12 · Άπληστοι Αλγόριθμοι II — Ελάχιστη Καθυστέρηση & Τοπολογική Διάταξη',
  description:
    'Χρονοπρογραμματισμός με ελάχιστη μέγιστη καθυστέρηση (Earliest Deadline First), το επιχείρημα ανταλλαγής, οι τρεις τεχνικές απόδειξης άπληστων αλγορίθμων, και η τοπολογική διάταξη σε DAG.',
}

export const frontmatter = {
  title: 'L12 · Άπληστοι Αλγόριθμοι II — Ελάχιστη Καθυστέρηση & Τοπολογική Διάταξη',
  slug: 'lectures/L12-greedy-ii',
  order: 12,
  prerequisites: ['lectures/L11-greedy-i'],
  examWeight: 7,
  estimatedReadTime: 36,
  lastUpdated: '2026-05-22',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Greedy</span>
  <span>·</span>
  <span>Διάλεξη 12</span>
  <span>·</span>
  <span>~36 min</span>
</div>

# L12 · Άπληστοι Αλγόριθμοι II — Ελάχιστη Καθυστέρηση &amp; Τοπολογική Διάταξη

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="lectures/L12-greedy-ii" />
  <Bookmark id="lectures/L12-greedy-ii" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc
  sources={{ pdf: 'L12 - Άπληστοι Αλγόριθμοι II.pdf' }}
  note="21 διαφάνειες — η δεύτερη τεχνική απόδειξης άπληστων αλγορίθμων, και η τοπολογική διάταξη."
/>

## Τι θα δούμε

Δύο θέματα — και μία τεχνική απόδειξης που θα ξαναδείς σε **κάθε** εξεταστική:

1. **Χρονοπρογραμματισμός με ελάχιστη καθυστέρηση**, και μαζί το **επιχείρημα ανταλλαγής** (exchange argument) — η δεύτερη μεγάλη τεχνική για άπληστους αλγορίθμους, μετά το «ο άπληστος προηγείται» του [L11](/lectures/L11-greedy-i).
2. **Τοπολογική διάταξη** — πώς βάζεις σε σειρά εργασίες με προαπαιτούμενα, και γιατί αυτό γίνεται **αν και μόνο αν** δεν υπάρχουν κύκλοι.

## Χρονοπρογραμματισμός με ελάχιστη καθυστέρηση

### Διατύπωση

Μία μηχανή, που επεξεργάζεται **μία εργασία τη φορά**.

- Η εργασία $j$ απαιτεί $t_j$ μονάδες χρόνου και έχει **προθεσμία** $d_j$.
- Αν ξεκινήσει τη στιγμή $s_j$, τελειώνει τη στιγμή $f_j = s_j + t_j$.
- **Καθυστέρηση** της $j$: $\ \ell_j = \max\{0,\ f_j - d_j\}$ — πόσο **μετά** την προθεσμία της τελείωσε (ή $0$ αν πρόλαβε).

**Στόχος:** προγραμμάτισε **όλες** τις εργασίες ελαχιστοποιώντας τη **μέγιστη** καθυστέρηση $L = \max_j \ell_j$.

<Callout type="intuition">
**Πρόσεξε τη διαφορά από το [L11](/lectures/L11-greedy-i).** Εκεί διαλέγαμε ένα **υποσύνολο**· εδώ προγραμματίζουμε **όλες** τις εργασίες — απλώς αποφασίζουμε τη **σειρά** τους. Και δεν μετράμε άθροισμα καθυστερήσεων, αλλά τη **χειρότερη**: μία πολύ αργοπορημένη εργασία χαλάει τη λύση, ακόμη κι αν όλες οι άλλες είναι στην ώρα τους.
</Callout>

### Υποψήφια κριτήρια

Πάλι το ίδιο άπληστο πρότυπο: βάλε τις εργασίες σε **κάποια σειρά**.

| Κριτήριο | Σειρά | Αποτέλεσμα |
|---|---|---|
| Μικρότερος χρόνος επεξεργασίας | αύξουσα $t_j$ | **αποτυγχάνει** |
| Μικρότερο χρονικό περιθώριο | αύξουσα $d_j - t_j$ | **αποτυγχάνει** |
| Μικρότερη προθεσμία | αύξουσα $d_j$ | **δουλεύει** ✓ |

<Callout type="warning">
**Γιατί αποτυγχάνει το «μικρότερος χρόνος επεξεργασίας»:** αγνοεί τελείως την προθεσμία — μια σύντομη εργασία με προθεσμία στο μακρινό μέλλον θα προηγηθεί μιας μακράς εργασίας που λήγει αμέσως. **Γιατί αποτυγχάνει το «μικρότερο περιθώριο»:** μια εργασία με μικρό περιθώριο αλλά μακρινή προθεσμία μπορεί να καθυστερήσει μία άλλη με κοντινή προθεσμία. Όπως πάντα με τους άπληστους — ένα αντιπαράδειγμα αρκεί για να απορρίψεις τον κανόνα.
</Callout>

### Ο αλγόριθμος: Earliest Deadline First

<Callout type="intuition">
**Διαισθητικά:** «κάνε πρώτα ό,τι λήγει πρώτο». Είναι ακριβώς αυτό που κάνεις πριν την εξεταστική — το μάθημα με την κοντινότερη ημερομηνία πάει πρώτο, ασχέτως πόσο διάβασμα θέλει.
</Callout>

<Algorithm
  name="Earliest Deadline First — ελάχιστη μέγιστη καθυστέρηση"
  idea="Ταξινόμησε τις εργασίες κατά αύξουσα προθεσμία και τρέξε τες τη μία μετά την άλλη, χωρίς κενά."
  complexity="O(n log n)"
  io={{
    input: 'n εργασίες, η καθεμία με χρόνο επεξεργασίας tⱼ και προθεσμία dⱼ',
    output: 'μια σειρά εκτέλεσης που ελαχιστοποιεί τη μέγιστη καθυστέρηση L',
  }}
>

**Με λόγια.** Ταξινόμησε τις εργασίες ώστε η προθεσμία να αυξάνει. Μετά τρέξε τες με αυτή τη σειρά, η μία αμέσως μετά την άλλη — η μηχανή δεν σταματά ποτέ.

<Pseudocode
  code={`earliest-deadline-first(t, d):
  ταξινόμησε τις εργασίες ώστε d₁ ≤ d₂ ≤ … ≤ dₙ
  χρόνος ← 0
  για j = 1 έως n:
    sⱼ ← χρόνος ;  fⱼ ← χρόνος + tⱼ
    χρόνος ← χρόνος + tⱼ
  επίστρεψε τα διαστήματα [sⱼ, fⱼ]`}
/>

**Πολυπλοκότητα.** Η ταξινόμηση κατά προθεσμία, $O(n \log n)$· η ανάθεση διαστημάτων, $O(n)$. Σύνολο **$O(n \log n)$**.

</Algorithm>

### Δύο παρατηρήσεις-κλειδιά

<Callout type="key">
**Αδρανής χρόνος** (idle time): στιγμές που η μηχανή δεν δουλεύει ενώ υπάρχουν εργασίες σε αναμονή.

- Υπάρχει **πάντα** μια βέλτιστη λύση **χωρίς** αδρανή χρόνο: αν η μηχανή κάθεται, σπρώξε τα πάντα νωρίτερα — καμία καθυστέρηση δεν μεγαλώνει.
- Ο **άπληστος** αλγόριθμος, εξ ορισμού, δεν αφήνει αδρανή χρόνο.
</Callout>

<Callout type="key">
**Αντιστροφή** (inversion): ένα ζεύγος εργασιών $i, j$ με $d_i < d_j$ — η $i$ λήγει νωρίτερα — αλλά η $j$ προγραμματίζεται **πριν** την $i$.

Ο άπληστος αλγόριθμος ταξινομεί ακριβώς κατά προθεσμία, οπότε η λύση του **δεν έχει καμία αντιστροφή**. Αυτό είναι το «δαχτυλικό αποτύπωμά» της.
</Callout>

<Callout type="intuition">
**Λεπτομέρεια που θα χρειαστούμε:** αν ένα χρονοδιάγραμμα χωρίς αδρανή χρόνο έχει έστω **μία** αντιστροφή, τότε έχει και μία αντιστροφή με τις δύο εργασίες **διαδοχικά** προγραμματισμένες. (Πάρε το πιο «κοντινό» αντεστραμμένο ζεύγος — ανάμεσά τους δεν μπορεί να μένει τίποτα εκτός σειράς.)
</Callout>

<div className="graph-canvas">
<svg viewBox="0 0 700 230" className="w-full max-w-3xl" xmlns="http://www.w3.org/2000/svg">
  <style>{`
    .inax { stroke: rgb(var(--border-strong)); stroke-width: 1.5; }
    .inbar { fill: rgb(var(--bg-soft)); stroke: rgb(var(--fg-muted)); stroke-width: 1.7; }
    .ininv { fill: rgb(244 63 94 / 0.28); stroke: rgb(244 63 94); stroke-width: 2.3; }
    .inlbl { font: 700 13px ui-sans-serif, system-ui; fill: rgb(var(--fg)); text-anchor: middle; dominant-baseline: middle; }
    .indl { font: 700 11px ui-sans-serif, system-ui; fill: rgb(var(--accent)); text-anchor: middle; }
    .inann { font: 600 12px ui-sans-serif, system-ui; fill: rgb(var(--fg-muted)); }
    .inarr { stroke: rgb(244 63 94); stroke-width: 2; fill: none; marker-end: url(#in-a); }
  `}</style>
  <defs>
    <marker id="in-a" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="rgb(244 63 94)" />
    </marker>
  </defs>
  {[
    {id:'a',x:60,w:90,inv:false},
    {id:'j',x:150,w:130,inv:true,d:'dⱼ'},
    {id:'i',x:280,w:90,inv:true,d:'dᵢ'},
    {id:'b',x:370,w:110,inv:false},
    {id:'c',x:480,w:140,inv:false},
  ].map((j) => (
    <g key={j.id}>
      <rect x={j.x} y="74" width={j.w} height="34" rx="3" className={j.inv ? 'ininv' : 'inbar'} />
      <text x={j.x + j.w/2} y="91" className="inlbl">{j.id}</text>
      {j.d && <text x={j.x + j.w/2} y="64" className="indl">{j.d}</text>}
    </g>
  ))}
  <line x1="60" y1="118" x2="620" y2="118" className="inax" />
  <path d="M 245 50 q 35 -28 70 0" className="inarr" />
  <text x="280" y="28" textAnchor="middle" className="inann">ΑΝΤΙΣΤΡΟΦΗ: dᵢ &lt; dⱼ, αλλά η j είναι πριν την i</text>
  <text x="340" y="158" textAnchor="middle" className="inann">Το επιχείρημα ανταλλαγής αντιμεταθέτει τις διαδοχικές i, j…</text>
  <text x="340" y="180" textAnchor="middle" className="inann">…μειώνοντας τις αντιστροφές κατά 1, χωρίς να μεγαλώνει η μέγιστη καθυστέρηση.</text>
</svg>
</div>

### Το επιχείρημα ανταλλαγής

<Callout type="key">
**Ισχυρισμός.** Η αντιμετάθεση δύο **διαδοχικών, αντεστραμμένων** εργασιών μειώνει τον αριθμό των αντιστροφών κατά $1$ και **δεν αυξάνει** τη μέγιστη καθυστέρηση.
</Callout>

**Απόδειξη.** Έστω $i, j$ διαδοχικές με αντιστροφή ($d_i < d_j$, αλλά η $j$ πριν την $i$). Αντιμεταθέτουμέ τες. Συμβολίζουμε με $\ell_k$ τις παλιές καθυστερήσεις και $\ell'_k$ τις νέες.

- Για κάθε $k \ne i, j$: η εργασία $k$ δεν μετακινήθηκε καθόλου, άρα $\ell'_k = \ell_k$.
- Η $i$ μετακινήθηκε **νωρίτερα**, άρα τελειώνει νωρίτερα: $\ell'_i \le \ell_i$.
- Η $j$ μετακινήθηκε αργότερα. Τελειώνει τώρα ακριβώς εκεί όπου τελείωνε **πριν** η $i$ — δηλαδή $f'_j = f_i$. Άρα:

$$
\ell'_j = f'_j - d_j = f_i - d_j \ \le\ f_i - d_i = \ell_i
$$

όπου η ανισότητα ισχύει επειδή $d_j > d_i$.

Άρα **και οι δύο** νέες καθυστερήσεις, $\ell'_i$ και $\ell'_j$, είναι $\le \ell_i$ — δηλαδή $\le$ από κάτι που ήδη μετριόταν στην παλιά μέγιστη. Η μέγιστη καθυστέρηση **δεν αυξήθηκε**. $\blacksquare$

<Callout type="key">
**Θεώρημα.** Η λύση του άπληστου αλγορίθμου είναι **βέλτιστη**.

**Απόδειξη (εις άτοπον).** Έστω $S^*$ μια βέλτιστη λύση χωρίς αδρανή χρόνο, διαλεγμένη με τον **ελάχιστο δυνατό αριθμό αντιστροφών**.

- Αν το $S^*$ **δεν** έχει αντιστροφές, είναι ταξινομημένο κατά προθεσμία — δηλαδή ταυτίζεται με τη λύση του άπληστου. Άρα ο άπληστος είναι βέλτιστος. ✓
- Αν το $S^*$ έχει αντιστροφή, έχει και μία **διαδοχική**. Αντιμεταθέτοντάς την, παίρνουμε λύση με **αυστηρά λιγότερες** αντιστροφές που δεν αυξάνει τη μέγιστη καθυστέρηση — εξίσου βέλτιστη. Αυτό **αντιφάσκει** με το ότι το $S^*$ είχε τις ελάχιστες αντιστροφές. $\blacksquare$
</Callout>

### Οι τρεις τεχνικές απόδειξης άπληστων αλγορίθμων

<Callout type="key">
Μέχρι τώρα έχουμε δει **τρεις** τρόπους να αποδείξεις ότι ένας άπληστος είναι βέλτιστος:

1. **Ο άπληστος προηγείται** (greedy stays ahead) — δείχνεις ότι μετά από κάθε βήμα η μερική του λύση είναι τουλάχιστον τόσο καλή όσο κάθε άλλης. *([L11](/lectures/L11-greedy-i), χρονοπρογραμματισμός διαστημάτων.)*
2. **Επιχείρημα ανταλλαγής** (exchange argument) — μετατρέπεις σταδιακά μια **οποιαδήποτε** βέλτιστη λύση σε αυτή του άπληστου, με μικρές ανταλλαγές που δεν χαλούν τη βελτιστότητα. *(Αυτή η διάλεξη.)*
3. **Δομικό φράγμα** (structural bound) — αποδεικνύεις ένα κάτω/άνω φράγμα μέσα στο πρόβλημα και δείχνεις ότι ο αλγόριθμος το πετυχαίνει ακριβώς. *([L11](/lectures/L11-greedy-i), διαμέριση διαστημάτων — το βάθος.)*
</Callout>

## Τοπολογική διάταξη

### Το πρόβλημα

Πολλές εργασίες με **προαπαιτούμενα**: η εργασία $u$ πρέπει να γίνει **πριν** την $v$. Τα μοντελοποιούμε με ένα **κατευθυνόμενο γράφημα** $G = (V, A)$ — η ακμή $(u, v)$ σημαίνει «η $u$ προηγείται της $v$».

<Callout type="key">
**Τοπολογική διάταξη** του $G$: μια διάταξη των κορυφών $v_1, v_2, \dots, v_n$ τέτοια ώστε **κάθε** ακμή $(v_i, v_j)$ να πηγαίνει «προς τα εμπρός» — δηλαδή $i < j$.
</Callout>

<Callout type="intuition">
**Διαισθητικά:** βάζεις όλες τις εργασίες σε μία γραμμή, έτσι ώστε κάθε βέλος να δείχνει **δεξιά**. Αν τα καταφέρεις, αυτή η σειρά είναι ένα έγκυρο πρόγραμμα εκτέλεσης — κάθε εργασία γίνεται αφού έχουν τελειώσει όλα τα προαπαιτούμενά της.

**Εφαρμογές:** προαπαιτούμενα μαθημάτων, σειρά μεταγλώττισης αρχείων κώδικα, διοχέτευση (pipeline) εργασιών, η σειρά υπολογισμού μιας παράστασης.
</Callout>

### Πότε υπάρχει;

Δεν έχουν **όλα** τα κατευθυνόμενα γραφήματα τοπολογική διάταξη. Αν $a \to b \to c \to a$, ποια μπαίνει πρώτη; Καμία — υπάρχει **κύκλος**.

<div className="graph-canvas">
<svg viewBox="0 0 700 250" className="w-full max-w-3xl" xmlns="http://www.w3.org/2000/svg">
  <style>{`
    .tn { fill: rgb(var(--bg-elevated)); stroke: rgb(var(--accent)); stroke-width: 2; }
    .tnbad { fill: rgb(244 63 94 / 0.18); stroke: rgb(244 63 94); stroke-width: 2.2; }
    .tl { font: 700 14px ui-sans-serif, system-ui; fill: rgb(var(--fg)); text-anchor: middle; dominant-baseline: middle; }
    .te { stroke: rgb(var(--fg-muted)); stroke-width: 1.9; fill: none; marker-end: url(#tg-a); }
    .tebad { stroke: rgb(244 63 94); stroke-width: 2.3; fill: none; marker-end: url(#tg-b); }
    .tann { font: 600 12px ui-sans-serif, system-ui; fill: rgb(var(--fg-muted)); text-anchor: middle; }
    .tord { font: 700 11px ui-sans-serif, system-ui; fill: rgb(var(--accent)); text-anchor: middle; }
  `}</style>
  <defs>
    <marker id="tg-a" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6.5" markerHeight="6.5" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="rgb(var(--fg-muted))" />
    </marker>
    <marker id="tg-b" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6.5" markerHeight="6.5" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="rgb(244 63 94)" />
    </marker>
  </defs>
  <line x1="86" y1="80" x2="170" y2="56" className="te" />
  <line x1="86" y1="80" x2="170" y2="128" className="te" />
  <line x1="200" y1="56" x2="284" y2="80" className="te" />
  <line x1="200" y1="128" x2="284" y2="92" className="te" />
  <line x1="200" y1="128" x2="370" y2="138" className="te" />
  <line x1="314" y1="84" x2="370" y2="120" className="te" />
  {[['a',70,80],['b',186,56],['c',186,128],['d',300,84],['e',386,128]].map(([id,x,y],n) => (
    <g key={id}>
      <circle cx={x} cy={y} r="22" className="tn" />
      <text x={x} y={y} className="tl">{id}</text>
      <text x={x} y={y + 36} className="tord">{n+1}η</text>
    </g>
  ))}
  <text x="228" y="200" className="tann">DAG — τοπολογική διάταξη: a, b, c, d, e (κάθε βέλος δείχνει δεξιά)</text>
  <line x1="556" y1="58" x2="606" y2="104" className="tebad" />
  <line x1="600" y1="128" x2="528" y2="128" className="tebad" />
  <line x1="514" y1="104" x2="544" y2="60" className="tebad" />
  {[['x',548,46],['y',616,118],['z',506,118]].map(([id,x,y]) => (
    <g key={id}>
      <circle cx={x} cy={y} r="22" className="tnbad" />
      <text x={x} y={y} className="tl">{id}</text>
    </g>
  ))}
  <text x="560" y="200" className="tann">Κύκλος x→y→z→x</text>
  <text x="560" y="220" className="tann">— καμία τοπολογική διάταξη</text>
</svg>
</div>

<Callout type="key">
**Θεώρημα.** Ένα κατευθυνόμενο γράφημα έχει τοπολογική διάταξη **αν και μόνο αν** είναι **άκυκλο** — ένα DAG (Directed Acyclic Graph).
</Callout>

**Μέρος 1 — αν έχει τοπολογική διάταξη, τότε είναι DAG.** Έστω ότι το $G$ έχει τοπολογική διάταξη $v_1, \dots, v_n$ *και* έναν κύκλο $C$. Έστω $v_i$ η κορυφή του $C$ με τον **μικρότερο** δείκτη, και $v_j$ η κορυφή ακριβώς **πριν** την $v_i$ μέσα στον $C$. Τότε η $(v_j, v_i)$ είναι ακμή. Από την επιλογή της $v_i$ ως μικρότερης, $i < j$· αλλά μια ακμή σε τοπολογική διάταξη πάει εμπρός, άρα $j < i$. **Άτοπο.** $\blacksquare$

**Μέρος 2 — κάθε DAG έχει κορυφή χωρίς εισερχόμενες ακμές.** Έστω ότι **κάθε** κορυφή έχει εισερχόμενη ακμή. Ξεκίνα από μια κορυφή και ακολούθα ακμές **προς τα πίσω**. Με $n$ κορυφές, μετά από $n$ βήματα θα συναντήσεις κάποια κορυφή **δύο φορές** — και το κομμάτι ανάμεσα στις δύο επισκέψεις είναι **κύκλος**. Άτοπο, αφού το $G$ είναι DAG. $\blacksquare$

**Μέρος 3 — κάθε DAG έχει τοπολογική διάταξη.** Επαγωγή στο πλήθος κορυφών. Βάση $n=1$: προφανές. Βήμα: σε ένα DAG με $k+1$ κορυφές, από το Μέρος 2 υπάρχει κορυφή $v$ χωρίς εισερχόμενες ακμές. Το $G \setminus \{v\}$ είναι DAG με $k$ κορυφές, άρα (επαγωγική υπόθεση) έχει τοπολογική διάταξη. Τοποθετούμε την $v$ **πρώτη** — αφού δεν έχει εισερχόμενες ακμές, κάθε ακμή της δείχνει εμπρός. $\blacksquare$

### Ο αλγόριθμος

Η απόδειξη του Μέρους 3 **είναι** ο αλγόριθμος: βγάζε επανειλημμένα μια κορυφή χωρίς εισερχόμενες ακμές.

<Algorithm
  name="Τοπολογική διάταξη"
  idea="Βγάλε επανειλημμένα μια κορυφή με εσώβαθμο 0 (μια «πηγή»)· διατήρησε τους εσώβαθμους σε μετρητές για να μην ψάχνεις κάθε φορά."
  complexity="O(m + n)"
  io={{
    input: 'κατευθυνόμενο γράφημα G',
    output: 'μια τοπολογική διάταξη — ή ένδειξη ότι το G έχει κύκλο',
  }}
>

**Με λόγια.** Υπολόγισε τον **εσώβαθμο** (πλήθος εισερχόμενων ακμών) κάθε κορυφής, και κράτα σε ένα σύνολο τις «πηγές» — όσες έχουν εσώβαθμο 0. Επαναλαμβανόμενα: βγάλε μια πηγή, βάλ' τη στο τέλος της διάταξης, και για κάθε εξερχόμενη ακμή της μείωσε τον εσώβαθμο του προορισμού — αν φτάσει 0, εκείνη η κορυφή γίνεται νέα πηγή. Αν στο τέλος μείνουν κορυφές που δεν έγιναν ποτέ πηγές, το γράφημα έχει κύκλο.

<Pseudocode
  code={`τοπολογική-διάταξη(G):
  για κάθε κορυφή w:  count[w] ← εσώβαθμος της w
  S ← { κορυφές με count = 0 }            // οι πηγές
  L ← κενή λίστα
  όσο το S δεν είναι άδειο:
    βγάλε μια κορυφή v από το S
    πρόσθεσε την v στο τέλος της L
    για κάθε ακμή (v, w):
      count[w] ← count[w] − 1
      αν count[w] = 0:  πρόσθεσε την w στο S
  επίστρεψε L`}
  caption="Αρχικοποίηση O(m + n)· κάθε ακμή εξετάζεται μία φορά στις μειώσεις count → συνολικά O(m + n). Η αφελής «ψάξε πηγή από την αρχή» θα έδινε O(n²)."
/>

</Algorithm>

Δες τον αλγόριθμο να τρέχει. Πρόσεξε τους εσώβαθμους να πέφτουν, και κάθε φορά που ένας φτάνει στο 0 να εμφανίζεται μια **νέα πηγή**:

<TopologicalSortViz />

## Κλείδωσε τη γνώση

Βάλε στη σειρά τα βήματα της τοπολογικής διάταξης:

<ReorderDrill
  prompt="Ο αλγόριθμος τοπολογικής διάταξης, από την πρώτη ενέργεια στην τελευταία."
  steps={[
    { id: 't1', text: 'Υπολόγισε τον εσώβαθμο κάθε κορυφής.' },
    { id: 't2', text: 'Βάλε σε ένα σύνολο όλες τις κορυφές με εσώβαθμο 0 — τις πηγές.' },
    { id: 't3', text: 'Βγάλε μια πηγή και τοποθέτησέ τη στο τέλος της τοπολογικής διάταξης.' },
    { id: 't4', text: 'Για κάθε εξερχόμενη ακμή της, μείωσε τον εσώβαθμο του προορισμού.' },
    { id: 't5', text: 'Αν κάποιος εσώβαθμος έπεσε στο 0, βάλε εκείνη την κορυφή στο σύνολο πηγών.' },
    { id: 't6', text: 'Επανάλαβε ώσπου να μπουν όλες οι κορυφές στη διάταξη.' },
  ]}
/>

Και συμπλήρωσε τα κενά:

<ClozeDrill
  prompt="Τα δύο μισά της διάλεξης σε τέσσερις λέξεις-κλειδιά."
  segments={[
    { text: 'Ο άπληστος για την ελάχιστη μέγιστη καθυστέρηση ταξινομεί τις εργασίες κατά ' },
    { blank: 'προθεσμία', hint: 'dⱼ' },
    { text: '. Η απόδειξη ορθότητάς του χρησιμοποιεί επιχείρημα ' },
    { blank: 'ανταλλαγής', hint: 'exchange argument' },
    { text: ': αντιμεταθέτει διαδοχικές αντιστροφές χωρίς να μεγαλώσει η μέγιστη καθυστέρηση. Η τοπολογική διάταξη υπάρχει αν και μόνο αν το γράφημα είναι ' },
    { blank: 'DAG', hint: 'άκυκλο' },
    { text: ', και ο αλγόριθμος βγάζει επανειλημμένα μια κορυφή με εσώβαθμο ' },
    { blank: '0', hint: 'τι τιμή;' },
    { text: '.' },
  ]}
/>

## Μοτίβο σκέψης

<ThinkingPattern
  title="Τρεις τρόποι να αποδείξεις έναν άπληστο"
  signals={['απόδειξε ότι ο άπληστος είναι βέλτιστος', 'γιατί δουλεύει αυτός ο κανόνας;']}
>
Έχεις έναν άπληστο κανόνα που πέρασε τον έλεγχο των αντιπαραδειγμάτων. Για την απόδειξη ορθότητας, δοκίμασε με τη σειρά:

1. **Ο άπληστος προηγείται.** Δείξε ότι μετά από κάθε βήμα η μερική του λύση είναι τουλάχιστον τόσο καλή όσο κάθε άλλης. Βολεύει όταν χτίζεις λύση **στοιχείο-στοιχείο** ([L11](/lectures/L11-greedy-i), διαστήματα).
2. **Επιχείρημα ανταλλαγής.** Πάρε μια οποιαδήποτε βέλτιστη λύση και μετάτρεψέ τη σταδιακά σε αυτή του άπληστου με μικρές ανταλλαγές, καμία από τις οποίες δεν τη χειροτερεύει. Βολεύει όταν η λύση είναι μια **διάταξη / σειρά** (εδώ, η σειρά εκτέλεσης).
3. **Δομικό φράγμα.** Βρες ένα κάτω ή άνω φράγμα μέσα στο πρόβλημα και δείξε ότι ο άπληστος το πιάνει ([L11](/lectures/L11-greedy-i), βάθος).

Συχνά παραπάνω από μία τεχνική δουλεύει — διάλεξε αυτή που γράφεται πιο καθαρά.
</ThinkingPattern>

<ExamRadar
  items={[
    {
      topic: 'Απόδειξη με επιχείρημα ανταλλαγής ότι ένας άπληστος αλγόριθμος είναι βέλτιστος',
      likelihood: 'high',
    },
    {
      topic: 'Earliest Deadline First — τρέξε τον και υπολόγισε τη μέγιστη καθυστέρηση',
      likelihood: 'high',
    },
    {
      topic: 'Τοπολογική διάταξη δοσμένου DAG — δώσε μία έγκυρη σειρά',
      likelihood: 'high',
    },
    {
      topic: 'Απόδειξη ότι τοπολογική διάταξη υπάρχει ⇔ το γράφημα είναι DAG',
      likelihood: 'medium',
    },
    {
      topic: 'Αναγνώρισε ποια από τις τρεις τεχνικές απόδειξης ταιριάζει σε ένα greedy πρόβλημα',
      likelihood: 'medium',
    },
  ]}
/>

<Recap>
**Τι κρατάμε από αυτή τη διάλεξη**

1. **Ελάχιστη μέγιστη καθυστέρηση** — μία μηχανή, εργασίες με $(t_j, d_j)$, καθυστέρηση $\ell_j = \max\{0, f_j - d_j\}$. Σωστό κριτήριο: **Earliest Deadline First** (αύξουσα προθεσμία).
2. **Αντιστροφή** = ζεύγος εργασιών εκτός σειράς προθεσμίας· η λύση του άπληστου δεν έχει καμία.
3. **Επιχείρημα ανταλλαγής** — η αντιμετάθεση μιας διαδοχικής αντιστροφής μειώνει τις αντιστροφές κατά $1$ χωρίς να μεγαλώνει το $L$. Άρα κάθε βέλτιστη λύση μετατρέπεται στη λύση του άπληστου.
4. **Τρεις τεχνικές απόδειξης:** greedy stays ahead, exchange argument, structural bound.
5. **Τοπολογική διάταξη** — διάταξη κορυφών ώστε κάθε ακμή να δείχνει εμπρός. Υπάρχει **αν και μόνο αν** το γράφημα είναι **DAG**.
6. Αλγόριθμος: βγάζε επανειλημμένα κορυφή με εσώβαθμο 0 — $O(m + n)$ με μετρητές `count[]` και σύνολο πηγών.
</Recap>

<NextUp current="lectures/L12-greedy-ii" />

<LectureExercises lectureSlug="lectures/L12-greedy-ii" />
