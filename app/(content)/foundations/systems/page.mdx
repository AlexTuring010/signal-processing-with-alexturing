export const metadata = {
  title: 'Συστήματα — Συνέλιξη και η Eigenfunction Property',
  description:
    'Τι είναι ένα σύστημα, γιατί νοιαζόμαστε για LTI, η κρουστική απόκριση h(t), η συνέλιξη βήμα-βήμα και η ιδιότητα που κάνει τα complex exponentials ξεχωριστά.',
}

export const frontmatter = {
  title: 'Συστήματα — Συνέλιξη και η Eigenfunction Property',
  slug: 'foundations/systems',
  order: 3,
  prerequisites: ['intro', 'foundations/signals'],
  examWeight: 12,
  estimatedReadTime: 40,
  lastUpdated: '2026-05-04',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'
import Link from 'next/link'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Foundations · Section 2</span>
  <span>·</span>
  <span>~40 min read</span>
</div>

# Συστήματα

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="foundations/systems" />
  <Bookmark id="foundations/systems" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

Στο [προηγούμενο κεφάλαιο](/foundations/signals) μάθαμε να αναγνωρίζουμε σήματα. Σε αυτό μαθαίνουμε **τι τους κάνουμε**: ένα σύστημα παίρνει σήμα είσοδο και βγάζει σήμα έξοδο. Στόχος μέχρι το τέλος: να καταλάβεις βαθιά μία πράξη — τη **συνέλιξη** — και ένα θεώρημα — ότι τα **complex exponentials** περνούν αναλλοίωτα από κάθε LTI σύστημα. Αυτά τα δύο είναι το «κλειδί» για όλη τη Fourier θεωρία που έρχεται.

## Τι είναι ένα σύστημα;

Στην πιο απλή του μορφή:

> *Ένα σύστημα είναι ένα κουτί που παίρνει στην είσοδο ένα σήμα και βγάζει στην έξοδο ένα άλλο σήμα.*

<SystemBoxDiagram />

Συμβολικά γράφουμε $y(t) = S\{x(t)\}$ — «το σύστημα $S$, με είσοδο $x(t)$, παράγει $y(t)$».

<Callout type="intuition">
  Παραδείγματα από τη ζωή:

  - **Μικρόφωνο:** ηχητική πίεση μπαίνει, τάση βγαίνει.
  - **Ενισχυτής:** μικρή τάση μπαίνει, μεγαλύτερη βγαίνει.
  - **Φίλτρο LP:** θορυβημένο σήμα μπαίνει, εξομαλυσμένο βγαίνει (έφυγαν οι υψηλές).
  - **Wireless κανάλι:** σήμα μπαίνει, εξασθενημένο + καθυστερημένο + θορυβημένο βγαίνει.
  - **Το αυτί σου:** πίεση αέρα μπαίνει, νευρικά ερεθίσματα βγαίνουν.

  Το «κουτί» μπορεί να είναι φυσικό (κύκλωμα, μηχανικός μετατροπέας) ή μαθηματικό (αλγόριθμος, εξίσωση που ορίζει τη μετασχηματιστική).
</Callout>

## Properties που μας ενδιαφέρουν

Από όλα τα πιθανά συστήματα στον κόσμο, υπάρχουν **δύο ιδιότητες** που τα κάνουν ξαφνικά τρομερά εργαλεία: γραμμικότητα και χρονική αμεταβλητότητα. Τα συστήματα που έχουν και τις δύο λέγονται **LTI** (Linear Time-Invariant). Όλο το υπόλοιπο μάθημα στηρίζεται σε αυτά.

### Γραμμικότητα

Ένα σύστημα είναι **γραμμικό** όταν ικανοποιεί την **αρχή της υπέρθεσης**:

$$
S\{a_1\,x_1(t) + a_2\,x_2(t)\} = a_1\,S\{x_1(t)\} + a_2\,S\{x_2(t)\}
$$

Σε απλά Ελληνικά: *«αν δώσεις στο σύστημα ένα **άθροισμα** σημάτων, η έξοδος είναι το **άθροισμα των εξόδων** που θα έπαιρνες αν τα έδινες ένα-ένα.»*

<LinearityChecker />

<Callout type="warning" title="Παγίδα — το «+3» σπάει τη γραμμικότητα">
  Πολλοί φοιτητές θεωρούν γραμμικά τα συστήματα της μορφής $y = a x + b$ επειδή είναι «γραμμή» στη γραφική παράσταση. **Λάθος.** Όταν το σταθερό offset $b \neq 0$, τότε για μηδενική είσοδο παίρνεις $b$ — όχι $0$. Αυτό σπάει την υπέρθεση.

  Τσέκαρε με το viz: το $y = 5x + 3$ βγάζει διαφορετικές εξόδους στις δύο πλευρές της εξίσωσης υπέρθεσης.
</Callout>

### Χρονική αμεταβλητότητα

Ένα σύστημα είναι **χρονικά αμετάβλητο** (time-invariant) όταν, αν καθυστερήσεις την είσοδο, η έξοδος είναι ίδια — απλώς καθυστερημένη κατά το ίδιο:

$$
\text{αν}\quad y(t) = S\{x(t)\},\qquad\text{τότε}\quad S\{x(t - t_0)\} = y(t - t_0)
$$

Σε απλά Ελληνικά: *«το σύστημα συμπεριφέρεται το ίδιο όποτε κι αν του δώσεις το σήμα. Η σχέση input→output δεν εξαρτάται από τον χρόνο.»*

Αν πεις «γεια» στο μικρόφωνο στις 10:00 ή στις 14:00, η ηχογράφηση ακούγεται ίδια — απλώς κάθεται σε άλλο σημείο του χρόνου. Αυτό είναι TI. Αντίθετα, ένα σύστημα όπως $y(t) = t \cdot x(t)$ πολλαπλασιάζει το σήμα με τον τρέχοντα χρόνο — όσο αργότερα δίνεις την είσοδο, τόσο μεγαλύτερη είναι η έξοδος. Όχι TI.

<TimeInvarianceChecker />

### LTI = ΓΧΑ

Ένα σύστημα που είναι **και** γραμμικό **και** χρονικά αμετάβλητο λέγεται **LTI** (Linear Time-Invariant). Στα Ελληνικά: **ΓΧΑ** (Γραμμικό Χρονικά Αμετάβλητο). Θα συναντήσεις και τους δύο όρους — εμείς θα χρησιμοποιούμε **LTI** σαν default.

<Callout type="key" title="Γιατί κολλάμε στο LTI">
  Σχεδόν **όλα** τα ισχυρά εργαλεία αυτού του μαθήματος — convolution, frequency response, Fourier, transfer functions — δουλεύουν **μόνο** για LTI συστήματα. Για μη-LTI, καμία από αυτές τις δουλειές δεν εφαρμόζεται απευθείας.

  Ευτυχώς, η συντριπτική πλειονότητα των φυσικών συστημάτων που μας ενδιαφέρουν είναι LTI σε καλή προσέγγιση: γραμμικά κυκλώματα, ιδανικά κανάλια, βασικά φίλτρα, διάδοση κύματος σε ομογενή μέσα… Όταν, λοιπόν, λέμε σε αυτό το μάθημα «σύστημα», εννοούμε «LTI σύστημα» — εκτός αν πούμε ρητά κάτι άλλο.

  **Στις εξετάσεις είναι πολύ συχνή ερώτηση** το «δείξτε αν αυτό το σύστημα είναι LTI». Δες προσεκτικά τα δύο vizzes παραπάνω.
</Callout>

### Άλλες ιδιότητες σύντομα

Δύο ακόμη ιδιότητες με τις οποίες θα ξαναβρεθούμε:

- **Αιτιατό (causal):** η έξοδος τη χρονική στιγμή $t$ εξαρτάται μόνο από τιμές της εισόδου σε χρόνους $\leq t$. Όλα τα φυσικά συστήματα πραγματικού χρόνου είναι αιτιατά — δεν μπορούν να «δουν το μέλλον».
- **Ευσταθές (BIBO stable):** σε κάθε **φραγμένη** είσοδο δίνει **φραγμένη** έξοδο (Bounded Input → Bounded Output). Στα μαθηματικά συμβαίνει στα LTI όταν η h(t) είναι απολύτως ολοκληρώσιμη.

Οι περισσότερες πρακτικές εφαρμογές απαιτούν αυτές τις ιδιότητες, αλλά για τη θεωρία θα τα προϋποθέτουμε σιωπηρά.

## Η κρουστική απόκριση h(t)

Πώς περιγράφουμε τη συμπεριφορά ενός LTI με **έναν** αντικείμενο, αντί να το δοκιμάζουμε με κάθε δυνατή είσοδο;

Η ιδέα: σου δίνω **ένα μόνο** σήμα — ένα που περιέχει «λίγο από κάθε συχνότητα και κάθε διάρκεια» — και βλέπω τι βγαίνει. Αυτό το σήμα είναι το $\delta(t)$.

> *Η έξοδος ενός LTI συστήματος όταν η είσοδος είναι $\delta(t)$ ονομάζεται **κρουστική απόκριση** $h(t)$:*
>
> $$ h(t) = S\{\delta(t)\} $$

<Callout type="intuition">
  Ίδια ιδέα, διαφορετική αναλογία: αν χτυπήσεις μια καμπάνα με ένα πανηγυρικό «ταπ», ο ήχος που ακούς **είναι** το $h(t)$ της καμπάνας. Αν θέλεις να μάθεις πώς θα ακουγόταν αν την έπαιζες με σφυράκι ή με μαλακιά μπαγκέτα, αρκεί να ξέρεις το $h(t)$ — μετά μπορείς να υπολογίσεις την έξοδο για **οποιαδήποτε** διέγερση.

  Στο [προηγούμενο κεφάλαιο](/foundations/signals) είπαμε ότι το $\delta(t)$ είναι «το test signal». Τώρα είδες γιατί.
</Callout>

<ImpulseResponseDemo />

### Γιατί η h(t) περιγράφει ΠΛΗΡΩΣ ένα LTI

Θα δείξουμε ότι, αν ξέρουμε την $h(t)$, μπορούμε να υπολογίσουμε την έξοδο για **οποιαδήποτε** είσοδο $x(t)$. Η μόνη απαίτηση: το σύστημα να είναι LTI. Σε **τέσσερα βήματα**:

1. **Σαρωτική ιδιότητα της δ.** Μπορούμε να γράψουμε την είσοδο $x(t)$ ως:

   $$
   x(t) = \int_{-\infty}^{\infty} \textcolor{#2563eb}{x(\tau)}\,\textcolor{#d97706}{\delta(t - \tau)}\,d\tau
   $$

   *Γιατί;* Η $\delta(t-\tau)$ είναι μη-μηδενική μόνο όταν $\tau = t$. Σε κάθε άλλο $\tau$, ο integrand είναι 0 και δεν συνεισφέρει. Άρα το ολοκλήρωμα «διαλέγει» μόνο την τιμή του $x$ στη θέση $\tau = t$, που είναι το $x(t)$.

2. **Time-invariance.** *(Σύμβαση: από εδώ και κάτω γράφουμε **είσοδος → έξοδος** για να δηλώσουμε «αυτή η είσοδος δίνει αυτή την έξοδο όταν περάσει από το σύστημα».)*

   Μια κρούση στο 0 δίνει $h(t)$ από τον ορισμό της κρουστικής απόκρισης. Από TI, μια κρούση στο $\tau$ δίνει το ίδιο, απλώς ολισθημένο:

   $$
   \textcolor{#d97706}{\delta(t - \tau)} \;\longrightarrow\; \textcolor{#059669}{h(t - \tau)}
   $$

3. **Linearity — ομογένεια.** Αν η κρούση $\delta(t - \tau)$ δίνει $h(t - \tau)$, τότε μια κρούση **scaled κατά $x(\tau)$** στην είσοδο πρέπει να δίνει το ίδιο scaling στην έξοδο:

   $$
   \underbrace{\textcolor{#2563eb}{x(\tau)}\,\textcolor{#d97706}{\delta(t - \tau)}}_{\text{«κομμάτι» εισόδου}} \;\longrightarrow\; \underbrace{\textcolor{#2563eb}{x(\tau)}\,\textcolor{#059669}{h(t - \tau)}}_{\text{«κομμάτι» εξόδου}}
   $$

   Πρόσεξε ότι το $\textcolor{#2563eb}{x(\tau)}$ είναι **ίδιο** και στις δύο πλευρές. **Η μόνη αλλαγή** είναι το $\textcolor{#d97706}{\delta(t-\tau)}$ έγινε $\textcolor{#059669}{h(t-\tau)}$ — δηλαδή η δ έδωσε τη θέση της στην h.

4. **Linearity — προσθετικότητα.** Από Βήμα 1, η είσοδος είναι ένα **ολοκλήρωμα** — ένα «άπειρο άθροισμα» — από κομμάτια της μορφής $x(\tau)\,\delta(t-\tau)$. Η **προσθετικότητα** της γραμμικότητας μάς λέει ότι αν η είσοδος είναι άθροισμα κομματιών, η έξοδος είναι το άθροισμα των αντίστοιχων εξόδων. Αυτό ισχύει και για ολοκληρώματα (που είναι «συνεχή αθροίσματα»). Από Βήμα 3 ξέρουμε σε τι μετατρέπεται κάθε κομμάτι: το $x(\tau)\,\delta(t-\tau)$ γίνεται $x(\tau)\,h(t-\tau)$. Άρα η συνολική έξοδος είναι:

$$
y(t) = \int_{-\infty}^{\infty} \textcolor{#2563eb}{x(\tau)}\,\textcolor{#059669}{h(t - \tau)}\,d\tau
$$

Αυτή είναι η **συνέλιξη** $y(t) = x(t) * h(t)$. Δεν την κατεβάσαμε από τον ουρανό — προέκυψε **αναγκαστικά** από τις τρεις ιδιότητες που ήδη ξέραμε.

<Callout type="key">
  Η συνέλιξη δεν είναι μαγική. Είναι αυτό που προκύπτει αν συνδυάσεις: **(1)** ότι κάθε σήμα είναι σύνθεση κρούσεων, **(2)** ότι το σύστημα είναι TI, **(3)** ότι το σύστημα είναι γραμμικό. Άλλαξε ένα από αυτά τα τρία και η συνέλιξη χάνει νόημα.
</Callout>

## Συνέλιξη

### Ο τύπος, εξηγημένος όρος-προς-όρο

$$
y(t) \;=\; x(t) * h(t) \;=\; \int_{-\infty}^{\infty} x(\tau)\,h(t - \tau)\,d\tau
$$

- **$\tau$** — η μεταβλητή ολοκλήρωσης. Όχι ο χρόνος της εξόδου, ούτε ο χρόνος της εισόδου. Είναι **βοηθητική**.
- **$x(\tau)$** — η τιμή της εισόδου τη χρονική στιγμή $\tau$.
- **$h(t - \tau)$** — η κρουστική απόκριση **αναποδογυρισμένη** (επειδή είναι $t - \tau$ αντί για $\tau - t$) **και μετατοπισμένη δεξιά κατά $t$**. Αυτή είναι η οπτική της επόμενης παραγράφου.
- **Το γινόμενο $x(\tau) \cdot h(t - \tau)$** — αυτό που ολοκληρώνουμε. Δείχνει σε κάθε σημείο $\tau$ «πόσο συμβάλλει» η αντίστοιχη κρούση της εισόδου στην έξοδο τη στιγμή $t$.
- **$y(t)$** — μία τιμή για κάθε $t$. Η ολοκλήρωση συσσωρεύει όλες τις συμβολές για το συγκεκριμένο $t$.

<Callout type="warning" title="Το αστεράκι ⊛ ≠ πολλαπλασιασμός">
  Στη συνέλιξη το σύμβολο $*$ δεν είναι πολλαπλασιασμός. Σε κάποιες πηγές (και σε αυτό το site) γράφεται και ως $\circledast$ ή $\ast$ για να ξεχωρίζει. Συνέλιξη και πολλαπλασιασμός είναι **πολύ διαφορετικές πράξεις** — αλλά (θα δούμε) συνδέονται μέσω Fourier.
</Callout>

### Flip-and-slide — η οπτική του τύπου

Πώς υπολογίζεις $y(t)$ για ένα συγκεκριμένο $t$;

1. Σχεδιάζεις το $x(\tau)$ στον άξονα $\tau$.
2. Παίρνεις το $h(\tau)$, **το αναποδογυρίζεις** οριζόντια για να γίνει $h(-\tau)$.
3. **Ολισθαίνεις** το flipped $h$ δεξιά κατά $t$ ⇒ προκύπτει το $h(t - \tau)$.
4. **Πολλαπλασιάζεις** σε κάθε $\tau$ τις δύο καμπύλες.
5. **Ολοκληρώνεις** το γινόμενο. Αυτό είναι το $y(t)$.
6. Επόμενο $t$ → επανάληψη.

<ConvolutionFlipAndSlide />

Πατώντας Παίξε το $t$ τρέχει, το flipped-and-shifted $h$ ολισθαίνει, το γινόμενο αλλάζει σχήμα και το εμβαδό του χτίζει σταδιακά την καμπύλη $y(t)$ στο κάτω panel. Αυτό **είναι** η συνέλιξη — η αλγεβρική φόρμουλα είναι απλά πιο συμπυκνωμένη γραφή της ίδιας πράξης.

### Worked example: $\Pi(\tau - 0.5) * \Pi(\tau - 0.5)$

Δύο ορθογώνια του ίδιου μήκους που συνελίσσονται. Διαλέγοντας το preset «Δύο ορθογώνια» στο viz βλέπεις τι συμβαίνει βήμα-βήμα. Η αλγεβρική λύση:

<Example title="Λύση βήμα-βήμα">
Έχουμε $x(\tau) = h(\tau) = \Pi(\tau - 0.5)$, δηλαδή $1$ στο διάστημα $[0, 1]$ και $0$ έξω από αυτό.

Το $h(t - \tau)$ είναι η ίδια καμπύλη αλλά flipped + shifted: $1$ για $\tau \in [t - 1, t]$.

Ολοκληρώνουμε το γινόμενο, που είναι $1$ μόνο στην **τομή** $[0, 1] \cap [t - 1, t]$.

- **Για $0 \leq t \leq 1$:** η τομή είναι $[0, t]$, μήκους $t$. Άρα $y(t) = t$.
- **Για $1 \leq t \leq 2$:** η τομή είναι $[t - 1, 1]$, μήκους $2 - t$. Άρα $y(t) = 2 - t$.
- **Αλλιώς:** οι δύο ορθογώνιοι παλμοί δεν επικαλύπτονται καθόλου, $y(t) = 0$.

Αποτέλεσμα: ένα **τρίγωνο** με κορυφή $1$ στο $t = 1$ και βάση $[0, 2]$. Δηλαδή $y(t) = \Lambda(t - 1)$.

Συμπέρασμα: η συνέλιξη δύο ίδιων ορθογωνίων είναι ένα τρίγωνο. Είναι τόσο σημαντική αυτή η σχέση που εμφανίζεται **παντού** — ως απόκριση των απλούστερων φίλτρων, ως μετασχηματισμός Fourier, και στα επόμενα κεφάλαια.
</Example>

## Ιδιότητες της συνέλιξης

Λίγες ταυτότητες, αλλά καθεμία αντιστοιχεί σε φυσική σύνθεση συστημάτων.

### Αντιμεταθετική

$$x(t) * h(t) = h(t) * x(t)$$

*«Δεν έχει σημασία ποιο σήμα παίζει το ρόλο της εισόδου και ποιο το ρόλο του συστήματος.»* Πρακτικά: στην εξεταστή, **flip-and-slide το πιο απλό σήμα** για να βγει πιο εύκολα το ολοκλήρωμα.

### Προσεταιριστική (cascade)

$$\bigl(x_1(t) * x_2(t)\bigr) * x_3(t) = x_1(t) * \bigl(x_2(t) * x_3(t)\bigr)$$

Φυσικό νόημα: αν βάλεις δύο LTI σε **σειρά** (έξοδος του πρώτου = είσοδος του δευτέρου), το συνολικό σύστημα είναι κι αυτό LTI με κρουστική απόκριση $h_1 * h_2$.

<CascadeDiagram />

### Επιμεριστική (parallel)

$$x(t) * \bigl(h_1(t) + h_2(t)\bigr) = x(t) * h_1(t) + x(t) * h_2(t)$$

Φυσικό νόημα: αν τροφοδοτήσεις την ίδια είσοδο σε **δύο LTI παράλληλα** και προσθέσεις τις εξόδους, το συνολικό σύστημα είναι LTI με $h_1 + h_2$.

<ParallelDiagram />

### Ταυτοτική — η δ είναι το «1» της συνέλιξης

$$x(t) * \delta(t) = x(t)$$
$$x(t) * \delta(t - t_0) = x(t - t_0)$$

Συνελίσσοντας με $\delta(t)$ δεν αλλάζει τίποτα. Με $\delta(t - t_0)$ απλώς **καθυστερείς** κατά $t_0$. Συμπέρασμα: ένα σύστημα **καθαρής καθυστέρησης** έχει $h(t) = \delta(t - t_0)$.

<Callout type="intuition" title="Όλες μαζί, σε ένα frame">
  - **Αντιμεταθετική** = οι δύο ρόλοι είναι ανταλλάξιμοι.
  - **Προσεταιριστική** = δύο σε σειρά ⟺ ένα με $h_1 * h_2$.
  - **Επιμεριστική** = δύο παράλληλα ⟺ ένα με $h_1 + h_2$.
  - **Ταυτοτική** = το $\delta$ είναι το «ουδέτερο» στοιχείο — με πραγματική φυσική σημασία (καθυστέρηση).
</Callout>

## Από τη συνέλιξη στη συχνότητα — teaser

Η συνέλιξη στον χρόνο είναι **πολύ φασαριόζικη** — flips, slides, ολοκληρώματα. Υπάρχει όμως ένα **εκπληκτικό αποτέλεσμα**:

<Callout type="note" title="Spoiler από το επόμενο κεφάλαιο">
  Αν αλλάξουμε προοπτική και κοιτάξουμε τα ίδια σήματα στη **συχνότητα** (Fourier transform), η συνέλιξη γίνεται απλό **πολλαπλασιασμό** σημείο-προς-σημείο:

  $$y(t) = x(t) * h(t)\quad\Longleftrightarrow\quad Y(f) = X(f) \cdot H(f)$$

  Αυτή είναι μία από τις βασικότερες αιτίες που το Fourier transform είναι **τόσο** σημαντικός. Δουλεύοντας στο πεδίο της συχνότητας, η πιο επίπονη πράξη γίνεται τετριμμένη.

  Πλήρης απόδειξη και χρήση: <Link href="/foundations/fourier-transform" className="text-accent hover:underline">/foundations/fourier-transform</Link>.
</Callout>

## Eigenfunction property — γιατί τα complex exponentials είναι ξεχωριστά

Στο [προηγούμενο κεφάλαιο](/foundations/signals) είπαμε ότι τα complex exponentials $e^{j\omega t}$ είναι «η μητρική γλώσσα των LTI». Τώρα μπορούμε να το αποδείξουμε.

Πάρε ένα LTI με κρουστική απόκριση $h(t)$ και στείλε στην είσοδο:

$$
x(t) = A\,e^{j(2\pi f_0 t + \phi)}
$$

Από τη συνέλιξη:

$$
y(t) = \int_{-\infty}^{\infty} h(\tau)\,A\,e^{j(2\pi f_0 (t - \tau) + \phi)}\,d\tau
$$

Παρατήρησε ότι το $A\,e^{j(2\pi f_0 t + \phi)}$ **δεν εξαρτάται από το $\tau$** — μπορούμε να το βγάλουμε έξω από το ολοκλήρωμα:

$$
y(t) = A\,e^{j(2\pi f_0 t + \phi)} \cdot \int_{-\infty}^{\infty} h(\tau)\,e^{-j 2\pi f_0 \tau}\,d\tau
$$

Το ολοκλήρωμα στα δεξιά **εξαρτάται μόνο από το $f_0$ και τη συγκεκριμένη $h$** — δεν αλλάζει με το $t$. Είναι ένας μιγαδικός αριθμός. Τον βαφτίζουμε:

$$
H(f_0) \;=\; \int_{-\infty}^{\infty} h(\tau)\,e^{-j 2\pi f_0 \tau}\,d\tau
$$

και έχουμε:

$$
\boxed{\;y(t) = H(f_0)\,\cdot\,x(t)\;}
$$

<Callout type="key" title="Τι μόλις είδαμε">
  Ένα **complex exponential μπαίνει** σε ένα LTI σύστημα. Στην έξοδο **βγαίνει το ίδιο complex exponential**, απλώς **πολλαπλασιασμένο με έναν μιγαδικό αριθμό** $H(f_0)$. Δεν αλλάζει σχήμα, δεν παραμορφώνεται, δεν δημιουργούνται καινούριες συχνότητες — αλλάζει μόνο **πλάτος** και **φάση**.

  Αυτή είναι η ορισμός της **eigenfunction**: αντικείμενο που, όταν περάσει από έναν τελεστή, βγαίνει το ίδιο πολλαπλασιασμένο με σταθερά. Στις matrices είναι τα eigenvectors. Στα LTI συστήματα είναι τα complex exponentials.
</Callout>

Τι σημαίνει αυτό σε πραγματικά (real-valued) σήματα; Επειδή κάθε cosine γράφεται σαν άθροισμα δύο complex exponentials, ισχύει το ίδιο:

$$
\cos(2\pi f_0 t) \,\longrightarrow\, |H(f_0)|\,\cos\bigl(2\pi f_0 t + \angle H(f_0)\bigr)
$$

**Ίδια συχνότητα μέσα και έξω.** Αλλάζει μόνο πλάτος ($|H(f_0)|$) και φάση ($\angle H(f_0)$). Αυτό αξίζει να το πεις δυνατά: **ένα LTI σύστημα δεν δημιουργεί ποτέ νέες συχνότητες**. Αν βάλεις 100 Hz μέσα, παίρνεις 100 Hz έξω. Πάντα. Αν βγαίνουν νέες συχνότητες — κάποιο modulator, π.χ. — το σύστημα **δεν είναι** LTI.

<EigenfunctionDemo />

Σύρε το $f_0$. Παρατήρησε ότι:
- Η **έξοδος (πράσινη)** έχει **πάντα την ίδια συχνότητα** με την είσοδο.
- Αλλάζουν μόνο το πλάτος ($|H(f_0)|$) και η φάση ($\angle H(f_0)$).
- Οι πράσινες κουκκίδες δεξιά «ζωγραφίζουν» την καμπύλη $|H(f)|$ και $\angle H(f)$ — αυτά **είναι** το frequency response του συστήματος.

<Callout type="note" title="Spoiler #2 από το επόμενο κεφάλαιο">
  Η εξίσωση που μόλις γράψαμε για το $H(f_0)$:
  $$ H(f) = \int_{-\infty}^{\infty} h(\tau)\,e^{-j 2\pi f \tau}\,d\tau $$
  είναι **ακριβώς** ο ορισμός του Fourier transform της $h(t)$. Δεν είναι σύμπτωση. Στο επόμενο κεφάλαιο θα δούμε γιατί κάθε σήμα μπορεί να αναλυθεί σε complex exponentials, και άρα γιατί η eigenfunction property είναι ο πραγματικός λόγος που υπάρχει ο μετασχηματισμός Fourier.

  Πλήρης κουβέντα: <Link href="/foundations/fourier-transform" className="text-accent hover:underline">/foundations/fourier-transform</Link>.
</Callout>

## 🧪 Lab — Ελέγχοντας γραμμικότητα και TI στο MATLAB

<LabBox title="Lab 3 — Γραμμικά συστήματα συνεχούς χρόνου (Προαιρετικό)">
Στο εργαστήριο εξετάζουμε αν συγκεκριμένα συστήματα είναι γραμμικά και TI με δύο τρόπους: **αλγεβρικά** (με χαρτί και μολύβι) και **προγραμματιστικά** (μετράμε μέγιστο σφάλμα).

```matlab
% Συμβατικός αριθμητικός έλεγχος γραμμικότητας
t = 0:0.001:2;
x1 = cos(2*pi*5*t);
x2 = exp(-0.5*t);
a1 = 2; a2 = 3;

% Συστημα: y = 5x  (linear)
y_combined = 5*(a1*x1 + a2*x2);
y_separate = a1*(5*x1) + a2*(5*x2);
fprintf('Linear?  max|Δ| = %.3e\n', max(abs(y_combined - y_separate)));

% Συστημα: y = 5x + 3  (NOT linear)
y_combined_aff = 5*(a1*x1 + a2*x2) + 3;
y_separate_aff = a1*(5*x1 + 3) + a2*(5*x2 + 3);
fprintf('Affine?  max|Δ| = %.3e\n', max(abs(y_combined_aff - y_separate_aff)));
```

Το πρώτο τυπώνει σχεδόν 0. Το δεύτερο τυπώνει κάτι **μεγάλο** — έτσι «πιάνεις» αλγοριθμικά τη μη γραμμικότητα.

Πλήρης οδηγός με ελέγχους TI και convolution: <Link href="/labs/03-systems" className="text-accent hover:underline">/labs/03-systems</Link> (🚧 σύντομα).
</LabBox>

## Εξάσκηση

<ExamProblem year="Linearity" weight="τυπικό">
**Ερώτηση:** Είναι το σύστημα $y(t) = x(t - 1) + x(t + 1)$ γραμμικό;

<Example title="Λύση" defaultOpen={true}>
Δοκιμάζουμε υπέρθεση. Έστω $x = a_1 x_1 + a_2 x_2$. Τότε:
$$y(t) = (a_1 x_1(t-1) + a_2 x_2(t-1)) + (a_1 x_1(t+1) + a_2 x_2(t+1))$$
Ομαδοποιώντας:
$$= a_1 \bigl(x_1(t-1) + x_1(t+1)\bigr) + a_2 \bigl(x_2(t-1) + x_2(t+1)\bigr) = a_1 y_1(t) + a_2 y_2(t)$$
**Ναι**, είναι γραμμικό. Άθροισμα ολισθήσεων του εισόδου είναι πάντα γραμμικό.
</Example>
</ExamProblem>

<ExamProblem year="Time-invariance" weight="τυπικό">
**Ερώτηση:** Είναι το σύστημα $y(t) = t \cdot x(t)$ TI;

<Example title="Λύση" defaultOpen={true}>
Έστω $z(t) = x(t - t_0)$ μια καθυστερημένη εκδοχή. Η έξοδος του συστήματος για το $z$ είναι:
$$S\{z(t)\} = t \cdot z(t) = t \cdot x(t - t_0)$$
Από την άλλη, αν ήταν TI θα έπρεπε να ισχύει:
$$S\{z(t)\} = y(t - t_0) = (t - t_0)\,x(t - t_0)$$
Οι δύο διαφέρουν κατά τον όρο $t_0\,x(t - t_0)$, άρα **όχι**, δεν είναι TI. Ο συντελεστής $t$ εξαρτάται από τον χρόνο.
</Example>
</ExamProblem>

<ExamProblem year="Convolution" weight="κλασικό">
**Ερώτηση:** Υπολόγισε τη συνέλιξη $\Pi(t/2) * \Pi(t/2)$, όπου $\Pi(t/2)$ είναι ορθογώνιος παλμός πλάτους 2 ($-1 \leq t \leq 1$).

<Example title="Λύση">
Δύο όμοια ορθογώνια πλάτους 2 ⇒ τρίγωνο πλάτους 4 και ύψους 2.

Φόρμουλα:
$$y(t) = \int_{-\infty}^{\infty} \Pi(\tau/2)\,\Pi((t-\tau)/2)\,d\tau$$

Το γινόμενο είναι $1$ μόνο στην τομή $[-1, 1] \cap [t - 1, t + 1]$.

- Αν $|t| \leq 2$: μήκος τομής $= 2 - |t|$, άρα $y(t) = 2 - |t|$.
- Αν $|t| > 2$: τα δύο ορθογώνια δεν επικαλύπτονται, $y(t) = 0$.

Συνολικά: $y(t) = 2 \cdot \Lambda(t/2)$ — τρίγωνο μέγιστου ύψους 2 στο $t = 0$, βάσης 4.
</Example>
</ExamProblem>

<ExamProblem year="Convolution με δ" weight="γρήγορο">
**Ερώτηση:** Υπολόγισε $x(t) * \delta(t - 3)$, όπου $x(t) = e^{-t}\,u(t)$.

<Example title="Λύση" defaultOpen={true}>
Από την ταυτοτική ιδιότητα ($x * \delta(t - t_0) = x(t - t_0)$):
$$x(t) * \delta(t - 3) = x(t - 3) = e^{-(t-3)}\,u(t - 3)$$
Δηλαδή το αρχικό σήμα μετατοπισμένο **κατά 3 δευτερόλεπτα δεξιά**. Η συνέλιξη με $\delta$ είναι ένας απλός «delay-er».
</Example>
</ExamProblem>

<ExamProblem year="Eigenfunction" weight="τυπικό">
**Ερώτηση:** Ένα LTI σύστημα δέχεται $x(t) = \cos(2\pi \cdot 10\,t)$ και βγάζει $y(t) = 0.5\,\cos(2\pi \cdot 10\,t - \pi/4)$. Πόσο είναι το $|H(10)|$ και το $\angle H(10)$;

<Example title="Λύση" defaultOpen={true}>
Από την eigenfunction property, ένα cosine στην είσοδο βγαίνει cosine **ίδιας συχνότητας** στην έξοδο, με νέο πλάτος και φάση:
$$y(t) = |H(f_0)|\,\cos\bigl(2\pi f_0 t + \angle H(f_0)\bigr)$$

Συγκρίνοντας με τη δοθείσα έξοδο:
- $|H(10)| = 0.5$
- $\angle H(10) = -\pi/4$ rad (= $-45°$)

Το σύστημα **εξασθενεί** το σήμα στα 10 Hz κατά παράγοντα 2 και του δίνει μια καθυστέρηση φάσης $-\pi/4$.
</Example>
</ExamProblem>

<Recap>
- Ένα **σύστημα** είναι ένα κουτί που μετατρέπει σήμα σε σήμα. Συμβολισμός: $y = S\{x\}$.
- Δύο ιδιότητες πρωταγωνιστούν: **γραμμικότητα** (υπέρθεση) και **χρονική αμεταβλητότητα** (delay-in ⇒ delay-out). Μαζί δίνουν τα **LTI** (= ΓΧΑ) — η κατηγορία πάνω στην οποία στηρίζονται όλα μας τα εργαλεία.
- Η **κρουστική απόκριση** $h(t) = S\{\delta(t)\}$ περιγράφει **πλήρως** ένα LTI. Συνδυάζοντας τη σαρωτική ιδιότητα της δ + TI + linearity → προκύπτει αναγκαστικά η **συνέλιξη**: $y(t) = x(t) * h(t)$.
- Η συνέλιξη είναι αντιμεταθετική, προσεταιριστική (cascade), επιμεριστική (parallel), και έχει το $\delta(t)$ ως ταυτοτικό στοιχείο.
- **Eigenfunction property:** complex exponentials $e^{j 2\pi f_0 t}$ περνούν αναλλοίωτα από κάθε LTI — βγαίνουν ίδιοι, πολλαπλασιασμένοι με $H(f_0) = \int h(\tau)\,e^{-j 2\pi f_0 \tau}\,d\tau$. Αυτή η εξίσωση είναι ο μετασχηματισμός Fourier της $h$ — έρχεται αναλυτικά αμέσως μετά.
</Recap>

<NextUp slug="foundations/fourier-series" />
