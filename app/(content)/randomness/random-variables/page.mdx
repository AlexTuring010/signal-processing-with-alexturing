export const metadata = {
  title: 'Random variables — γρήγορος οδηγός',
  description:
    'Quick recap πάνω στις τυχαίες μεταβλητές: PDF/CDF, μέσος, variance, κύριες κατανομές (Gaussian, uniform, exponential), independence, joint distributions, LOTUS — και πώς τα εφαρμόζουμε σε τυχαίες διαδικασίες.',
}

export const frontmatter = {
  title: 'Random variables',
  slug: 'randomness/random-variables',
  order: 2,
  prerequisites: ['randomness/why'],
  examWeight: 1,
  estimatedReadTime: 30,
  lastUpdated: '2026-05-25',
}

import { CompleteToggle } from '@/components/layout/CompleteToggle'
import { Bookmark } from '@/components/layout/Bookmark'
import { PrerequisitesBar } from '@/components/layout/PrerequisitesBar'

<div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-wider text-fg-subtle">
  <span>Randomness · 2</span>
  <span>·</span>
  <span>~30 min read</span>
  <span>·</span>
  <span className="font-semibold text-emerald-700 dark:text-emerald-400">🟢 Light exam — recap που πρέπει να ξυπνήσει</span>
</div>

# Random variables — γρήγορος οδηγός

<div className="-mt-1 mb-4 flex flex-wrap items-center gap-2">
  <CompleteToggle slug="randomness/random-variables" />
  <Bookmark id="randomness/random-variables" />
</div>

<PrerequisitesBar prerequisites={frontmatter.prerequisites} />

<SourceDoc
  sources={{
    pdf: 'SE_session9_random1_upload.pdf',
    slides: '8, 10, 14–19',
    label: 'PMF/PDF (slide 8), μέσος ΤΔ με παράμετρο a (slide 10), Άσκηση 1 — ΤΜ-μηχανές σε ΤΔ (slides 14–19)',
  }}
  note="Recap από προηγούμενα μαθήματα (Πιθανότητες & Στατιστική). Στο K21 δεν υπάρχει ξεχωριστή διαφάνεια για ΤΜ — το υλικό εμφανίζεται «εν παρόδω» μέσα στο deck των ΤΔ."
/>

<OptionalResource
  href="/probabilities_book.pdf"
  downloadName="probabilities_book.pdf"
  title="Προαιρετικό extra — επανάληψη στις πιθανότητες"
>
  Αυτή η σελίδα χτίζει ό,τι χρειάζεσαι **από το μηδέν**, οπότε **δεν είναι καθόλου απαραίτητο** να ανοίξεις κάτι άλλο για να την παρακολουθήσεις. Αν όμως έχεις χρόνο και θέλεις μια πιο γενική επανάληψη στις πιθανότητες, εδώ είναι ένα PDF που είχα φτιάξει πριν μερικά χρόνια για το μάθημα Πιθανοτήτων του τμήματος — καθαρά **bonus** υλικό για όποιον ενδιαφέρεται. Για το K21 δεν το χρειάζεσαι.
</OptionalResource>

## 1. Νιώσε

Το έχεις ξανασυναντήσει σε άλλο μάθημα (Πιθανότητες & Στατιστική). **Δεν θα το ξαναδιδάξουμε από την αρχή.** Όμως αν το έχεις ξεχάσει ή δεν το είδες ποτέ καλά, αυτή η σελίδα είναι ο γρήγορος οδηγός: ένα recap **εστιασμένο μόνο σε όσα χρειάζεται το K21**.

Γιατί είναι κρίσιμο τώρα; Γιατί στο επόμενο κεφάλαιο **κάθε** στατιστική που θα γράψουμε για μια τυχαία διαδικασία είναι στο βάθος της απλώς **μια στατιστική τυχαίας μεταβλητής σε σταθερό («παγωμένο») χρόνο**:

<Callout type="key" title="Η μεγάλη ιδέα — γιατί αξίζει αυτή η σελίδα">
  Στο επόμενο κεφάλαιο θα «παγώνουμε» ένα τυχαίο σήμα $X(t)$ σε μία στιγμή $t$ και θα κοιτάμε την τιμή του εκείνη τη στιγμή — που είναι **μια τυχαία μεταβλητή**. Κάθε ποσότητα που θα υπολογίσουμε για το σήμα γεννιέται από εκεί:

  - ο **μέσος** του σήματος σε μια στιγμή είναι ο μέσος **μιας** ΤΜ·
  - η **αυτοσυσχέτιση** (μια ποσότητα που θα μετράει πόσο «μοιάζει» το σήμα ανάμεσα σε δύο στιγμές) είναι ο μέσος ενός **γινομένου δύο** ΤΜ — γι' αυτό θα χρειαστούμε τη *joint* κατανομή τους (§6)·
  - το **φάσμα ισχύος** προκύπτει με Fourier πάνω σε αυτή την αυτοσυσχέτιση.

  Δεν χρειάζεται να μπορείς να **διαβάσεις ή να γράψεις** τους τύπους τους τώρα — θα τις συναντήσεις γραμμένες ως $m_X(t)$, $R_X(t_1, t_2)$, $S_X(f)$, και χτίζονται μία-μία **από το μηδέν** στα [Random processes](/randomness/random-processes) και [PSD](/randomness/psd). Εδώ κράτα μόνο **το σχήμα**: όλα ανάγονται σε «μέσο μιας ΤΜ» ή «μέσο γινομένου δύο ΤΜ». Και ακριβώς αυτά τα δύο τα στήνει η σελίδα που διαβάζεις τώρα (μέσος → §4, γινόμενο δύο ΤΜ → §6).

  Άρα αυτή δεν είναι «φιλική παρένθεση», αλλά **ο σκελετός κάθε επόμενου υπολογισμού** σε ολόκληρη την ομάδα Randomness και στην ανάλυση θορύβου που ακολουθεί (πλήρης χάρτης στην §13).
</Callout>

Αν λοιπόν αυτή η σελίδα σου φαίνεται «εύκολη», **αυτό είναι καλό σημάδι** — σημαίνει ότι θα διαβάζεις τις επόμενες σελίδες χωρίς να σκοντάφτεις σε ΤΜ-διαδικαστικά.

## 2. Τι είναι μια τυχαία μεταβλητή

Μια **τυχαία μεταβλητή (ΤΜ)** $X$ — στα αγγλικά *random variable*, με συντομογραφία *RV* — είναι ένας αριθμός που παίρνει την τιμή του από ένα τυχαίο πείραμα. Θα τη γράφουμε **ΤΜ** σε όλη τη σελίδα. Παραδείγματα:

- Ρίξε νόμισμα: $X = 0$ αν κορώνα, $X = 1$ αν γράμματα.
- Μέτρα τη θερμοκρασία ενός αντιστάτη: $X$ = η ένδειξη του βολτομέτρου σε ένα τυχαίο πείραμα.
- Δες το επόμενο sample θορύβου από έναν δέκτη: $X$ = η τιμή του στο επόμενο millisecond.

**Σύμβαση συμβολισμού** — υπάρχουν **τρεις** βασικοί συμβολισμοί που θα δεις παντού στο κεφάλαιο:

| Σύμβολο | Σημασία |
| --- | --- |
| $X$ (κεφαλαίο) | Η ΤΜ ως **αντικείμενο** — η αφηρημένη έννοια. |
| $x$ (πεζό) | Μια **συγκεκριμένη τιμή** που μπορεί να πάρει η $X$. |
| $a$ (πεζό λατινικό, κάποιες φορές) | Ίδιο πράγμα με το $x$ — οι διαφάνειες του K21 προτιμούν την παράμετρο $a$ μέσα στο $f_{X(t)}(a)$ όταν δουλεύουν με ΤΔ (βλ. **slide 10**). Είναι απλώς θέμα γραμμάτου — όχι διαφορά νοήματος. |

Όταν δηλαδή γράφουμε $P(X \leq x)$ ή $f_X(x)$, το $x$ είναι το «**όρισμα**» — δείχνει «σε ποια τιμή αξιολογώ τη συνάρτηση». Το $X$ από την άλλη είναι η ΤΜ καθαυτή. (Το σύμβολο $P(\cdot)$ διαβάζεται «η πιθανότητα του…» — το χρησιμοποιούμε αμέσως παρακάτω στην §3.)

## 3. CDF και PDF — η ταυτότητα μιας ΤΜ

Μια ΤΜ χαρακτηρίζεται **πλήρως** από την **κατανομή πιθανότητας** της — δηλαδή από το «ποιες τιμές παίρνει, και με τι πιθανότητα την καθεμία» (η έννοια που χτίσαμε διαισθητικά στο [/randomness/why](/randomness/why)). Δύο ισοδύναμοι τρόποι να την περιγράψεις:

### 3α. CDF (cumulative distribution function)

Η CDF δίνει την πιθανότητα η ΤΜ να μην ξεπερνά κάποιο όριο $x$ — δηλαδή «πόση πιθανότητα έχει **μαζευτεί** μέχρι το $x$». Συγκεκριμένα, αυτή η μαζεμένη πιθανότητα είναι το **εμβαδόν κάτω από την καμπύλη πυκνότητας** $f_X$ από το $-\infty$ μέχρι το $x$:

$$
F_X(x) = P(X \leq x) = \int_{-\infty}^{x} f_X(u)\, du.
$$

(Η $f_X \geq 0$ είναι η **πυκνότητα πιθανότητας** — πόσο «πυκνά» στοιβάζεται πιθανότητα γύρω από κάθε τιμή· την ορίζουμε ακριβώς στην §3β αμέσως παρακάτω. Το $u$ είναι απλώς η μεταβλητή ολοκλήρωσης που «τρέχει» από το $-\infty$ μέχρι το όριο $x$ — γράφουμε $u$ και όχι $x$ για να μην μπερδεύεται με το σταθερό πάνω όριο.)

Με αυτή τη μορφή, **το γιατί η $F_X$ είναι πάντα μη-φθίνουσα γίνεται προφανές**: καθώς μεγαλώνει το $x$, το πάνω όριο του ολοκληρώματος μετακινείται δεξιά, οπότε σαρώνεις **περισσότερο** εμβαδόν κάτω από μια καμπύλη που **δεν γίνεται ποτέ αρνητική** ($f_X \geq 0$). Άρα το μαζεμένο εμβαδόν μόνο να μένει ίδιο ή να μεγαλώνει μπορεί — ποτέ να μικραίνει. (Με λόγια: για μεγαλύτερο $x$ προσθέτεις τις πιθανότητες ακόμη περισσότερων τιμών, και οι πιθανότητες δεν είναι ποτέ αρνητικές.) Τα δύο άκρα βγαίνουν από το ίδιο ολοκλήρωμα: $F_X(-\infty) = 0$ (μηδενικό εμβαδόν — δεν έχει μαζευτεί τίποτα ακόμη) και $F_X(+\infty) = 1$ (όλο το εμβαδόν κάτω από την $f_X$, που είναι πάντα 1 — η συνολική μάζα πιθανότητας).

### 3β. PDF (probability density function)

Μόλις γράψαμε την CDF ως **ολοκλήρωμα** της πυκνότητας $f_X$. Πηγαίνοντας **αντίστροφα**, η $f_X$ είναι η **παράγωγος** της CDF:

$$
f_X(x) = \frac{dF_X(x)}{dx}.
$$

Αυτό δεν είναι νέος, ανεξάρτητος ορισμός — είναι ακριβώς το αντίστροφο του ολοκληρώματος της §3α. Η παράγωγος ενός ολοκληρώματος ως προς το **πάνω όριό** του σου δίνει πίσω την ίδια τη συνάρτηση που ολοκλήρωσες (αυτό είναι το **θεμελιώδες θεώρημα του λογισμού**). Άρα CDF και PDF είναι οι δύο όψεις του ίδιου νομίσματος: **ολοκληρώνεις** την πυκνότητα για να μαζέψεις πιθανότητα (CDF), **παραγωγίζεις** τη CDF για να πάρεις πίσω την πυκνότητα. Διαισθητικά: **η πυκνότητα είναι ο ρυθμός με τον οποίο μαζεύεται η πιθανότητα** καθώς προχωράς στο $x$.

Ιδιότητες: $f_X(x) \geq 0$ και $\int_{-\infty}^{\infty} f_X(x)\, dx = 1$ (το ολικό εμβαδόν κάτω από την PDF είναι **πάντα** 1 — η συνολική «μάζα πιθανότητας»).

**Διαβάζοντας την PDF** — η πιθανότητα η ΤΜ να βρίσκεται σε ένα μικρό διάστημα γύρω από $x$ είναι περίπου $f_X(x)\,dx$. **Όχι** «η πιθανότητα να είναι ακριβώς $x$» — αυτή είναι 0 για συνεχείς ΤΜ. Σκέψου την PDF ως «πυκνότητα πιθανότητας» (γι' αυτό το όνομα). Για να πάρεις πραγματική πιθανότητα, ολοκληρώνεις:

$$
P(a \leq X \leq b) = \int_a^b f_X(x)\, dx.
$$

### 3γ. Η ίδια ιδέα για διακριτές ΤΜ — PMF

Για **διακριτές** ΤΜ (που παίρνουν πεπερασμένες ή αριθμήσιμες τιμές — δηλαδή μια ξεχωριστή λίστα τιμών, π.χ. $0, 1, 2, \ldots$, όχι ένα συνεχές διάστημα), χρησιμοποιούμε τη **συνάρτηση μάζας πιθανότητας (probability mass function, PMF)** αντί για PDF:

$$
P(X = x_i) = p_i, \qquad \sum_i p_i = 1.
$$

Εδώ το $P(X = x_i)$ είναι **πραγματική πιθανότητα**, όχι πυκνότητα.

<Callout type="note">
  **Slide 8** γράφει ξεκάθαρα και τους δύο όρους:
  *«Η συνάρτηση μάζας πιθανότητας (probability mass function) της διακριτής ΤΜ X(0) είναι… Στην περίπτωση συνεχούς ΤΜ ορίζεται η συνάρτηση πυκνότητας πιθανότητας (probability density function).»* — οι ελληνικοί όροι στις εξετάσεις είναι αυτοί που πρέπει να αναγνωρίσεις.
</Callout>

<RecallCard
  variant="concept"
  title="PDF/CDF/PMF σε μία ανάσα"
  keywords={[
    'CDF = P(X ≤ x), πάντα 0 → 1',
    'PDF = παράγωγος CDF, εμβαδόν = 1',
    'P(a ≤ X ≤ b) = ∫_a^b f_X dx',
    'PMF για διακριτές, μάζα όχι πυκνότητα',
    'PDF · dx ≈ πιθανότητα γύρω από x',
  ]}
  skeleton={[
    'Αν σου δίνουν CDF και χρειάζεσαι PDF: παράγωγο.',
    'Αν σου δίνουν PDF και χρειάζεσαι πιθανότητα σε διάστημα: ολοκλήρωμα.',
    'Αν σου δίνουν PDF και χρειάζεσαι μέσο/διασπορά: τύπος LOTUS (§7).',
    'Στις διακριτές δουλεύεις με αθροίσματα αντί για ολοκληρώματα — η «πυκνότητα» γίνεται «μάζα».',
  ]}
  trap={
    <>
      Το <strong>f_X(x) δεν είναι πιθανότητα</strong>. Είναι πυκνότητα — μπορεί να ξεπερνά το 1 (π.χ. Gaussian με σ &lt; 0.4). Πιθανότητα παίρνεις μόνο μετά από ολοκλήρωμα.
    </>
  }
/>

## 4. Mean, variance, και ισχύς

### 4α. Expectation (μέσος, mean)

Το αριθμητικό «κέντρο» της κατανομής:

$$
\mu_X = E[X] = \int_{-\infty}^{\infty} x\, f_X(x)\, dx
$$

(ή $\sum_i x_i\, p_i$ για διακριτές).

<Callout type="warning" title="Το E[·] εδώ είναι Expectation, όχι Energy">
  Αλλού στο μάθημα το $E$ συμβολίζει **ενέργεια** (ενέργεια σήματος, $E_b$ ανά bit κ.λπ.). Εδώ — και σε όλη τη θεωρία πιθανοτήτων — το $E[\,\cdot\,]$ είναι ο **τελεστής μέσης τιμής** (expectation): «πάρε τη μέση τιμή αυτού που βρίσκεται μέσα στις αγκύλες». Το ξεχωρίζεις από τις **αγκύλες** $E[\,\cdot\,]$ με ένα όρισμα μέσα, π.χ. $E[X]$ ή $E[X^2]$.
</Callout>

**Τι σου λέει ο μέσος;** Είναι η «τυπική», κεντρική τιμή της ΤΜ — το **σημείο ισορροπίας** της κατανομής. Αν επαναλάμβανες το τυχαίο πείραμα πολλές φορές και σημείωνες κάθε φορά την τιμή της $X$, ο **μακροπρόθεσμος μέσος όρος** όλων αυτών των τιμών θα έτεινε στο $E[X]$. Στον τύπο, κάθε δυνατή τιμή $x$ «ζυγίζεται» με την πυκνότητα/πιθανότητά της $f_X(x)$ — γι' αυτό ο μέσος είναι ένας **σταθμισμένος μέσος**.

**Τι ΔΕΝ σου λέει;** Τίποτα για το **πόσο απλώνονται** οι τιμές γύρω από αυτό το κέντρο. Δύο ΤΜ μπορεί να έχουν **ακριβώς τον ίδιο μέσο** αλλά εντελώς διαφορετική συμπεριφορά: η μία να κάθεται κολλητά γύρω από τον μέσο, η άλλη να πετάγεται συχνά μακριά του. Αυτό το «πόσο μακριά απλώνεται» δεν φαίνεται καθόλου στον $E[X]$ — το μετράει η **διασπορά (variance)**, που είναι ακριβώς το επόμενο βήμα (§4β).

<Callout type="note">
  **Notation alert για ΤΔ:** όταν αργότερα θα μιλάμε για τη **μέση τιμή ενός τυχαίου σήματος**, οι διαφάνειες γράφουν το γράμμα $m_X$ αντί για $\mu_X$, με την παράμετρο $a$:
  $$
  m_X(t) \triangleq \int_{-\infty}^{\infty} a\, f_{X(t)}(a)\, da
  $$
  (**slide 10**). Είναι **ο ίδιος τύπος** — απλώς $\mu \to m$ και $x \to a$. Μην μπερδευτείς όταν δεις τη μετάβαση.
</Callout>

### 4β. Variance (διασπορά)

Η μέση τετραγωνική απόκλιση από τον μέσο:

$$
\sigma_X^2 = \mathrm{Var}(X) = E[(X - \mu_X)^2] = E[X^2] - \mu_X^2.
$$

**Standard deviation:** $\sigma_X = \sqrt{\sigma_X^2}$.

**Mean square value (η πιο «ηλεκτρική» στατιστική):**

$$
E[X^2] = \int_{-\infty}^{\infty} x^2\, f_X(x)\, dx = \mu_X^2 + \sigma_X^2.
$$

**Γιατί νοιάζεσαι;** Αν $X$ είναι μια στιγμιαία τιμή σήματος, τότε **το $E[X^2]$ είναι η μέση ισχύς** του σήματος. Για zero-mean σήματα ($\mu_X = 0$), ισχύς $=$ διασπορά $= \sigma_X^2$. Αυτή είναι η σύνδεση που θα ξανασυναντήσεις σε κάθε υπολογισμό SNR αργότερα.

### 4γ. Linearity — και τα όριά της

Για **οποιεσδήποτε** σταθερές $a, b$ και ΤΜ $X, Y$:

$$
E[aX + bY] = a\, E[X] + b\, E[Y].
$$

Ισχύει **πάντα**, **ανεξάρτητα** από το αν τα $X, Y$ είναι ανεξάρτητα μεταξύ τους. Είναι η πιο χρήσιμη ιδιότητα στο κεφάλαιο — την εφαρμόζεις χωρίς δεύτερη σκέψη.

Για **διασπορά** όμως:

$$
\mathrm{Var}(aX + bY) = a^2\, \mathrm{Var}(X) + b^2\, \mathrm{Var}(Y) + 2ab\, \mathrm{Cov}(X, Y).
$$

Εδώ εμφανίζεται μια καινούρια ποσότητα, το $\mathrm{Cov}(X, Y)$ (**συνδιακύμανση / covariance**): ένα μέτρο του πόσο «κινούνται μαζί» οι δύο ΤΜ. Δεν χρειάζεται να ξέρεις ακόμη τον τύπο της — τον χτίζουμε από το μηδέν στην §6γ· για τώρα αρκεί η διαίσθηση ότι μετράει **μόνο τη γραμμική** συμμεταβολή (το κατά πόσο τα ζεύγη $(X, Y)$ ακολουθούν μια **ευθεία** τάση, $Y \approx aX + b$) και είναι **0 όταν δεν υπάρχει τέτοια γραμμική σχέση** (τότε οι ΤΜ λέγονται **ασυσχέτιστες**). Τι ακριβώς σημαίνει «γραμμική σχέση» και **πώς την ελέγχεις**, στην §6γ.

<Callout type="warning" title="«Ασυσχέτιστες» δεν είναι το ίδιο με «ανεξάρτητες»">
  Εύκολο να μπερδευτείς εδώ. Το να είναι **ανεξάρτητες** — το αποτέλεσμα της μίας να μην αλλάζει καθόλου τις πιθανότητες της άλλης — είναι **πιο δυνατή** συνθήκη από το $\mathrm{Cov}=0$:
  - **ανεξάρτητες $\Rightarrow$ $\mathrm{Cov}=0$** πάντα·
  - αλλά **$\mathrm{Cov}=0 \not\Rightarrow$ ανεξάρτητες**: δύο ΤΜ μπορεί να είναι ακόμη και **απόλυτα εξαρτημένες** και όμως να έχουν $\mathrm{Cov}=0$, αν η σχέση τους είναι «καμπύλη». Το covariance βλέπει μόνο **ευθείες** — αν η μία καθορίζει την άλλη με παραβολή (π.χ. $Y=X^2$), δεν το «πιάνει».

  Άρα: «δεν σχετίζονται **γραμμικά**» (ασυσχέτιστες) $\neq$ «δεν σχετίζονται **καθόλου**» (ανεξάρτητες). Τη διάκριση τη χτίζουμε αναλυτικά στην §6δ, με ακριβώς το παράδειγμα $Y=X^2$.
</Callout>

Ο σταυρωτός όρος $2ab\,\mathrm{Cov}(X, Y)$ εξαφανίζεται **μόνο** όταν $\mathrm{Cov}(X,Y)=0$ — αρκεί δηλαδή οι ΤΜ να είναι **ασυσχέτιστες** (η ανεξαρτησία είναι αρκετή, αλλά παραπάνω απ' όσο χρειάζεται). Αυτή η ασυμμετρία «linearity πάντα, variance υπό όρους» είναι κλασική πηγή λαθών.

## 5. Κύριες κατανομές που θα συναντήσεις

**Πρώτα, η σημειογραφία $X \sim \ldots$** Το σύμβολο $\sim$ διαβάζεται «**ακολουθεί την κατανομή**» (ή «κατανέμεται ως»). Έτσι το $X \sim U(a, b)$ σημαίνει «η $X$ έχει **uniform** κατανομή με παραμέτρους $a, b$» — μας λέει δηλαδή συνοπτικά **ποια είναι η PDF της $X$**. Το γράμμα πριν την παρένθεση είναι το **όνομα της οικογένειας** ($U$ = Uniform, $\mathcal{N}$ = Normal/Gaussian, …) και οι αριθμοί μέσα οι **παράμετροί** της. Π.χ. «$X \sim U(0,1)$» είναι σκέτη συντομογραφία του «$f_X(x) = 1$ στο $[0,1]$, $0$ αλλιώς». (Για συνεχείς ΤΜ, $U[a,b]$ και $U(a,b)$ είναι ουσιαστικά το ίδιο: τα άκρα δεν αλλάζουν πιθανότητες, αφού ένα μεμονωμένο σημείο έχει πιθανότητα $0$.)

Αντί να τις διαβάσεις απλώς ως πίνακα, **σύρε τους ολισθητές** στο παρακάτω και νιώσε πώς η κάθε παράμετρος αλλάζει το σχήμα:

<DistributionExplorerViz />

<Callout type="intuition">
  Παρατήρηση από το viz (κοίτα το **ύψος της κορυφής σε σχέση με τις οριζόντιες γραμμές ύψους** — ο κάθετος άξονας είναι πλέον **σταθερός**): όταν διπλασιάζεις το $\sigma$ μιας Gaussian, το peak της PDF πέφτει **στο μισό** (γιατί το συνολικό εμβαδόν παραμένει 1). Όταν πλαταίνεις το διάστημα $[a, b]$ μιας Uniform, το ύψος της πέφτει για τον ίδιο λόγο. **Η κατανομή «εξαπλώνεται» αλλά παραμένει η ίδια ταυτότητα.** Και αντίστροφα: για **μικρό** $\sigma$ (ή στενό $[a,b]$) η κορυφή **ξεπερνά το 1** — δες την να περνάει πάνω από τη γραμμή $y=1$. Δεν είναι λάθος: η PDF είναι **πυκνότητα**, όχι πιθανότητα.
</Callout>

### 5α. Uniform U(a, b)

PDF: $f_X(x) = 1/(b - a)$ για $x \in [a, b]$, αλλιώς 0.

- $E[X] = (a + b)/2$
- $\mathrm{Var}(X) = (b - a)^2 / 12$

**Συχνή χρήση στο K21:** η **τυχαία φάση** $\Theta \sim U(0, 2\pi)$ (ή $U(-\pi, \pi)$, ή $U(0, \pi)$) σε διαμορφώσεις — όταν δεν έχουμε καμία προτίμηση για συγκεκριμένη φάση, η ομοιόμορφη κατανομή είναι το «αμερόληπτο» μοντέλο. Θα τη συναντήσεις **παντού** στο επόμενο κεφάλαιο.

### 5β. Gaussian / Normal $\mathcal{N}(\mu, \sigma^2)$

$$
f_X(x) = \frac{1}{\sigma\sqrt{2\pi}}\, e^{-(x - \mu)^2/(2\sigma^2)}.
$$

- $E[X] = \mu$
- $\mathrm{Var}(X) = \sigma^2$
- **68 %** της μάζας μέσα σε $\pm\sigma$, **95 %** σε $\pm 2\sigma$, **99.7 %** σε $\pm 3\sigma$ — αυτό είναι το «εμπειρικό κανόνα» που θα σου χρειαστεί για να εκτιμήσεις πιθανότητες σφαλμάτων στις noise σελίδες.

**Πανταχού παρούσα στα συστήματα επικοινωνιών:**
- Ο **θερμικός θόρυβος** ακολουθεί Gaussian κατανομή πλάτους — γι' αυτό λέμε **AWGN (Additive White Gaussian Noise)**. Η αιτία είναι το **Central Limit Theorem** (CLT): ο θόρυβος είναι το άθροισμα πολλών μικρών ανεξάρτητων ηλεκτρικών διεγέρσεων, άρα τείνει να γίνει Gaussian.
- Πολλά τυχαία σήματα (interference, fading) προσεγγίζονται καλά ως Gaussian.

<Callout type="warning">
  **Παγίδα Σ/Λ που εμφανίζεται κάθε χρόνο:** «λευκός θόρυβος» ≠ «Gaussian θόρυβος». **Δεν είναι το ίδιο πράγμα**. Ο «λευκός» αναφέρεται στο **φάσμα** (επίπεδη PSD — όλες οι συχνότητες ίδιες). Ο «Gaussian» αναφέρεται στην **κατανομή πλάτους** (πώς μοιάζει το ιστόγραμμα των τιμών). Ο θερμικός θόρυβος είναι **και τα δύο** (γι' αυτό AWGN), αλλά π.χ. ο uniform-amplitude λευκός θόρυβος είναι λευκός χωρίς να είναι Gaussian. Το βλέπουμε αναλυτικά στο [/noise/sources](/noise/sources).
</Callout>

### 5γ. Exponential$(\lambda)$

$$
f_X(x) = \lambda\, e^{-\lambda x} \quad \text{για } x \geq 0
$$

- $E[X] = 1/\lambda$
- $\mathrm{Var}(X) = 1/\lambda^2$

**Χρήση:** χρόνοι μεταξύ τυχαίων γεγονότων (π.χ. arrivals πακέτων σε ένα δίκτυο). Το K21 δεν εξετάζει συστηματικά εκθετικές, αλλά είναι καλό να την αναγνωρίζεις — αν δεις «memoryless» ή «inter-arrival time», αυτή είναι.

### 5δ. Bernoulli, Binomial, Poisson

Διακριτές κατανομές — δεν αποτελούν κεντρικό υλικό στο K21:

- **Bernoulli(p)**: $P(X=1) = p$, $P(X=0) = 1-p$. Νόμισμα.
- **Binomial(n, p)**: άθροισμα n ανεξάρτητων Bernoulli. «Πόσες κορώνες σε n ρίψεις;»
- **Poisson(λ)**: αριθμός γεγονότων σε σταθερό διάστημα όταν ο ρυθμός είναι λ.

<Collectible id="polka-skin" />

## 6. Joint distributions, independence, covariance

Πολλά πράγματα στο κεφάλαιο εμπλέκουν **δύο** ΤΜ (π.χ. $X(t_1)$ και $X(t_2)$ — η ίδια ΤΔ σε δύο διαφορετικές στιγμές). Χρειαζόμαστε λοιπόν εργαλεία για ζεύγη.

### 6α. Joint PDF

Για δύο ΤΜ $X, Y$, η **joint PDF** $f_{X,Y}(x, y)$ είναι η **πυκνότητα του ζεύγους** — το 2D ανάλογο της PDF μιας μεταβλητής: μια επιφάνεια πάνω από το επίπεδο των $(x, y)$, ψηλή εκεί όπου το ζεύγος εμφανίζεται συχνά. Η πιθανότητα ότι το $(X, Y)$ πέφτει σε μια μικρή περιοχή γύρω από $(x, y)$ είναι περίπου $f_{X,Y}(x, y)\, dx\, dy$.

Ορίζεται για **οποιοδήποτε** ζεύγος ΤΜ — ανεξάρτητα ή όχι — και είναι η **πλήρης** περιγραφή του πώς συν-κατανέμονται τα δύο. Δύο πράγματα να προσέξεις εδώ, γιατί μπερδεύουν. Πρώτον, **εν γένει δεν χτίζεται** από τις δύο ξεχωριστές $f_X, f_Y$ — σε λίγο θα δεις γιατί. Δεύτερον, και πιο σημαντικό για τα συστήματα επικοινωνιών: **δεν σου δίνεται έτοιμη σαν τύπος** $f_{X,Y}(x,y) = \ldots$. Αυτό που έχεις στα χέρια σου είναι μια **σχέση** — π.χ. $Y = X + N$ (λαμβανόμενο = σήμα + θόρυβος), ή τα $X(t_1), X(t_2)$ της ίδιας ΤΔ σε δύο στιγμές, ή η είσοδος/έξοδος ενός φίλτρου — μαζί με τις κατανομές των κομματιών. Από αυτά **την κατασκευάζεις**· το εργαλείο της κατασκευής (ο κανόνας γινομένου) έρχεται αμέσως παρακάτω, και μετά τη φτιάχνουμε ολόκληρη σε ένα ρεαλιστικό παράδειγμα.

**Marginals** — από την joint μπορείς **πάντα** να γυρίσεις στην κατανομή μιας μόνο μεταβλητής, «ολοκληρώνοντας έξω» την άλλη:

$$
f_X(x) = \int_{-\infty}^{\infty} f_{X,Y}(x, y)\, dy.
$$

Πρόσεξε την **ασυμμετρία**: joint $\to$ marginal γίνεται πάντα (ολοκληρώνεις έξω τη μία)· το αντίστροφο **όχι** — από τα δύο marginals **και μόνο** δεν ξαναφτιάχνεις την joint, γιατί σου λείπει το «δέσιμό» τους.

Αυτό το δέσιμο το δίνει η **δεσμευμένη (conditional) πυκνότητα**. Πράγματι, **πάντα** — ανεξάρτητα ή όχι — η joint γράφεται ως **(δεσμευμένη) × (περιθώρια)**:

$$
f_{X,Y}(x, y) = f_{X\mid Y}(x\mid y)\, f_Y(y) = f_{Y\mid X}(y\mid x)\, f_X(x),
$$

όπου $f_{X\mid Y}(x\mid y)$ διαβάζεται «η πυκνότητα της $X$ **δεδομένου ότι** η $Y$ πήρε την τιμή $y$». Το επιπλέον λοιπόν που χρειάζεσαι πέρα από τα δύο marginals είναι ακριβώς αυτή η δεσμευμένη — αυτή κουβαλάει όλη την πληροφορία για το πώς «δένεται» η μία ΤΜ με την άλλη.

<Example title="Παράδειγμα — λαμβανόμενο σήμα + θόρυβος: κατασκευάζω την joint και βγάζω marginal" defaultOpen={true}>
Εδώ **δεν** μας δίνουν την $f_{X,Y}$· μας δίνουν μια **σχέση** και τα κομμάτια της. Έστω σήμα $X \sim U[0,1]$ και **ανεξάρτητος** θόρυβος $N \sim U[0,1]$, και λαμβάνουμε

$$
Y = X + N.
$$

(Διαλέγω uniform κομμάτια μόνο για να βγαίνει καθαρά το ολοκλήρωμα· με Gaussian θόρυβο — η περίπτωση AWGN — η μηχανική είναι **ολόιδια**.) Οι $X, Y$ είναι ολοφάνερα **εξαρτημένες**: το $Y$ κουβαλάει μέσα του το $X$.

**Βήμα 1 — φτιάχνω την joint με τον κανόνα γινομένου.** Δεν την έχω έτοιμη, τη συναρμολογώ. Από τις δύο μορφές $f_{X,Y} = f_{X\mid Y}\,f_Y = f_{Y\mid X}\,f_X$ διαλέγω εκείνη που **διαβάζεται κατευθείαν από τη σχέση** — την $f_{Y\mid X}\,f_X$:
$$
f_{X,Y}(x, y) = f_{Y\mid X}(y\mid x)\, f_X(x).
$$
Το $f_X(x) = 1$ στο $[0,1]$. **Σημείωση** — το $1$ εδώ είναι **πυκνότητα**, όχι πιθανότητα: πιθανότητα παίρνεις μόνο ως **εμβαδόν**, και σε όλο το $[0,1]$ βγαίνει $\int_0^1 1\,dx = 1$ (= 100%, το $X$ είναι σίγουρα κάπου εκεί)· ένα μεμονωμένο σημείο έχει $P(X = x) = 0$. Για το conditional: **δεδομένου ότι $X = x$**, είναι $Y = x + N \Leftrightarrow N = Y - x$ — το $Y$ είναι απλώς ο θόρυβος μετατοπισμένος κατά τη σταθερά $x$. Γιατί τότε $f_{Y\mid X}(y\mid x) = f_N(y - x)$; Επειδή για να βγει το $Y$ **ακριβώς** στην τιμή $y$, ο θόρυβος πρέπει να πάρει **ακριβώς** την τιμή $N = y - x$ — **μόνο τότε** $x + N = y$. Άρα η πυκνότητα του $Y$ στο $y$ είναι όση ακριβώς και η πυκνότητα του $N$ στο $y - x$ (κι αφού ο $N$ είναι ανεξάρτητος του $X$, το να ξέρουμε το $x$ δεν αλλάζει καθόλου την κατανομή του $N$). Τυπικά: $P(Y \le y \mid X = x) = P(N \le y - x) = F_N(y - x)$, και παραγωγίζοντας ως προς $y$ προκύπτει:
$$
f_{Y\mid X}(y\mid x) = f_N(y - x) = 1 \quad \text{για } 0 \le y - x \le 1,\ \text{δηλαδή } x \le y \le x + 1.
$$
Το «$= 1$» είναι ακριβώς η **uniform** πυκνότητα: η $U[0,1]$ έχει $f_N = 1$ σταθερά μέσα στο $[0,1]$ (και $0$ έξω) — πάλι ύψος $1$ ώστε το εμβαδόν να βγει $1$. Άρα η joint είναι
$$
f_{X,Y}(x, y) = f_X(x)\, f_N(y - x) = 1 \quad \text{στη λωρίδα } \{\, 0 \le x \le 1,\ x \le y \le x + 1 \,\}, \ \text{αλλιώς } 0.
$$
Νά την — **την υπολογίσαμε** από τη σχέση, δεν μας δόθηκε.

**Βήμα 2 — marginal $f_Y(y)$, «ολοκληρώνοντας έξω» το $x$.** Ο ίδιος τύπος marginalization, με τους ρόλους $x \leftrightarrow y$ αλλαγμένους:
$$
f_Y(y) = \int_{-\infty}^{\infty} f_{X,Y}(x, y)\, dx = \int_{-\infty}^{\infty} f_N(y - x)\, f_X(x)\, dx.
$$
Στάσου μια στιγμή εδώ: αυτό είναι **συνέλιξη**, $f_Y = f_X * f_N$. Δεν είναι σύμπτωση — η πυκνότητα ενός **αθροίσματος ανεξάρτητων** ΤΜ είναι πάντα η συνέλιξη των δύο πυκνοτήτων (η ίδια συνέλιξη που κυριαρχεί σε όλο το μάθημα).

Το ολοκλήρωμα βγαίνει «μετρώντας επικάλυψη». Ο integrand $f_N(y - x)\, f_X(x)$ είναι $1$ μόνο εκεί που ισχύουν **ταυτόχρονα** και τα δύο support (αλλιώς $0$): το $x$ μέσα στο $[0, 1]$ (από το $f_X$) **και** το $x$ μέσα στο $[y - 1,\, y]$ (από το $0 \le y - x \le 1$). Επειδή εκεί ο integrand είναι σταθερά $1$, το ολοκλήρωμα **είναι ακριβώς το μήκος** αυτής της επικάλυψης.

Το παράθυρο $[y - 1,\, y]$ έχει σταθερό πλάτος $1$ και, καθώς το $y$ ανεβαίνει από $0$ ως $2$, ολισθαίνει προς τα δεξιά πάνω από το σταθερό $[0, 1]$. Τρεις περιπτώσεις:

- **$0 \le y \le 1$** — το παράθυρο μπαίνει από αριστερά: $\max(0,\, y - 1) = 0$ και $\min(1,\, y) = y$, άρα επικάλυψη $[0,\, y]$, μήκος $y - 0 = y$.
- **$1 \le y \le 2$** — το παράθυρο βγαίνει από δεξιά: $\max(0,\, y - 1) = y - 1$ και $\min(1,\, y) = 1$, άρα επικάλυψη $[y - 1,\, 1]$, μήκος $1 - (y - 1) = 2 - y$.
- **$y < 0$ ή $y > 2$** — τα δύο διαστήματα δεν τέμνονται, μήκος $0$. (Λογικό: $Y = X + N$ με $X, N \in [0, 1]$, άρα το $Y$ ζει μόνο στο $[0, 2]$.)

Συνολικά:
$$
f_Y(y) = \begin{cases} y, & 0 \le y \le 1,\\ 2 - y, & 1 \le y \le 2,\\ 0, & \text{αλλιώς.} \end{cases}
$$
**Τριγωνική** πυκνότητα στο $[0, 2]$, με κορυφή στο $y = 1$. Γρήγορος έλεγχος ότι στέκει: εμβαδόν $= \tfrac{1}{2}\cdot 2 \cdot 1 = 1$. ✓

Το «δέσιμο» φαίνεται με γυμνό μάτι: το ίδιο το support συνδέει τα $x, y$ (για δοσμένο $x$, το $y$ ζει μόνο στο $[x,\, x + 1]$), οπότε $f_{X,Y} \ne f_X f_Y$ — **εξαρτημένες**, όπως το περιμέναμε αφού το $Y$ είναι το $X$ συν θόρυβο. Με Gaussian θόρυβο η ίδια ακριβώς διαδικασία δίνει ζεύγος jointly Gaussian, και η συνέλιξη δύο Gaussians ξαναβγάζει Gaussian με **αθροισμένες** variances — το «οι διασπορές προστίθενται» που θα ξαναδείς στον θόρυβο.
</Example>

**Το $E[XY]$ και πώς πάντα «σπάει» σε δύο κομμάτια.** Μόλις έχεις joint PDF, μπορείς να ορίσεις τον μέσο του **γινομένου** δύο ΤΜ, $E[XY] = \iint xy\, f_{X,Y}(x,y)\,dx\,dy$ — η ποσότητα-κλειδί για δύο ΤΜ μαζί. Αξίζει να δεις από τώρα ότι **πάντα** σπάει σε δύο κομμάτια:

$$
E[XY] = \underbrace{\mu_X\,\mu_Y}_{\text{γινόμενο των μέσων}} \;+\; \underbrace{\mathrm{Cov}(X,Y)}_{\text{πόσο συν-κινούνται γύρω από τους μέσους}}, \qquad \mathrm{Cov}(X,Y) \triangleq E[(X-\mu_X)(Y-\mu_Y)].
$$

**Απόδειξη** (μόνο linearity του $E$, αφού οι $\mu_X, \mu_Y$ είναι σταθερές): άνοιξε το γινόμενο μέσα στον ορισμό του $\mathrm{Cov}$,

$$
\mathrm{Cov}(X,Y) = E[XY - \mu_X Y - \mu_Y X + \mu_X\mu_Y] = E[XY] - \mu_X\mu_Y - \mu_Y\mu_X + \mu_X\mu_Y = E[XY] - \mu_X\mu_Y,
$$

και αναδιατάσσοντας παίρνεις $E[XY] = \mu_X\mu_Y + \mathrm{Cov}(X,Y)$.

Με λόγια: το $E[XY]$ είναι **το γινόμενο των μέσων** ($\mu_X\mu_Y$, το σταθερό «DC» κομμάτι) **συν** το πόσο οι δύο ΤΜ ταλαντώνονται μαζί γύρω από τους μέσους τους ($\mathrm{Cov}$). Τι ακριβώς μετράει το $\mathrm{Cov}$ και πώς το κανονικοποιούμε, το εξετάζουμε αναλυτικά στην **§6γ**· εδώ μας αρκεί η σχέση, γιατί κάνει την **ανεξαρτησία** που ακολουθεί να βγαίνει με μια κίνηση.

### 6β. Independence — το ισχυρότερο που μπορείς να πεις

Αυτή είναι ακριβώς η **ειδική περίπτωση** όπου η joint $f_{X,Y}$ (από την §6α) **σπάει** στο γινόμενο των δύο marginals. $X, Y$ είναι **ανεξάρτητα** αν και μόνο αν:

$$
f_{X,Y}(x, y) = f_X(x)\, f_Y(y) \quad \text{για όλα τα } x, y.
$$

Συνδέεται απευθείας με τον κανόνα γινομένου της §6α: ανεξαρτησία σημαίνει ότι η **δεσμευμένη πέφτει πάνω στην περιθώρια**, $f_{X\mid Y}(x\mid y) = f_X(x)$ — δηλαδή το να ξέρεις την τιμή της $Y$ **δεν αλλάζει** καθόλου την κατανομή της $X$. Βάζοντας $f_{X\mid Y} = f_X$ στο γενικό $f_{X,Y} = f_{X\mid Y}\, f_Y$, καταρρέει αμέσως στο γινόμενο $f_X f_Y$.

Με λόγια: όταν είναι ανεξάρτητα, η πλήρης 2D περιγραφή **καταρρέει** στο γινόμενο των δύο 1D κομματιών — ξέρεις τα πάντα για το ζεύγος ξέροντας μόνο το καθένα χωριστά. Όταν **δεν** είναι ανεξάρτητα, η $f_{X,Y}$ **εξακολουθεί να υπάρχει** κανονικά (ορίστηκε στην §6α για κάθε ζεύγος) — απλώς δεν παραγοντοποιείται, και τη χρειάζεσαι **ολόκληρη** για να περιγράψεις το ζεύγος.

**Συνέπεια που θα χρησιμοποιήσεις διαρκώς:**

$$
E[XY] = E[X]\, E[Y] \quad \text{(αν ανεξάρτητα)}.
$$

**Γιατί ισχύει;** Γρήγορη απόδειξη — χρειάζεσαι μόνο τον ορισμό του μέσου (LOTUS για συνάρτηση δύο ΤΜ, εδώ $g(X, Y) = XY$) και την ανεξαρτησία $f_{X,Y} = f_X\, f_Y$:

$$
\begin{aligned}
E[XY] &= \iint xy\, f_{X,Y}(x, y)\, dx\, dy = \iint xy\, f_X(x)\, f_Y(y)\, dx\, dy \\
&= \Big(\int x\, f_X(x)\, dx\Big)\Big(\int y\, f_Y(y)\, dy\Big) = E[X]\, E[Y].
\end{aligned}
$$

Το κρίσιμο βήμα είναι το **σπάσιμο** του διπλού ολοκληρώματος: μόλις ο integrand γράφεται «κάτι μόνο με $x$» επί «κάτι μόνο με $y$» (το $x f_X(x)$ επί το $y f_Y(y)$), τα δύο ολοκληρώματα ξεχωρίζουν. Χωρίς ανεξαρτησία η $f_{X,Y}$ δεν παραγοντοποιείται και το σπάσιμο δεν γίνεται, οπότε η ανεξαρτησία **δεν εγγυάται** πια την ισότητα — είναι όμως **ικανή** συνθήκη, όχι αναγκαία.

**Δες το και μέσα από τη σχέση $E[XY] = \mu_X\mu_Y + \mathrm{Cov}(X,Y)$:** αυτό που μόλις αποδείξαμε σημαίνει ότι η ανεξαρτησία **μηδενίζει το $\mathrm{Cov}$** — κόβει όλη τη συν-κίνηση και αφήνει μόνο το γινόμενο των μέσων, $E[XY] = \mu_X\mu_Y = E[X]E[Y]$. Άρα «ανεξάρτητες ⇒ $\mathrm{Cov} = 0$», δηλαδή ⇒ **ασυσχέτιστες**.

Πρόσεξε όμως την **κατεύθυνση**: το αντίστροφο **δεν** ισχύει. Η ισότητα (δηλαδή $\mathrm{Cov} = 0$, «**ασυσχέτιστες**») μπορεί να κρατάει και για ΤΜ **εξαρτημένες αλλά ασυσχέτιστες**. Δηλαδή η ανεξαρτησία είναι ο πιο **εύκολος** λόγος να την επικαλεστείς (όποτε σου δίνεται, μπες κατευθείαν), όχι ο μοναδικός· τη γενική εικόνα (ανεξάρτητες / ασυσχέτιστες / ορθογώνιες) τη χτίζουμε στην **§6δ**.

Αυτή η ισότητα είναι το «εργαλείο μηδενισμού» που εφαρμόζεις παντού στο κεφάλαιο της ετεροσυσχέτισης — αν οι δύο ΤΜ είναι ανεξάρτητες, η ετεροσυσχέτιση «κόβει» στο γινόμενο των μέσων (βλ. **slide 18 — Άσκηση 1 λύση 4/5** των διαφανειών).

### 6γ. Covariance & correlation

Πρώτα, **τι εννοούμε «γραμμική σχέση»**: δύο ΤΜ έχουν γραμμική σχέση όταν η μία είναι, κατά προσέγγιση, ένα **σταθερό πολλαπλάσιο** της άλλης συν μια σταθερά — $Y \approx aX + b$. Αν σχεδίαζες πολλά ζεύγη $(X, Y)$ ως σημεία σε άξονες, θα έπεφταν κοντά σε μια **ευθεία γραμμή** (ανηφορική αν $a>0$, κατηφορική αν $a<0$). «Καμία γραμμική σχέση» σημαίνει ότι **δεν υπάρχει τέτοια ευθεία τάση** — προσοχή, **όχι** ότι δεν υπάρχει καμία σχέση (μπορεί κάλλιστα να υπάρχει καμπύλη, όπως $Y=X^2$).

**Covariance — ο τρόπος να το μετρήσεις:**

$$
\mathrm{Cov}(X, Y) = E[(X - \mu_X)(Y - \mu_Y)] = E[XY] - \mu_X\,\mu_Y.
$$

Διάβασέ τον τύπο ως «έλεγχο»: για κάθε αποτέλεσμα κοιτάς αν το $X$ είναι **πάνω ή κάτω** από τον δικό του μέσο (το πρόσημο του $X-\mu_X$) και το ίδιο για το $Y$, και **πολλαπλασιάζεις** τις δύο αποκλίσεις.
- Αν τα δύο τείνουν να είναι **μαζί** πάνω ή **μαζί** κάτω από τους μέσους τους, τα γινόμενα βγαίνουν κυρίως **θετικά** → $\mathrm{Cov} > 0$ (ανηφορική ευθεία τάση).
- Αν όταν το ένα ανεβαίνει το άλλο **κατεβαίνει**, τα γινόμενα είναι κυρίως **αρνητικά** → $\mathrm{Cov} < 0$ (κατηφορική).
- Αν δεν υπάρχει τέτοια συστηματική τάση, τα θετικά και τα αρνητικά γινόμενα **αλληλοεξουδετερώνονται** → $\mathrm{Cov} \approx 0$.

**Correlation coefficient (συντελεστής συσχέτισης)** — το πρόβλημα του covariance είναι ότι η τιμή του εξαρτάται από τις μονάδες/κλίμακα των $X, Y$, οπότε σκέτο «μεγάλο» ή «μικρό» δεν λέει πόσο **δυνατή** είναι η σχέση. Το κανονικοποιούμε διαιρώντας με τα $\sigma_X, \sigma_Y$:

$$
\rho_{XY} = \frac{\mathrm{Cov}(X, Y)}{\sigma_X\, \sigma_Y} \in [-1, 1].
$$

Το $\rho$ είναι ο καθαρός **«δείκτης γραμμικότητας»** — πάντα ανάμεσα σε $-1$ και $1$, ανεξάρτητα από μονάδες:
- $\rho = +1$: **τέλεια** ανηφορική ευθεία — τα σημεία πέφτουν **ακριβώς** πάνω σε γραμμή $Y = aX + b$ με $a>0$.
- $\rho = -1$: τέλεια κατηφορική ευθεία ($a<0$).
- $\rho = 0$: **καμία γραμμική τάση** (γραμμικά ασύσχετα — όχι απαραίτητα ανεξάρτητα· βλ. trap παρακάτω).
- ενδιάμεσα: όσο πιο κοντά στο $\pm1$, τόσο πιο «σφιχτά» μαζεμένα γύρω από μια ευθεία· όσο πιο κοντά στο $0$, τόσο πιο θολή η ευθεία τάση.

Άρα **«πώς ελέγχω αν υπάρχει γραμμική σχέση;»**: υπολογίζεις το $\mathrm{Cov}$ (ή, καθαρότερα, το $\rho$). Μη-μηδενικό → υπάρχει γραμμική τάση, και το **πρόσημο** δίνει την κατεύθυνση· μηδέν → **καμία γραμμική** τάση (αλλά, ξανά, μπορεί να κρύβεται καμπύλη σχέση — γι' αυτό «ασυσχέτιστες» δεν σημαίνει «ανεξάρτητες»).

Σύρε το $\rho$ στο παρακάτω interactive και δες τη γραμμική σχέση να «σφίγγει» γύρω από μια ευθεία ή να χαλαρώνει. Τα σημεία είναι **χρωματισμένα με το πρόσημο του $(X-\mu_X)(Y-\mu_Y)$** — ακριβώς το μέγεθος που μέσο-ποιεί το covariance — οπότε βλέπεις *γιατί* βγαίνει θετικό, αρνητικό ή μηδέν. Πάτα και το preset **«Καμπύλη $Y=X^2$»**: η $Y$ καθορίζεται πλήρως από την $X$ (απόλυτη εξάρτηση) κι όμως το $\rho$ καταρρέει στο $\approx 0$ — η ζωντανή εκδοχή της παγίδας που έρχεται στην §6δ.

<CorrelationScatterViz />

### 6δ. Τρεις όροι που μπερδεύονται: ανεξάρτητες, ασυσχέτιστες, ορθογώνιες

Αυτές οι τρεις λέξεις ακούγονται συνώνυμες αλλά **δεν είναι** — και το να τις μπερδέψεις είναι από τις πιο συχνές πηγές λάθους σε Σ/Λ. Πρώτα οι ορισμοί, καθαρά:

| Όρος | Ορισμός | Πώς το ελέγχεις στην πράξη |
| --- | --- | --- |
| **Ανεξάρτητες** | $f_{X,Y}(x,y) = f_X(x)\, f_Y(y)$ για κάθε $x, y$ | δες αν η joint PDF **παραγοντοποιείται** σε γινόμενο των δύο marginals |
| **Ασυσχέτιστες** | $\mathrm{Cov}(X, Y) = 0$, δηλ. $E[XY] = E[X]\,E[Y]$ | υπολόγισε $E[XY]$ και σύγκρινέ το με το $E[X]\,E[Y]$ |
| **Ορθογώνιες** | $E[XY] = 0$ | υπολόγισε $E[XY]$ και δες αν βγαίνει $0$ |

Δύο σχέσεις συνδέουν αυτούς τους όρους — μία «κάθετη» (πόσο δυνατή συνθήκη) και μία «οριζόντια» (η διαφορά τους):

**(1) Κάθετα — από το πιο δυνατό στο πιο αδύνατο:** **ανεξάρτητες ⇒ ασυσχέτιστες**, και ισχύει **πάντα** (αν παραγοντοποιείται η joint, τότε $E[XY] = E[X]E[Y]$, άρα $\mathrm{Cov} = 0$). Το **αντίστροφο όμως δεν ισχύει** γενικά: ασυσχέτιστες ⇏ ανεξάρτητες — εκτός αν οι ΤΜ είναι **jointly Gaussian** (δηλαδή η *από κοινού* κατανομή τους $f_{X,Y}$ είναι Gaussian — όχι απλώς η καθεμία ξεχωριστά), οπότε μόνο τότε ταυτίζονται οι δύο έννοιες.

**(2) Οριζόντια — ασυσχέτιστες vs ορθογώνιες:** **δεν** είναι το ίδιο. Διαφέρουν ακριβώς κατά τον όρο $E[X]E[Y]$, αφού $\mathrm{Cov}(X,Y) = E[XY] - E[X]E[Y]$. Άρα οι δύο συνθήκες («$\mathrm{Cov}=0$» και «$E[XY]=0$») γίνονται **η ίδια** ακριβώς όταν $E[X]E[Y]=0$ — δηλαδή όταν **τουλάχιστον μία** από τις δύο ΤΜ έχει **μηδενικό μέσο**. Στα συστήματα επικοινωνιών ο θόρυβος είναι σχεδόν πάντα zero-mean, οπότε εκεί «ασυσχέτιστες» και «ορθογώνιες» τις περισσότερες φορές **συμπίπτουν** — γι' αυτό είναι εύκολο να ξεχάσεις ότι γενικά είναι ξεχωριστές.

Η ορθογωνιότητα ($E[XY]=0$) δεν είναι ακαδημαϊκή λεπτομέρεια: όταν προσθέτεις δύο σήματα, ο σταυρωτός όρος στη μέση ισχύ είναι $E[(X+Y)^2] = E[X^2] + E[Y^2] + 2\,E[XY]$, και το $2E[XY]$ **μηδενίζεται ακριβώς όταν** οι δύο είναι ορθογώνιες — τότε **η ισχύς του αθροίσματος είναι το άθροισμα των δύο ισχύων** (η αιτία του +3 dB στην άσκηση «Gaussian» πιο κάτω).

<Callout type="warning">
  **Παγίδα που πέφτουν όλοι:** «οι $X, Y$ έχουν μηδενικό covariance, άρα είναι ανεξάρτητες». **ΛΑΘΟΣ γενικά.** Αντιπαράδειγμα: $X \sim U[-1, 1]$ και $Y = X^2$ είναι **απόλυτα εξαρτημένες** (το $Y$ καθορίζεται πλήρως από το $X$) και όμως έχουν $\mathrm{Cov}(X, Y) = 0$. Η συνεπαγωγή «ασυσχέτιστες ⇒ ανεξάρτητες» ισχύει **μόνο για jointly Gaussian** ΤΜ. Το αναλύουμε βήμα-βήμα στην εξάσκηση (§10, άσκηση «Independence»).
</Callout>

<RecallCard
  variant="concept"
  title="Joint, independence, covariance σε μία ανάσα"
  keywords={[
    'f_{X,Y} = f_X · f_Y ⇔ ανεξάρτητες',
    'E[XY] = E[X]·E[Y] (αν ανεξάρτητες)',
    'Cov = E[XY] − μ_X μ_Y',
    'ρ ∈ [−1, 1]',
    'indep ⇒ uncorr, αντίστροφο μόνο Gaussian',
  ]}
  skeleton={[
    'Πρώτα ρωτάς: μου έδωσαν ανεξαρτησία ως δεδομένο; Αν ναι, χρησιμοποίησε E[XY] = E[X]E[Y].',
    'Αν χρειάζεσαι covariance: υπολόγισε E[XY] με ολοκλήρωμα 2 διαστάσεων, μετά αφαίρεσε μ_X μ_Y.',
    'Αν χρειάζεσαι correlation coefficient: διαίρεσε με σ_X σ_Y.',
    'Όταν η εκφώνηση λέει «ασυσχέτιστες», μην παραπληροφορηθείς ότι είναι ανεξάρτητες — δες αν είναι Gaussian.',
  ]}
  trap={
    <>
      Το να πεις «uncorrelated άρα independent» χωρίς να ελέγξεις πρώτα ότι οι ΤΜ είναι jointly Gaussian είναι το <strong>πιο συχνό λάθος</strong> σε Σ/Λ προβλήματα.
    </>
  }
/>

## 7. LOTUS — υπολόγισε E[g(X)] χωρίς να βρεις την κατανομή του g(X)

Αν $Y = g(X)$ είναι μια ντετερμινιστική συνάρτηση μιας ΤΜ, τότε:

$$
E[g(X)] = \int_{-\infty}^{\infty} g(x)\, f_X(x)\, dx.
$$

Αυτό λέγεται **Law of the Unconscious Statistician (LOTUS)** — δεν χρειάζεται να βρεις πρώτα τη $f_Y(y)$. Απλώς πολλαπλασιάζεις τη $g(x)$ με τη γνωστή σου $f_X(x)$ και ολοκληρώνεις.

**Δύο σχεδόν-κανόνες που θα τους δεις παντού:** για $\Theta \sim U(0, 2\pi)$,

$$
E[\cos\Theta] = \int_0^{2\pi} \cos\theta \cdot \frac{1}{2\pi}\, d\theta = 0
$$

και

$$
E[\cos^2\Theta] = \int_0^{2\pi} \cos^2\theta \cdot \frac{1}{2\pi}\, d\theta = \frac{1}{2}.
$$

**Γιατί τα ξεχωρίζουμε;** Γιατί είναι **οι δύο συχνότερες ταυτότητες** σε όλη τη θεωρία τυχαίων σημάτων του K21. Κάθε φορά που θα δεις $A\cos(2\pi f_0 t + \Theta)$ με $\Theta$ uniform, αυτές οι δύο ταυτότητες θα είναι ακριβώς στην καρδιά του υπολογισμού.

## 8. Πώς συνδέεται με τις τυχαίες διαδικασίες

Αυτή είναι η **πιο σημαντική παράγραφος** της σελίδας — γιατί όλο το επόμενο κεφάλαιο χτίζεται πάνω στο πέρασμα ΤΜ → ΤΔ.

**Παράδειγμα — Άσκηση 1 από τις διαφάνειες** (slides 14–19):

Έστω η ΤΔ $X(t) = A\cos(2\pi f_1 t + \phi)$ με $A$, $f_1$ σταθερές και $\phi \sim U[0, \pi]$:

$$
f_\phi(\phi) = \begin{cases} 1/\pi, & \phi \in [0, \pi] \\ 0, & \text{αλλού.} \end{cases}
$$

**Πώς υπολογίζεις τον μέσο $m_X(t)$ της ΤΔ;** Σε σταθερό $t$, η $X(t)$ είναι **μια ΤΜ** (που ως μοναδική πηγή τυχαιότητας έχει την $\phi$). Άρα ο μέσος είναι **LOTUS** με $g(\phi) = A\cos(2\pi f_1 t + \phi)$ και $f_\phi(\phi) = 1/\pi$:

$$
\begin{aligned}
m_X(t) &= \int_0^\pi A\cos(2\pi f_1 t + \phi)\, \frac{1}{\pi}\, d\phi \\
&= \frac{A}{\pi}\, \big[\sin(2\pi f_1 t + \phi)\big]_0^\pi \\
&= \frac{A}{\pi}\, \big[\sin(2\pi f_1 t + \pi) - \sin(2\pi f_1 t)\big] \\
&= \frac{A}{\pi}\, \big[-\sin(2\pi f_1 t) - \sin(2\pi f_1 t)\big] \\
&= -\frac{2A}{\pi}\, \sin(2\pi f_1 t).
\end{aligned}
$$

(Slide 15, ακριβώς αυτή η ροή.)

**Παρατήρησε:** η ΤΜ-μηχανή που χρησιμοποιήσαμε ήταν **LOTUS + ολοκλήρωμα της PDF του $\phi$**. Καμία «νέα» ιδέα — απλώς οι §§ 3-7 σε εφαρμογή.

<RandomPhaseCosinePdfViz />

**Πώς να διαβάσεις το viz** — δες δύο άξονες:
- **Οριζόντια** (κάθε γραμμή ξεχωριστά): μία «realization» — μία πραγματοποίηση του τυχαίου πειράματος (μία τιμή της $\phi$), που δίνει ένα συγκεκριμένο cosine.
- **Κάθετα** (μία στιγμή $t$): «τέμνεις» τις realizations σε σταθερό $t$ και παίρνεις την ΤΜ $X(t)$. Η ιστογραμμική κατανομή αυτών των τιμών είναι η PDF της $X(t)$ — και είναι αυτή που χρησιμοποιεί το LOTUS για να βρει $m_X(t), R_X(t_1, t_2), \ldots$

<Callout type="key">
  **Η συνδετική γέφυρα:** για κάθε ΤΔ X(t) στα επόμενα κεφάλαια, οι στατιστικές της (μέσος, αυτοσυσχέτιση) θα υπολογιστούν με LOTUS πάνω στις παραμέτρους τυχαιότητας — που είναι ΤΜ. Δεν υπάρχει «νέο μαθηματικό όπλο» στις ΤΔ· υπάρχει μόνο **εφαρμογή** των ΤΜ-εργαλείων που θυμάσαι από εδώ.
</Callout>

## 9. Σύνοψη τύπων

| Έννοια | Τύπος |
| --- | --- |
| CDF | $F_X(x) = P(X \leq x)$ |
| PDF | $f_X(x) = dF_X/dx$, $\int f_X = 1$ |
| PMF (διακριτές) | $P(X = x_i) = p_i$, $\sum p_i = 1$ |
| Mean | $\mu_X = \int x f_X\, dx$ (ή $m_X$ — ίδιο πράγμα) |
| Mean square | $E[X^2] = \int x^2 f_X\, dx$ |
| Variance | $\sigma_X^2 = E[X^2] - \mu_X^2$ |
| Linearity (πάντα) | $E[aX + bY] = aE[X] + bE[Y]$ |
| Variance sum | $\mathrm{Var}(aX + bY) = a^2 \mathrm{Var}(X) + b^2 \mathrm{Var}(Y) + 2ab\, \mathrm{Cov}(X, Y)$ |
| Covariance | $\mathrm{Cov}(X, Y) = E[XY] - \mu_X\mu_Y$ |
| Independence | $f_{X,Y} = f_X \cdot f_Y$ |
| Indep ⇒ uncorrelated | (πάντα) |
| Uncorrelated ⇒ indep | (μόνο για Gaussian) |
| LOTUS | $E[g(X)] = \int g(x) f_X(x)\, dx$ |
| $E[\cos\Theta], \Theta \sim U(0, 2\pi)$ | $= 0$ |
| $E[\cos^2\Theta], \Theta \sim U(0, 2\pi)$ | $= 1/2$ |

## 10. Εξάσκηση

<ExerciseProgress slug="randomness/random-variables" total={6} />

Έξι ασκήσεις πάνω στις τυχαίες μεταβλητές. Οι πέντε πρώτες δοκιμάζουν τις βασικές ταυτότητες· η έκτη είναι **η γέφυρα προς το επόμενο κεφάλαιο** — εφαρμόζει LOTUS στην ΤΔ της Άσκησης 1 των διαφανειών.

<ExamProblem id="uniform-mean-var" year="Computation" weight="κλασικό uniform">
**Ερώτηση:** $\Theta \sim U[0, 2\pi)$. Βρες τη μέση τιμή $E[\Theta]$, τη διασπορά $\sigma_\Theta^2$, και την $E[\cos\Theta]$.

**Πριν τη λύση** — αναγνωρίζεις δύο τύπους υπολογισμού:
1. *Μέσος/διασπορά της ίδιας της Θ:* εφαρμόζεις απευθείας ορισμούς, $\mu = \int \theta f_\Theta d\theta$ και $\sigma^2 = E[\Theta^2] - \mu^2$.
2. *Μέσος του cos Θ:* αυτό είναι **LOTUS** — $E[g(\Theta)]$ με $g(\theta) = \cos\theta$. Δεν βρίσκεις πρώτα την PDF του $\cos\Theta$.

<Example title="Λύση" defaultOpen={true}>
PDF: $f_\Theta(\theta) = 1/(2\pi)$ για $\theta \in [0, 2\pi)$, αλλιώς 0.

**Mean:** $E[\Theta] = \int_0^{2\pi}\theta\cdot\frac{1}{2\pi}d\theta = \frac{1}{2\pi}\cdot\frac{(2\pi)^2}{2} = \pi$.

**Variance:** $E[\Theta^2] = \int_0^{2\pi}\theta^2\cdot\frac{1}{2\pi}d\theta = \frac{1}{2\pi}\cdot\frac{(2\pi)^3}{3} = \frac{(2\pi)^2}{3} = \frac{4\pi^2}{3}$.
$\sigma_\Theta^2 = E[\Theta^2] - \mu^2 = \frac{4\pi^2}{3} - \pi^2 = \frac{\pi^2}{3}$.

**$E[\cos\Theta]$** (LOTUS):
$$E[\cos\Theta] = \int_0^{2\pi}\cos\theta\cdot\frac{1}{2\pi}d\theta = \frac{1}{2\pi}[\sin\theta]_0^{2\pi} = 0.$$

**Take-away:** η τιμή $E[\cos\Theta] = 0$ θα εμφανιστεί επανειλημμένα στο κεφάλαιο για το random-phase cosine $X(t) = A\cos(2\pi f_0 t + \Theta)$ — εκεί είναι ακριβώς αυτή η LOTUS που μηδενίζει τον μέσο της ΤΔ.
</Example>
</ExamProblem>

<ExamProblem id="gaussian-properties" year="Gaussian" weight="θεμελιώδες">
**Δήλωση:** Αν $X \sim \mathcal{N}(0, \sigma^2)$ και $Y \sim \mathcal{N}(0, \sigma^2)$ είναι ανεξάρτητες, τότε $Z = X + Y \sim \mathcal{N}(0, \sigma^2)$.

**Πριν τη λύση** — δύο τμήματα: (α) **μέσος** του $Z$ είναι linear → πάντα κρατάει, ανεξαρτησία δεν παίζει ρόλο. (β) **διασπορά** όμως απαιτεί ανεξαρτησία για να αθροιστεί απλά. Άρα ο σωστός έλεγχος είναι: «αναλύω χωριστά mean και variance».

<Example title="Λύση" defaultOpen={true}>
**Λάθος** στη διασπορά. Από linearity του mean: $E[Z] = 0 + 0 = 0$. ✓

Από ανεξαρτησία: $\mathrm{Var}(Z) = \mathrm{Var}(X) + \mathrm{Var}(Y) = \sigma^2 + \sigma^2 = 2\sigma^2$. **Όχι** $\sigma^2$.

Άρα $Z \sim \mathcal{N}(0, 2\sigma^2)$. Σημαντικό: η Gaussian είναι **κλειστή ως προς πρόσθεση** — άθροισμα ανεξάρτητων Gaussians είναι Gaussian — αλλά η διασπορά αθροίζεται.

**Take-away εφαρμογή στα ΣΕ:** άθροισμα 2 ανεξάρτητων θορύβων ίσης ισχύος δίνει θόρυβο διπλάσιας ισχύος (**+3 dB**). Αυτή είναι η αιτία που το «προσθέτω noise sources» πάντα αυξάνει την ισχύ θορύβου — και μάλιστα σε dB ο κανόνας είναι +3 dB ανά διπλασιασμό.
</Example>
</ExamProblem>

<ExamProblem id="independence-uncorrelation" year="Independence" weight="key trap">
**Δήλωση:** Δύο τυχαίες μεταβλητές $X$, $Y$ με $\mathrm{Cov}(X, Y) = 0$ είναι πάντα ανεξάρτητες.

**Πριν τη λύση** — η ερώτηση είναι «αντίστροφη κατεύθυνση». Η σωστή κατεύθυνση πάντα είναι: independence ⇒ uncorrelated. Η αντίστροφη ισχύει **αυστηρά μόνο για jointly Gaussian**. Άρα ψάχνεις **αντιπαράδειγμα** σε μη-Gaussian ΤΜ.

<Example title="Λύση" defaultOpen={true}>
**Λάθος γενικά**, **σωστό μόνο για Gaussian**.

**Αντιπαράδειγμα:** $X \sim U[-1, 1]$, $Y = X^2$. Είναι **απόλυτα εξαρτημένες** (το $Y$ καθορίζεται πλήρως από το $X$). Όμως:
$$\mathrm{Cov}(X, Y) = E[XY] - E[X]E[Y] = E[X^3] - 0\cdot E[X^2] = 0$$

(αφού $E[X^3] = 0$ από symmetry για uniform γύρω από 0, και $E[X] = 0$).

**Σωστή κατεύθυνση πάντα:** independence ⇒ uncorrelated. Η αντίστροφη ισχύει **μόνο** για jointly Gaussian.

**Take-away:** όταν στο εξεταστικό δεις «ασυσχέτιστες», **μη το διαβάσεις ως ανεξάρτητες** εκτός αν σου έχουν δώσει ξεκάθαρα ότι είναι Gaussian. Αυτή είναι η κλασική Σ/Λ παγίδα.
</Example>
</ExamProblem>

<ExamProblem id="lotus-application" year="LOTUS" weight="practical">
**Ερώτηση:** $X \sim \mathcal{N}(0, 1)$ (standard Gaussian). Βρες το $E[X^2]$.

**Πριν τη λύση** — δύο διαφορετικές προσεγγίσεις δίνουν την ίδια απάντηση: (α) απευθείας LOTUS με $g(X) = X^2$, ή (β) γνωρίζοντας ότι $E[X^2] = \mu^2 + \sigma^2$ από τον τύπο της variance. Και τα δύο πρέπει να σου δώσουν 1.

<Example title="Λύση">
**Δύο τρόποι:**

**(α) LOTUS:** $E[X^2] = \int_{-\infty}^{\infty} x^2 \cdot \frac{1}{\sqrt{2\pi}}e^{-x^2/2}dx = 1$ (αυτό το συγκεκριμένο ολοκλήρωμα βγαίνει ακριβώς 1 — είναι ένα από τα τυπικά Gaussian ολοκληρώματα· στην εξέταση δεν θα χρειαστεί να το λύσεις με το χέρι).

**(β) Από τον ορισμό:** $E[X^2] = \mu^2 + \sigma^2 = 0 + 1 = 1$.

**Γενικά για zero-mean Gaussian:** $E[X^2] = \sigma^2$.

**Take-away εφαρμογή:** για zero-mean θόρυβο, **η ισχύς είναι ακριβώς $\sigma^2$** — η variance. Αυτή είναι η πιο συχνή χρήση του $E[X^2]$ στις noise σελίδες: «ισχύς λευκού θερμικού θορύβου $N$» $= \sigma_n^2$.
</Example>
</ExamProblem>

<ExamProblem id="linearity-of-expectation" year="Linearity" weight="πάντα">
**Δήλωση:** Για **οποιεσδήποτε** ΤΜ $X, Y$ (όχι απαραίτητα ανεξάρτητες): $E[aX + bY] = aE[X] + bE[Y]$.

**Πριν τη λύση** — αυτή είναι μία από τις λίγες ιδιότητες χωρίς προϋποθέσεις. Αρκεί να ξέρεις ότι linearity = «πάντα», variance = «μόνο αν Cov = 0».

<Example title="Λύση" defaultOpen={true}>
**Σωστό.** Linearity of expectation ισχύει **πάντα**, χωρίς προϋποθέσεις. Δεν χρειάζεται independence.

**Αλλά προσοχή:** για **variance**, ισχύει $\mathrm{Var}(aX + bY) = a^2\mathrm{Var}(X) + b^2\mathrm{Var}(Y) + 2ab\,\mathrm{Cov}(X,Y)$ — και ο cross term εξαφανίζεται **μόνο** όταν $\mathrm{Cov} = 0$ (uncorrelated, ή ανεξάρτητες).

**Take-away:** στην εξέταση, η ασυμμετρία «linearity = πάντα, variance = υπό όρους» είναι κλειδί. Πολλά Σ/Λ προβλήματα δοκιμάζουν αν την έχεις εμπεδώσει.
</Example>
</ExamProblem>

<ExamProblem id="slide-15-bridge" year="Άσκηση 1" weight="γέφυρα ΤΜ → ΤΔ">
**Ερώτηση** (Άσκηση 1 από τις διαφάνειες, slides 14–15): Έστω η ΤΔ $X(t) = A\cos(2\pi f_1 t + \phi)$ με $A$, $f_1$ σταθερές και $\phi \sim U[0, \pi]$. Υπολόγισε τον μέσο $m_X(t)$.

**Πριν τη λύση** — αυτή είναι η **πρότυπη** εφαρμογή LOTUS σε ΤΔ. Αναγνωρίζεις:
- Σταθερό $t$ → η $X(t)$ είναι ΤΜ (μόνη τυχαιότητα: η $\phi$).
- $E[X(t)] = E[g(\phi)]$ όπου $g(\phi) = A\cos(2\pi f_1 t + \phi)$.
- Άρα LOTUS: $m_X(t) = \int_0^\pi g(\phi) f_\phi(\phi) d\phi$ με $f_\phi(\phi) = 1/\pi$.

<Example title="Λύση" defaultOpen={true}>
**Βήμα 1 — Εφαρμογή LOTUS:**

$$
m_X(t) = E[X(t)] = \int_0^\pi A\cos(2\pi f_1 t + \phi) \cdot \frac{1}{\pi}\, d\phi = \frac{A}{\pi} \int_0^\pi \cos(2\pi f_1 t + \phi)\, d\phi.
$$

**Βήμα 2 — Αντιπαραγωγή του cosine** (το $t$ παίζει ρόλο σταθεράς):

$$
\frac{A}{\pi}\, \big[\sin(2\pi f_1 t + \phi)\big]_0^\pi = \frac{A}{\pi}\, \big[\sin(2\pi f_1 t + \pi) - \sin(2\pi f_1 t)\big].
$$

**Βήμα 3 — Χρήση $\sin(x + \pi) = -\sin(x)$:**

$$
\frac{A}{\pi}\, \big[-\sin(2\pi f_1 t) - \sin(2\pi f_1 t)\big] = -\frac{2A}{\pi}\, \sin(2\pi f_1 t).
$$

**Άρα:** $m_X(t) = -\dfrac{2A}{\pi}\, \sin(2\pi f_1 t)$.

**Take-away:** πρόσεξε ότι **ο μέσος εξαρτάται από το $t$** — δεν βγαίνει ένας σταθερός αριθμός, αλλά μια καμπύλη (το $\sin$ παραπάνω) που κυματίζει με τον χρόνο. Κράτα αυτή την παρατήρηση — είναι το κλειδί. Στο [/randomness/stationarity](/randomness/stationarity) θα μάθεις τον επίσημο όρο γι' αυτό: μια ΤΔ της οποίας ο μέσος **δεν** μένει σταθερός στον χρόνο λέγεται ότι **δεν είναι «στάσιμη ως προς τη μέση τιμή»** (το πλήρες κριτήριο που τα μαζεύει όλα λέγεται **WSS**). Δεν χρειάζεται να ξέρεις τον όρο ακόμη — αρκεί να δεις **τι** το καθορίζει εδώ: αν η φάση ήταν $\phi \sim U[0, 2\pi)$ αντί για $U[0, \pi]$, ο μέσος θα μηδενιζόταν παντού (ακριβώς από την ταυτότητα $E[\cos\Theta] = 0$ της §7) και θα έβγαινε σταθερός. Δηλαδή **το διάστημα (support) της uniform φάσης αλλάζει ποιοτικά τη συμπεριφορά της ΤΔ** — κάτι που θα αξιοποιείς συνεχώς στο επόμενο κεφάλαιο.
</Example>
</ExamProblem>

## 11. Ανακάλεσε — drills

<ReorderDrill
  id="lotus-pipeline"
  prompt="Βάλε στη σωστή σειρά τα 5 βήματα για να υπολογίσεις τον μέσο μιας ΤΔ με τυχαία φάση μέσω LOTUS:"
  steps={[
    "Αναγνωρίζω την πηγή τυχαιότητας της ΤΔ (π.χ. μία ΤΜ φ).",
    "Σε σταθερό t, γράφω την X(t) ως g(φ) — μία ντετερμινιστική συνάρτηση της φ.",
    "Στήνω το LOTUS integral: ∫ g(φ) f_φ(φ) dφ πάνω στο support της φ (το διάστημα τιμών όπου f_φ ≠ 0).",
    "Ολοκληρώνω (αντιπαραγωγή της g θεωρώντας t σταθερό).",
    "Διαβάζω αν ο μέσος εξαρτάται από t — αν ναι, δεν είναι σταθερός, άρα η ΤΔ δεν είναι «στάσιμη ως προς τη μέση τιμή» (κριτήριο WSS, στο επόμενο κεφάλαιο).",
  ]}
/>

<ClozeDrill
  id="rv-fundamentals-fill"
  prompt="Συμπλήρωσε τις βασικές ιδιότητες:"
  parts={[
    'PDF: ολικό εμβαδόν κάτω από την f_X είναι ',
    { blank: 'pdf-area', accept: ['1'], hint: '∫f_X dx = ?' },
    '. Variance: σ² = E[X²] − ',
    { blank: 'var-mean-sq', accept: ['μ²', 'mu²', 'μ_x²', 'mux²', 'm²', 'm_x²'], hint: 'τι αφαιρείς από E[X²];' },
    '. Linearity E[aX+bY] = aE[X]+bE[Y] χρειάζεται ',
    { blank: 'lin-cond', accept: ['τίποτα', 'τιποτα', 'nothing', 'καμία προϋπόθεση', 'καμμία προϋπόθεση'], hint: 'υπό ποια προϋπόθεση;' },
    '. Variance sum Var(X+Y) = Var(X) + Var(Y) χρειάζεται ',
    { blank: 'var-cond', accept: ['cov=0', 'cov = 0', 'covariance=0', 'ασυσχέτιστες', 'ανεξάρτητες', 'independence', 'uncorrelated'], hint: 'τι σχέση πρέπει να έχουν;' },
    '. Uncorrelated ⇒ independent ισχύει μόνο για ',
    { blank: 'uncorr-indep', accept: ['gaussian', 'Gaussian', 'jointly gaussian', 'jointly Gaussian', 'κανονικές', 'κανονική'], hint: 'για ποια κατανομή;' },
    '.',
  ]}
  solution={
    <p>
      <strong>1</strong>, <strong>μ²</strong>, <strong>τίποτα</strong> (πάντα ισχύει),{' '}
      <strong>Cov = 0</strong> (ή ανεξάρτητες — ισχυρότερη συνθήκη),{' '}
      <strong>Gaussian</strong> (αυστηρά: jointly Gaussian).
    </p>
  }
/>

<RecallDrill
  id="distribution-stats"
  prompt="Από μνήμη: γράψε μέσο και variance για Uniform U(a,b), Gaussian N(μ,σ²), και Exponential(λ). Επίσης: ποιο E[cos Θ] και ποιο E[cos² Θ] για Θ ~ U(0, 2π);"
  answer={
    <ul className="space-y-1">
      <li><strong>Uniform U(a,b):</strong> E = (a+b)/2, Var = (b−a)²/12</li>
      <li><strong>Gaussian N(μ,σ²):</strong> E = μ, Var = σ²</li>
      <li><strong>Exponential(λ):</strong> E = 1/λ, Var = 1/λ²</li>
      <li><strong>E[cos Θ] = 0</strong> και <strong>E[cos² Θ] = 1/2</strong> για Θ ~ U(0, 2π)</li>
    </ul>
  }
/>

## 12. Αναγνώρισε — όταν δεις αυτές τις φράσεις στην εξέταση

<ThinkingPattern
  signals={[
    'τυχαία φάση φ ~ U[…]',
    'δοθείσα PDF f_X(x)',
    'E[g(X)] = ?',
    'άθροισμα ανεξάρτητων Gaussians',
    'E[X²] zero-mean θόρυβος',
    'λευκός vs Gaussian θόρυβος',
    'ασυσχέτιστες αλλά εξαρτημένες',
  ]}
>
  <ul className="ml-5 list-disc space-y-1.5">
    <li><strong>«τυχαία φάση φ ~ U[…]»</strong> → πάει εκεί όπου εφαρμόζεις LOTUS με $g(\phi)$ = το cosine/sine που σου δίνεται. Δες την «slide-15-bridge» άσκηση πάνω για το βήμα-βήμα.</li>
    <li><strong>«δοθείσα PDF»</strong> → ζητάει mean/variance/πιθανότητα. Όχι τύπος-από-καρδιάς· πρώτα ελέγχεις αν η εκφώνηση σου δίνει την PDF ή απλώς το όνομα της κατανομής (Gaussian/Uniform/Exponential).</li>
    <li><strong>«E[g(X)]»</strong> → LOTUS. Μη μπεις στον πειρασμό να βρεις πρώτα την κατανομή του $g(X)$.</li>
    <li><strong>«άθροισμα ανεξάρτητων Gaussians»</strong> → μέσος προσθέτεται γραμμικά, διασπορά **προστίθεται** (όχι √ άθροισμα — αυτό είναι το σ). Σε dB: +3 dB ανά διπλασιασμό.</li>
    <li><strong>«E[X²] zero-mean»</strong> → ισούται με $\sigma^2$. Συχνά λύση SNR.</li>
    <li><strong>«λευκός θόρυβος» vs «Gaussian θόρυβος»</strong> → δύο **διαφορετικές** ιδιότητες. Σ/Λ ερωτήσεις δοκιμάζουν αυτή την παγίδα κάθε χρόνο.</li>
    <li><strong>«ασυσχέτιστες»</strong> → όχι αυτόματα ανεξάρτητες. Έλεγξε αν είναι Gaussian πρώτα.</li>
  </ul>
</ThinkingPattern>

<ExamRadar
  title="Πού οι ΤΜ-έννοιες εμφανίζονται στα παλιά θέματα"
  ids={[
    {
      id: 'lec-rp-1',
      likelihood: 'high',
      note: 'Άσκηση 1 από τις διαφάνειες (slides 14–19): X(t) = Acos(2πf₁t + φ), φ~U[0,π]· Y(t) = αcos(2πf₂t), α~U[0,2]· φ,α ανεξάρτητες. Δοκιμάζει ολόκληρη τη ΤΜ-μηχανή σε ΤΔ — m_X, m_Y, R_X, R_XY, C_XY.',
    },
    {
      id: 'lec-rp-2',
      likelihood: 'high',
      note: 'Άσκηση 5 από τις διαφάνειες (slide 32): Z(t) = Acos(2πft + θ), θ~U[0,2π]. Ergodicity· δοκιμάζει τόσο τη ΤΜ-στατιστική όσο και τις χρονικές μέσες τιμές.',
    },
    {
      id: 'jan26-th1-3',
      likelihood: 'medium',
      note: 'Σ/Λ — «λευκός θόρυβος ⇔ Gaussian» — η κλασική παγίδα που σε αυτή τη σελίδα ξεκαθαρίσαμε (διαφορετικά concepts).',
    },
    {
      id: 'pa25-th1-5',
      likelihood: 'medium',
      note: 'Ίδια Σ/Λ με jan26-th1-3, repeat-group «tf-white-noise-gaussian» — μία από τις πιο συχνές παγίδες κάθε εξέταση.',
    },
    {
      id: 'pa25-th2-2',
      likelihood: 'medium',
      note: 'Σ/Λ — «θερμικός θόρυβος Gauss» — δοκιμάζει αν αναγνωρίζεις ότι «κατανομή πλάτους» (Gaussian) είναι ξεχωριστό από «PSD» (επίπεδη = λευκός).',
    },
    {
      id: 'sept25-th3-10',
      likelihood: 'low',
      note: 'PSD θερμικού θορύβου — εφαρμόζει τα ΤΜ-εργαλεία (mean, autocorrelation) στο θερμικό θόρυβο. Δες πώς η Gaussian-κατανομή πλάτους + η flat-PSD δουλεύουν μαζί.',
    },
  ]}
/>

## 13. Πού θα χρειαστείς αυτές τις έννοιες αργότερα

- **[/randomness/random-processes](/randomness/random-processes)** — κάθε $m_X(t)$, $R_X(t_1, t_2)$, $C_X$ θα υπολογιστεί με LOTUS πάνω στις παραμέτρους τυχαιότητας της ΤΔ (όπως είδαμε στην slide-15-bridge άσκηση). **Αυτό είναι το αμέσως επόμενο κεφάλαιο.**
- **[/randomness/stationarity](/randomness/stationarity)** — η συνθήκη WSS είναι «μέσος **σταθερός** στο χρόνο, αυτοσυσχέτιση εξαρτάται μόνο από τη χρονική διαφορά $\tau$». Η §4α (mean) και η §3 (PDF/CDF) σου δίνουν τις στατιστικές που πρέπει να σταθεροποιηθούν.
- **[/randomness/psd](/randomness/psd)** — η Wiener-Khinchin είναι ο **Fourier transform** της autocorrelation $R_X(\tau)$. Η $R_X$ είναι το $E[X(t)X(t+\tau)]$ — πάλι ΤΜ-εκφώνηση πάνω στο joint distribution των $X(t), X(t+\tau)$ (§6α).
- **[/noise/sources](/noise/sources)** — ο θερμικός θόρυβος έχει **Gaussian κατανομή πλάτους** (§5β). Το γιατί ακριβώς Gaussian (CLT) είναι load-bearing για το AWGN μοντέλο.
- **[/noise/through-filters](/noise/through-filters)** — Gaussian θόρυβος μέσα από LTI **παραμένει Gaussian**. Αυτή είναι μια κρίσιμη ιδιότητα της Gaussian κατανομής (συνεπώς της §5β) που χρησιμοποιείται διαρκώς.
- **[/noise/bandpass](/noise/bandpass)** — οι I/Q συνιστώσες του bandpass θορύβου είναι **κάθε μία Gaussian** και ανεξάρτητες μεταξύ τους — εδώ ο συνδυασμός §5β (Gaussian) + §6β (independence) πληρώνει.
- **[/am/modulator-demodulator](/am/modulator-demodulator)** — η ανάλυση AM-σήματος μέσα σε θόρυβο χρησιμοποιεί $E[n^2(t)] = \sigma_n^2$ (§4β-§5β) ως ισχύ θορύβου στο SNR.

## 14. Συμπύκνωσε — όλο το κεφάλαιο

<RecallCard
  variant="summary"
  keywords={[
    'PDF f_X(x), εμβαδόν = 1',
    'μέσος μ = ∫x f_X, διασπορά σ² = E[X²] − μ²',
    'LOTUS: E[g(X)] = ∫g(x) f_X(x) dx',
    'Uniform: U(0,2π) μηδενίζει cos',
    'Gaussian: AWGN, CLT, σ² = ισχύς zero-mean',
    'Linearity πάντα, Var-sum υπό Cov=0',
    'Indep ⇒ uncorr (αντίστροφο μόνο Gaussian)',
    'ΤΜ → ΤΔ: LOTUS σε σταθερό t',
  ]}
  skeleton={[
    'Δες αν σου δίνουν PDF ή απλώς κατανομή — αν δεύτερο, ανέκαλεσε τύπους από §5.',
    'Για μέσο/διασπορά μιας ΤΜ: ολοκληρώματα §4· για E[g(X)]: LOTUS §7.',
    'Για άθροισμα ΤΜ: linearity για mean (πάντα), Cov ελέγχει το cross term για variance.',
    'Για 2 ΤΜ: γράφε joint, έλεγξε independence, υπολόγισε Cov με E[XY] − μ_X μ_Y.',
    'Για ΤΔ X(t): σε σταθερό t είναι ΤΜ → εφάρμοσε §§3-7 πάνω στην παράμετρο τυχαιότητας.',
    'Αναγνώρισε τις Σ/Λ παγίδες: λευκός ≠ Gaussian, uncorr ≠ indep (εκτός Gaussian).',
  ]}
  trap={
    <>
      Δύο επικίνδυνες ταυτίσεις: <strong>(α)</strong> «λευκός» με «Gaussian» — είναι δύο διαφορετικές ιδιότητες (φασματική vs αμπλιτουδική), και <strong>(β)</strong> «ασυσχέτιστες» με «ανεξάρτητες» — η ισοδυναμία ισχύει μόνο για jointly Gaussian.
    </>
  }
/>

<Recap>
- Μια **τυχαία μεταβλητή** χαρακτηρίζεται πλήρως από την PDF (ή CDF). Από αυτές παίρνεις mean, variance, και κάθε άλλη στατιστική.
- Οι σημαντικότερες κατανομές για το K21: **Uniform** (τυχαία φάση), **Gaussian** (θερμικός θόρυβος, CLT).
- **LOTUS** είναι ο πιο χρήσιμος τύπος του κεφαλαίου — $E[g(X)] = \int g(x) f_X(x) dx$ χωρίς να βρεις πρώτα την κατανομή του $g(X)$.
- **Linearity πάντα** ισχύει για mean, αλλά για variance χρειάζεσαι **Cov = 0**.
- **Independence ⇒ zero covariance**, αλλά zero covariance δεν συνεπάγεται γενικά independence (εκτός από Gaussian).
- **Δύο Σ/Λ παγίδες:** «λευκός θόρυβος» ≠ «Gaussian θόρυβος», «ασυσχέτιστες» ≠ «ανεξάρτητες» γενικά.
- **Η γέφυρα προς το επόμενο κεφάλαιο:** για κάθε ΤΔ $X(t)$, σε σταθερό $t$ είναι ΤΜ — εφαρμόζεις LOTUS πάνω στην παράμετρο τυχαιότητας (slide 15 derivation).
</Recap>

<NextUp slug="randomness/random-processes" />
