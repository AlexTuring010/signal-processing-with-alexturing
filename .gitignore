# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# claude code local config — user-specific files only.
# Project-shared tooling (skills, commands) is checked in.
.claude/*
!.claude/skills/
!.claude/commands/

# typescript
*.tsbuildinfo
next-env.d.ts.local

# sprite preview renders (the script is checked in; outputs are throwaway)
scripts/sprite-preview/tmp/

# raw university exam / frontistirio materials — never commit or publish.
# Scanned past-exam papers and frontistirio decks. They are transcribed
# (anonymised) into content/practice/exercises.tsx; the raw files stay
# local-only. See plans/EXAM_TRANSCRIPTION.md.
/private_material
