/**
 * Quiz bank — True/False + Multiple Choice questions for the quiz modes.
 *
 * Strategy: cover every chapter group with at least 3 questions, mix
 * difficulty, and spotlight the recurring exam traps (e.g. "Dijkstra works
 * with negative edges" → false).
 *
 * Status: empty stub. Populate lecture-by-lecture as we move through the
 * syllabus. The /practice/quiz route still renders an empty-state.
 */

import type { QuizQuestion } from './types'

export const QUIZ_BANK: QuizQuestion[] = []
